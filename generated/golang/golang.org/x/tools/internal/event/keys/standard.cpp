// generated by GoCpp from file '$(ImportDir)/golang.org/x/tools/internal/event/keys/standard.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "golang/golang.org/x/tools/internal/event/keys/standard.h"
#include "gocpp/support.h"

#include "golang/golang.org/x/tools/internal/event/keys/keys.h"

namespace golang::keys
{
    namespace rec
    {
    }

    // Msg is a key used to add message strings to label lists.
    keys::String* Msg = NewString("message"_s, "a readable message"_s);
    // Label is a key used to indicate an event adds labels to the context.
    keys::Tag* Label = NewTag("label"_s, "a label context marker"_s);
    // Start is used for things like traces that have a name.
    keys::String* Start = NewString("start"_s, "span start"_s);
    // Metric is a key used to indicate an event records metrics.
    keys::Tag* End = NewTag("end"_s, "a span end marker"_s);
    // Metric is a key used to indicate an event records metrics.
    keys::Tag* Detach = NewTag("detach"_s, "a span detach marker"_s);
    // Err is a key used to add error values to label lists.
    keys::Error* Err = NewError("error"_s, "an error that occurred"_s);
    // Metric is a key used to indicate an event records metrics.
    keys::Tag* Metric = NewTag("metric"_s, "a metric event marker"_s);
}

