// generated by GoCpp from file 'tests/TourOfGo/flowcontrol/if-and-else.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "tests/TourOfGo/flowcontrol/if-and-else.h"
#include "gocpp/support.h"

#include "golang/fmt/print.h"
#include "golang/math/pow.h"

namespace golang::main
{
    namespace rec
    {
        using namespace mocklib::rec;
        using namespace fmt::rec;
        using namespace math::rec;
    }

    double pow(double x, double n, double lim)
    {
        if(auto v = mocklib::Pow(x, n); v < lim)
        {
            return v;
        }
        else
        {
            mocklib::Printf("%g >= %g\n", v, lim);
        }
        return lim;
    }

    void main()
    {
        mocklib::Println(pow(3, 2, 10), pow(3, 3, 20));
    }

}

int main()
{
    try
    {
        std::cout << std::boolalpha << std::setprecision(5) << std::fixed;
        golang::main::main();
        return 0;
    }
    catch(const gocpp::GoPanic& ex)
    {
        std::cout << "Panic: " << ex.what() << std::endl;
        return -1;
    }
}
