// generated by GoCpp from file '$(ImportDir)/golang.org/x/tools/internal/event/event.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "golang/golang.org/x/tools/internal/event/event.h"
#include "gocpp/support.h"

#include "golang/context/context.h"
#include "golang/golang.org/x/tools/internal/event/core/event.h"
#include "golang/golang.org/x/tools/internal/event/core/export.h"
#include "golang/golang.org/x/tools/internal/event/keys/keys.h"
#include "golang/golang.org/x/tools/internal/event/keys/standard.h"
#include "golang/golang.org/x/tools/internal/event/label/label.h"

namespace golang::event
{
    namespace rec
    {
        using core::rec::Label;
        using keys::rec::New;
        using keys::rec::Of;
        using label::rec::Key;
    }

    // Exporter is a function that handles events.
    // It may return a modified context and event.
    // SetExporter sets the global exporter function that handles all events.
    // The exporter is called synchronously from the event call site, so it should
    // return quickly so as not to hold up user code.
    void SetExporter(Exporter e)
    {
        core::SetExporter(core::Exporter(e));
    }

    // Log takes a message and a label list and combines them into a single event
    // before delivering them to the exporter.
    void Log(context::Context ctx, gocpp::string message, gocpp::slice<label::Label> labels)
    {
        core::Export(ctx, core::MakeEvent(gocpp::array<label::Label, 3> {
            rec::Of(gocpp::recv(keys::Msg), message)
        }, labels));
    }

    // IsLog returns true if the event was built by the Log function.
    // It is intended to be used in exporters to identify the semantics of the
    // event when deciding what to do with it.
    bool IsLog(core::Event ev)
    {
        return rec::Key(gocpp::recv(rec::Label(gocpp::recv(ev), 0))) == keys::Msg;
    }

    // Error takes a message and a label list and combines them into a single event
    // before delivering them to the exporter. It captures the error in the
    // delivered event.
    void Error(context::Context ctx, gocpp::string message, gocpp::error err, gocpp::slice<label::Label> labels)
    {
        core::Export(ctx, core::MakeEvent(gocpp::array<label::Label, 3> {
            rec::Of(gocpp::recv(keys::Msg), message),
            rec::Of(gocpp::recv(keys::Err), err)
        }, labels));
    }

    // IsError returns true if the event was built by the Error function.
    // It is intended to be used in exporters to identify the semantics of the
    // event when deciding what to do with it.
    bool IsError(core::Event ev)
    {
        return rec::Key(gocpp::recv(rec::Label(gocpp::recv(ev), 0))) == keys::Msg &&
                rec::Key(gocpp::recv(rec::Label(gocpp::recv(ev), 1))) == keys::Err;
    }

    // Metric sends a label event to the exporter with the supplied labels.
    void Metric(context::Context ctx, gocpp::slice<label::Label> labels)
    {
        core::Export(ctx, core::MakeEvent(gocpp::array<label::Label, 3> {
            rec::New(gocpp::recv(keys::Metric))
        }, labels));
    }

    // IsMetric returns true if the event was built by the Metric function.
    // It is intended to be used in exporters to identify the semantics of the
    // event when deciding what to do with it.
    bool IsMetric(core::Event ev)
    {
        return rec::Key(gocpp::recv(rec::Label(gocpp::recv(ev), 0))) == keys::Metric;
    }

    // Label sends a label event to the exporter with the supplied labels.
    context::Context Label(context::Context ctx, gocpp::slice<label::Label> labels)
    {
        return core::Export(ctx, core::MakeEvent(gocpp::array<label::Label, 3> {
            rec::New(gocpp::recv(keys::Label))
        }, labels));
    }

    // IsLabel returns true if the event was built by the Label function.
    // It is intended to be used in exporters to identify the semantics of the
    // event when deciding what to do with it.
    bool IsLabel(core::Event ev)
    {
        return rec::Key(gocpp::recv(rec::Label(gocpp::recv(ev), 0))) == keys::Label;
    }

    // Start sends a span start event with the supplied label list to the exporter.
    // It also returns a function that will end the span, which should normally be
    // deferred.
    std::tuple<context::Context, std::function<void ()>> Start(context::Context ctx, gocpp::string name, gocpp::slice<label::Label> labels)
    {
        return core::ExportPair(ctx, core::MakeEvent(gocpp::array<label::Label, 3> {
            rec::Of(gocpp::recv(keys::Start), name)
        }, labels), core::MakeEvent(gocpp::array<label::Label, 3> {
            rec::New(gocpp::recv(keys::End))
        }, nullptr));
    }

    // IsStart returns true if the event was built by the Start function.
    // It is intended to be used in exporters to identify the semantics of the
    // event when deciding what to do with it.
    bool IsStart(core::Event ev)
    {
        return rec::Key(gocpp::recv(rec::Label(gocpp::recv(ev), 0))) == keys::Start;
    }

    // IsEnd returns true if the event was built by the End function.
    // It is intended to be used in exporters to identify the semantics of the
    // event when deciding what to do with it.
    bool IsEnd(core::Event ev)
    {
        return rec::Key(gocpp::recv(rec::Label(gocpp::recv(ev), 0))) == keys::End;
    }

    // Detach returns a context without an associated span.
    // This allows the creation of spans that are not children of the current span.
    context::Context Detach(context::Context ctx)
    {
        return core::Export(ctx, core::MakeEvent(gocpp::array<label::Label, 3> {
            rec::New(gocpp::recv(keys::Detach))
        }, nullptr));
    }

    // IsDetach returns true if the event was built by the Detach function.
    // It is intended to be used in exporters to identify the semantics of the
    // event when deciding what to do with it.
    bool IsDetach(core::Event ev)
    {
        return rec::Key(gocpp::recv(rec::Label(gocpp::recv(ev), 0))) == keys::Detach;
    }

}

