// generated by GoCpp from file '$(ImportDir)/container/heap/heap.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/container/heap/heap.fwd.h"
#include "gocpp/support.h"

#include "golang/sort/sort.h"

namespace golang::heap
{
    struct Interface : virtual gocpp::Interface, sort::Interface
    {
        using gocpp::Interface::operator==;
        using gocpp::Interface::operator!=;

        Interface(){}
        Interface(Interface& i) = default;
        Interface(const Interface& i) = default;
        Interface& operator=(Interface& i) = default;
        Interface& operator=(const Interface& i) = default;

        inline Interface(nullptr_t) {};
        Interface& operator=(nullptr_t) { mValue.reset(); }

        template<typename T>
        Interface(T& ref);

        template<typename T>
        Interface(const T& ref);

        template<typename T>
        Interface(T* ptr);

        using isGoInterface = void;

        std::ostream& PrintTo(std::ostream& os) const;

        struct IInterface: virtual sort::Interface::IInterface
        {
            virtual void vPush(go_any x) = 0; // add x as element Len()
            virtual go_any vPop() = 0; // remove and return element Len() - 1.
            virtual void* getPtr() = 0;
        };

        template<typename T, typename TStore, typename TInterface = IInterface>
        struct InterfaceImpl : virtual TInterface, virtual sort::Interface::InterfaceImpl<T, TStore, TInterface>
        {
            explicit InterfaceImpl(T* ptr): sort::Interface::InterfaceImpl<T, TStore, TInterface>(ptr)
            {
                value.reset(ptr);
            }

            void vPush(go_any x) override;

            go_any vPop() override;

            void* getPtr() override
            {
                return value.get();
            }

            TStore value;
        };

        inline IInterface* value() const;

        std::shared_ptr<IInterface> mValue;
    };

    namespace rec
    {
        void Push(const gocpp::PtrRecv<struct Interface, false>& self, go_any x);
        void Push(const gocpp::ObjRecv<struct Interface>& self, go_any x);

        go_any Pop(const gocpp::PtrRecv<struct Interface, false>& self);
        go_any Pop(const gocpp::ObjRecv<struct Interface>& self);

        int Len(const gocpp::PtrRecv<struct Interface, false>& self);
        int Len(const gocpp::ObjRecv<struct Interface>& self);

        bool Less(const gocpp::PtrRecv<struct Interface, false>& self, int i, int j);
        bool Less(const gocpp::ObjRecv<struct Interface>& self, int i, int j);

        void Swap(const gocpp::PtrRecv<struct Interface, false>& self, int i, int j);
        void Swap(const gocpp::ObjRecv<struct Interface>& self, int i, int j);
    }

    std::ostream& operator<<(std::ostream& os, const struct Interface& value);
    void Init(Interface h);
    void Push(Interface h, go_any x);
    go_any Pop(Interface h);
    go_any Remove(Interface h, int i);
    void Fix(Interface h, int i);
    void up(Interface h, int j);
    bool down(Interface h, int i0, int n);

    namespace rec
    {
    }
}

