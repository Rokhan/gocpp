// generated by GoCpp from file '$(ImportDir)/go/types/slice.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "golang/go/types/slice.h"
#include "gocpp/support.h"

#include "golang/go/types/type.h"
#include "golang/go/types/typestring.h"

namespace golang::types
{
    namespace rec
    {
    }

    // A Slice represents a slice type.
    
    template<typename T> requires gocpp::GoStruct<T>
    Slice::operator T()
    {
        T result;
        result.elem = this->elem;
        return result;
    }

    template<typename T> requires gocpp::GoStruct<T>
    bool Slice::operator==(const T& ref) const
    {
        if (elem != ref.elem) return false;
        return true;
    }

    std::ostream& Slice::PrintTo(std::ostream& os) const
    {
        os << '{';
        os << "" << elem;
        os << '}';
        return os;
    }

    std::ostream& operator<<(std::ostream& os, const struct Slice& value)
    {
        return value.PrintTo(os);
    }

    // NewSlice returns a new slice type for the given element type.
    Slice* NewSlice(golang::types::Type elem)
    {
        return gocpp::InitPtr<Slice>([=](auto& x) {
            x.elem = elem;
        });
    }

    // Elem returns the element type of slice s.
    golang::types::Type rec::Elem(Slice* s)
    {
        return s->elem;
    }

    golang::types::Type rec::Underlying(Slice* s)
    {
        return s;
    }

    gocpp::string rec::String(Slice* s)
    {
        return TypeString(s, nullptr);
    }

}

