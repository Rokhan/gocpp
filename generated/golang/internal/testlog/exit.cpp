// generated by GoCpp from file '$(ImportDir)/internal/testlog/exit.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "golang/internal/testlog/exit.h"
#include "gocpp/support.h"

#include "golang/sync/mutex.h"

namespace golang::testlog
{
    namespace rec
    {
        using mocklib::rec::Lock;
        using mocklib::rec::Unlock;
    }

    // PanicOnExit0 reports whether to panic on a call to os.Exit(0).
    // This is in the testlog package because, like other definitions in
    // package testlog, it is a hook between the testing package and the
    // os package. This is used to ensure that an early call to os.Exit(0)
    // does not cause a test to pass.
    bool PanicOnExit0()
    {
        gocpp::Defer defer;
        try
        {
            rec::Lock(gocpp::recv(panicOnExit0.mu));
            defer.push_back([=]{ rec::Unlock(gocpp::recv(panicOnExit0.mu)); });
            return panicOnExit0.val;
        }
        catch(gocpp::GoPanic& gp)
        {
            defer.handlePanic(gp);
        }
    }

    
    template<typename T> requires gocpp::GoStruct<T>
    panicOnExit0Struct::operator T()
    {
        T result;
        result.mu = this->mu;
        result.val = this->val;
        return result;
    }

    template<typename T> requires gocpp::GoStruct<T>
    bool panicOnExit0Struct::operator==(const T& ref) const
    {
        if (mu != ref.mu) return false;
        if (val != ref.val) return false;
        return true;
    }

    std::ostream& panicOnExit0Struct::PrintTo(std::ostream& os) const
    {
        os << '{';
        os << "" << mu;
        os << " " << val;
        os << '}';
        return os;
    }

    std::ostream& operator<<(std::ostream& os, const struct panicOnExit0Struct& value)
    {
        return value.PrintTo(os);
    }


    // panicOnExit0 is the flag used for PanicOnExit0. This uses a lock
    // because the value can be cleared via a timer call that may race
    // with calls to os.Exit
    panicOnExit0Struct panicOnExit0;
    // SetPanicOnExit0 sets panicOnExit0 to v.
    void SetPanicOnExit0(bool v)
    {
        gocpp::Defer defer;
        try
        {
            rec::Lock(gocpp::recv(panicOnExit0.mu));
            defer.push_back([=]{ rec::Unlock(gocpp::recv(panicOnExit0.mu)); });
            panicOnExit0.val = v;
        }
        catch(gocpp::GoPanic& gp)
        {
            defer.handlePanic(gp);
        }
    }

}

