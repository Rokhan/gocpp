// generated by GoCpp from file '$(ImportDir)/golang.org/x/sync/errgroup/errgroup.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/golang.org/x/sync/errgroup/errgroup.fwd.h"
#include "gocpp/support.h"


namespace golang::errgroup
{
    struct token
    {

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct token& value);
}
#include "golang/sync/once.h"
#include "golang/sync/waitgroup.h"

namespace golang::errgroup
{
    struct Group
    {
        std::function<void (gocpp::error _1)> cancel{};
        sync::WaitGroup wg{};
        gocpp::channel<token> sem{};
        sync::Once errOnce{};
        gocpp::error err{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct Group& value);
}
#include "golang/context/context.h"

namespace golang::errgroup
{
    std::tuple<Group*, context::Context> WithContext(context::Context ctx);

    namespace rec
    {
        void done(Group* g);
        gocpp::error Wait(Group* g);
        void Go(Group* g, std::function<gocpp::error ()> f);
        bool TryGo(Group* g, std::function<gocpp::error ()> f);
        void SetLimit(Group* g, int n);
    }
}

