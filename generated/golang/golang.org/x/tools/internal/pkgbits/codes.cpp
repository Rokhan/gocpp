// generated by GoCpp from file '$(ImportDir)/golang.org/x/tools/internal/pkgbits/codes.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "golang/golang.org/x/tools/internal/pkgbits/codes.h"
#include "gocpp/support.h"

#include "golang/golang.org/x/tools/internal/pkgbits/sync.h"

namespace golang::pkgbits
{
    namespace rec
    {
    }

    // A Code is an enum value that can be encoded into bitstreams.
    //
    // Code types are preferable for enum types, because they allow
    // Decoder to detect desyncs.
    
    template<typename T>
    Code::Code(T& ref)
    {
        mValue.reset(new CodeImpl<T, std::unique_ptr<T>>(new T(ref)));
    }

    template<typename T>
    Code::Code(const T& ref)
    {
        mValue.reset(new CodeImpl<T, std::unique_ptr<T>>(new T(ref)));
    }

    template<typename T>
    Code::Code(T* ptr)
    {
        mValue.reset(new CodeImpl<T, gocpp::ptr<T>>(ptr));
    }

    std::ostream& Code::PrintTo(std::ostream& os) const
    {
        return os;
    }

    template<typename T, typename TStore, typename TInterface>
    SyncMarker Code::CodeImpl<T, TStore, TInterface>::vMarker()
    {
        return rec::Marker(gocpp::PtrRecv<T, false>(value.get()));
    }
    template<typename T, typename TStore, typename TInterface>
    int Code::CodeImpl<T, TStore, TInterface>::vValue()
    {
        return rec::Value(gocpp::PtrRecv<T, false>(value.get()));
    }

    inline Code::ICode* Code::value() const
    {
        if(auto res = mValue.get()) { return res; }
        throw gocpp::GoPanic("using nil value for interface 'Code'");
    }

    namespace rec
    {
        SyncMarker Marker(const gocpp::PtrRecv<struct Code, false>& self)
        {
            return self.ptr->value()->vMarker();
        }

        SyncMarker Marker(const gocpp::ObjRecv<struct Code>& self)
        {
            return self.obj.value()->vMarker();
        }

        int Value(const gocpp::PtrRecv<struct Code, false>& self)
        {
            return self.ptr->value()->vValue();
        }

        int Value(const gocpp::ObjRecv<struct Code>& self)
        {
            return self.obj.value()->vValue();
        }
    }

    std::ostream& operator<<(std::ostream& os, const struct Code& value)
    {
        return value.PrintTo(os);
    }

    // A CodeVal distinguishes among go/constant.Value encodings.
    SyncMarker rec::Marker(CodeVal c)
    {
        return SyncVal;
    }

    int rec::Value(CodeVal c)
    {
        return int(c);
    }

    // A CodeType distinguishes among go/types.Type encodings.
    SyncMarker rec::Marker(CodeType c)
    {
        return SyncType;
    }

    int rec::Value(CodeType c)
    {
        return int(c);
    }

    // A CodeObj distinguishes among go/types.Object encodings.
    SyncMarker rec::Marker(CodeObj c)
    {
        return SyncCodeObj;
    }

    int rec::Value(CodeObj c)
    {
        return int(c);
    }

}

