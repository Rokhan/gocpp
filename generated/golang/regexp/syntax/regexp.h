// generated by GoCpp from file '$(ImportDir)/regexp/syntax/regexp.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/regexp/syntax/regexp.fwd.h"
#include "gocpp/support.h"

#include "golang/regexp/syntax/parse.h"
#include "golang/strings/builder.fwd.h"

namespace golang::syntax
{
    struct Regexp
    {
        Op Op{}; // operator
        Flags Flags{};
        gocpp::slice<Regexp*> Sub{}; // subexpressions, if any
        gocpp::array<Regexp*, 1> Sub0{}; // storage for short Sub
        gocpp::slice<gocpp::rune> Rune{}; // matched runes, for OpLiteral, OpCharClass
        gocpp::array<gocpp::rune, 2> Rune0{}; // storage for short Rune
        int Min{}; // min, max for OpRepeat
        int Max{};
        int Cap{}; // capturing index, for OpCapture
        gocpp::string Name{}; // capturing name, for OpCapture

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct Regexp& value);
    void escape(strings::Builder* b, gocpp::rune r, bool force);
    void addSpan(Regexp* start, Regexp* last, printFlags f, gocpp::map<Regexp*, printFlags>* flags);
    std::tuple<printFlags, printFlags> calcFlags(Regexp* re, gocpp::map<Regexp*, printFlags>* flags);
    void writeRegexp(strings::Builder* b, Regexp* re, printFlags f, gocpp::map<Regexp*, printFlags> flags);

    namespace rec
    {
        bool Equal(Regexp* x, Regexp* y);
        gocpp::string String(Regexp* re);
        int MaxCap(Regexp* re);
        gocpp::slice<gocpp::string> CapNames(Regexp* re);
        void capNames(Regexp* re, gocpp::slice<gocpp::string> names);
    }
}

