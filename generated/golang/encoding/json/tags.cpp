// generated by GoCpp from file '$(ImportDir)/encoding/json/tags.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "golang/encoding/json/tags.h"
#include "gocpp/support.h"

#include "golang/strings/strings.h"

namespace golang::json
{
    namespace rec
    {
    }

    // tagOptions is the string following a comma in a struct field's "json"
    // tag, or the empty string. It does not include the leading comma.
    // parseTag splits a struct field's json tag into its name and
    // comma-separated options.
    std::tuple<gocpp::string, tagOptions> parseTag(gocpp::string tag)
    {
        auto [tag_tmp, opt, gocpp_id_0] = strings::Cut(tag, ","_s);
        auto& tag = tag_tmp;
        return {tag, tagOptions(opt)};
    }

    // Contains reports whether a comma-separated list of options
    // contains a particular substr flag. substr must be surrounded by a
    // string boundary or commas.
    bool rec::Contains(tagOptions o, gocpp::string optionName)
    {
        if(len(o) == 0)
        {
            return false;
        }
        auto s = gocpp::string(o);
        for(; s != ""_s; )
        {
            gocpp::string name = {};
            std::tie(name, s, std::ignore) = strings::Cut(s, ","_s);
            if(name == optionName)
            {
                return true;
            }
        }
        return false;
    }

}

