// generated by GoCpp from file '$(ImportDir)/golang.org/x/tools/internal/pkgbits/reloc.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "golang/golang.org/x/tools/internal/pkgbits/reloc.h"
#include "gocpp/support.h"

namespace golang::pkgbits
{
    namespace rec
    {
    }

    // A RelocKind indicates a particular section within a unified IR export.
    // An Index represents a bitstream element index within a particular
    // section.
    // A relocEnt (relocation entry) is an entry in an element's local
    // reference table.
    //
    // TODO(mdempsky): Rename this too.
    
    template<typename T> requires gocpp::GoStruct<T>
    RelocEnt::operator T()
    {
        T result;
        result.Kind = this->Kind;
        result.Idx = this->Idx;
        return result;
    }

    template<typename T> requires gocpp::GoStruct<T>
    bool RelocEnt::operator==(const T& ref) const
    {
        if (Kind != ref.Kind) return false;
        if (Idx != ref.Idx) return false;
        return true;
    }

    std::ostream& RelocEnt::PrintTo(std::ostream& os) const
    {
        os << '{';
        os << "" << Kind;
        os << " " << Idx;
        os << '}';
        return os;
    }

    std::ostream& operator<<(std::ostream& os, const struct RelocEnt& value)
    {
        return value.PrintTo(os);
    }

}

