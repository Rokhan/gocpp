// generated by GoCpp from file '$(ImportDir)/golang.org/x/tools/internal/event/label/label.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "golang/golang.org/x/tools/internal/event/label/label.h"
#include "gocpp/support.h"

#include "golang/fmt/print.h"
#include "golang/io/io.h"
#include "golang/reflect/value.h"

namespace golang::label
{
    namespace rec
    {
    }

    // Key is used as the identity of a Label.
    // Keys are intended to be compared by pointer only, the name should be unique
    // for communicating with external systems, but it is not required or enforced.
    
    template<typename T>
    Key::Key(T& ref)
    {
        mValue.reset(new KeyImpl<T, std::unique_ptr<T>>(new T(ref)));
    }

    template<typename T>
    Key::Key(const T& ref)
    {
        mValue.reset(new KeyImpl<T, std::unique_ptr<T>>(new T(ref)));
    }

    template<typename T>
    Key::Key(T* ptr)
    {
        mValue.reset(new KeyImpl<T, gocpp::ptr<T>>(ptr));
    }

    std::ostream& Key::PrintTo(std::ostream& os) const
    {
        return os;
    }

    template<typename T, typename TStore, typename TInterface>
    gocpp::string Key::KeyImpl<T, TStore, TInterface>::vName()
    {
        return rec::Name(gocpp::PtrRecv<T, false>(value.get()));
    }
    template<typename T, typename TStore, typename TInterface>
    gocpp::string Key::KeyImpl<T, TStore, TInterface>::vDescription()
    {
        return rec::Description(gocpp::PtrRecv<T, false>(value.get()));
    }
    template<typename T, typename TStore, typename TInterface>
    void Key::KeyImpl<T, TStore, TInterface>::vFormat(io::Writer w, gocpp::slice<unsigned char> buf, golang::label::Label l)
    {
        return rec::Format(gocpp::PtrRecv<T, false>(value.get()), w, buf, l);
    }

    inline Key::IKey* Key::value() const
    {
        if(auto res = mValue.get()) { return res; }
        throw gocpp::GoPanic("using nil value for interface 'Key'");
    }

    namespace rec
    {
        gocpp::string Name(const gocpp::PtrRecv<struct Key, false>& self)
        {
            return self.ptr->value()->vName();
        }

        gocpp::string Name(const gocpp::ObjRecv<struct Key>& self)
        {
            return self.obj.value()->vName();
        }

        gocpp::string Description(const gocpp::PtrRecv<struct Key, false>& self)
        {
            return self.ptr->value()->vDescription();
        }

        gocpp::string Description(const gocpp::ObjRecv<struct Key>& self)
        {
            return self.obj.value()->vDescription();
        }

        void Format(const gocpp::PtrRecv<struct Key, false>& self, io::Writer w, gocpp::slice<unsigned char> buf, golang::label::Label l)
        {
            return self.ptr->value()->vFormat(w, buf, l);
        }

        void Format(const gocpp::ObjRecv<struct Key>& self, io::Writer w, gocpp::slice<unsigned char> buf, golang::label::Label l)
        {
            return self.obj.value()->vFormat(w, buf, l);
        }
    }

    std::ostream& operator<<(std::ostream& os, const struct Key& value)
    {
        return value.PrintTo(os);
    }

    // Label holds a key and value pair.
    // It is normally used when passing around lists of labels.
    
    template<typename T> requires gocpp::GoStruct<T>
    Label::operator T()
    {
        T result;
        result.key = this->key;
        result.packed = this->packed;
        result.untyped = this->untyped;
        return result;
    }

    template<typename T> requires gocpp::GoStruct<T>
    bool Label::operator==(const T& ref) const
    {
        if (key != ref.key) return false;
        if (packed != ref.packed) return false;
        if (untyped != ref.untyped) return false;
        return true;
    }

    std::ostream& Label::PrintTo(std::ostream& os) const
    {
        os << '{';
        os << "" << key;
        os << " " << packed;
        os << " " << untyped;
        os << '}';
        return os;
    }

    std::ostream& operator<<(std::ostream& os, const struct Label& value)
    {
        return value.PrintTo(os);
    }

    // Map is the interface to a collection of Labels indexed by key.
    
    template<typename T>
    Map::Map(T& ref)
    {
        mValue.reset(new MapImpl<T, std::unique_ptr<T>>(new T(ref)));
    }

    template<typename T>
    Map::Map(const T& ref)
    {
        mValue.reset(new MapImpl<T, std::unique_ptr<T>>(new T(ref)));
    }

    template<typename T>
    Map::Map(T* ptr)
    {
        mValue.reset(new MapImpl<T, gocpp::ptr<T>>(ptr));
    }

    std::ostream& Map::PrintTo(std::ostream& os) const
    {
        return os;
    }

    template<typename T, typename TStore, typename TInterface>
    golang::label::Label Map::MapImpl<T, TStore, TInterface>::vFind(golang::label::Key key)
    {
        return rec::Find(gocpp::PtrRecv<T, false>(value.get()), key);
    }

    inline Map::IMap* Map::value() const
    {
        if(auto res = mValue.get()) { return res; }
        throw gocpp::GoPanic("using nil value for interface 'Map'");
    }

    namespace rec
    {
        golang::label::Label Find(const gocpp::PtrRecv<struct Map, false>& self, golang::label::Key key)
        {
            return self.ptr->value()->vFind(key);
        }

        golang::label::Label Find(const gocpp::ObjRecv<struct Map>& self, golang::label::Key key)
        {
            return self.obj.value()->vFind(key);
        }
    }

    std::ostream& operator<<(std::ostream& os, const struct Map& value)
    {
        return value.PrintTo(os);
    }

    // List is the interface to something that provides an iterable
    // list of labels.
    // Iteration should start from 0 and continue until Valid returns false.
    
    template<typename T>
    List::List(T& ref)
    {
        mValue.reset(new ListImpl<T, std::unique_ptr<T>>(new T(ref)));
    }

    template<typename T>
    List::List(const T& ref)
    {
        mValue.reset(new ListImpl<T, std::unique_ptr<T>>(new T(ref)));
    }

    template<typename T>
    List::List(T* ptr)
    {
        mValue.reset(new ListImpl<T, gocpp::ptr<T>>(ptr));
    }

    std::ostream& List::PrintTo(std::ostream& os) const
    {
        return os;
    }

    template<typename T, typename TStore, typename TInterface>
    bool List::ListImpl<T, TStore, TInterface>::vValid(int index)
    {
        return rec::Valid(gocpp::PtrRecv<T, false>(value.get()), index);
    }
    template<typename T, typename TStore, typename TInterface>
    golang::label::Label List::ListImpl<T, TStore, TInterface>::vLabel(int index)
    {
        return rec::Label(gocpp::PtrRecv<T, false>(value.get()), index);
    }

    inline List::IList* List::value() const
    {
        if(auto res = mValue.get()) { return res; }
        throw gocpp::GoPanic("using nil value for interface 'List'");
    }

    namespace rec
    {
        bool Valid(const gocpp::PtrRecv<struct List, false>& self, int index)
        {
            return self.ptr->value()->vValid(index);
        }

        bool Valid(const gocpp::ObjRecv<struct List>& self, int index)
        {
            return self.obj.value()->vValid(index);
        }

        golang::label::Label Label(const gocpp::PtrRecv<struct List, false>& self, int index)
        {
            return self.ptr->value()->vLabel(index);
        }

        golang::label::Label Label(const gocpp::ObjRecv<struct List>& self, int index)
        {
            return self.obj.value()->vLabel(index);
        }
    }

    std::ostream& operator<<(std::ostream& os, const struct List& value)
    {
        return value.PrintTo(os);
    }

    // list implements LabelList for a list of Labels.
    
    template<typename T> requires gocpp::GoStruct<T>
    list::operator T()
    {
        T result;
        result.labels = this->labels;
        return result;
    }

    template<typename T> requires gocpp::GoStruct<T>
    bool list::operator==(const T& ref) const
    {
        if (labels != ref.labels) return false;
        return true;
    }

    std::ostream& list::PrintTo(std::ostream& os) const
    {
        os << '{';
        os << "" << labels;
        os << '}';
        return os;
    }

    std::ostream& operator<<(std::ostream& os, const struct list& value)
    {
        return value.PrintTo(os);
    }

    // filter wraps a LabelList filtering out specific labels.
    
    template<typename T> requires gocpp::GoStruct<T>
    filter::operator T()
    {
        T result;
        result.keys = this->keys;
        result.underlying = this->underlying;
        return result;
    }

    template<typename T> requires gocpp::GoStruct<T>
    bool filter::operator==(const T& ref) const
    {
        if (keys != ref.keys) return false;
        if (underlying != ref.underlying) return false;
        return true;
    }

    std::ostream& filter::PrintTo(std::ostream& os) const
    {
        os << '{';
        os << "" << keys;
        os << " " << underlying;
        os << '}';
        return os;
    }

    std::ostream& operator<<(std::ostream& os, const struct filter& value)
    {
        return value.PrintTo(os);
    }

    // listMap implements LabelMap for a simple list of labels.
    
    template<typename T> requires gocpp::GoStruct<T>
    listMap::operator T()
    {
        T result;
        result.labels = this->labels;
        return result;
    }

    template<typename T> requires gocpp::GoStruct<T>
    bool listMap::operator==(const T& ref) const
    {
        if (labels != ref.labels) return false;
        return true;
    }

    std::ostream& listMap::PrintTo(std::ostream& os) const
    {
        os << '{';
        os << "" << labels;
        os << '}';
        return os;
    }

    std::ostream& operator<<(std::ostream& os, const struct listMap& value)
    {
        return value.PrintTo(os);
    }

    // mapChain implements LabelMap for a list of underlying LabelMap.
    
    template<typename T> requires gocpp::GoStruct<T>
    mapChain::operator T()
    {
        T result;
        result.maps = this->maps;
        return result;
    }

    template<typename T> requires gocpp::GoStruct<T>
    bool mapChain::operator==(const T& ref) const
    {
        if (maps != ref.maps) return false;
        return true;
    }

    std::ostream& mapChain::PrintTo(std::ostream& os) const
    {
        os << '{';
        os << "" << maps;
        os << '}';
        return os;
    }

    std::ostream& operator<<(std::ostream& os, const struct mapChain& value)
    {
        return value.PrintTo(os);
    }

    // OfValue creates a new label from the key and value.
    // This method is for implementing new key types, label creation should
    // normally be done with the Of method of the key.
    golang::label::Label OfValue(golang::label::Key k, gocpp::go_any value)
    {
        return gocpp::Init<golang::label::Label>([=](auto& x) {
            x.key = k;
            x.untyped = value;
        });
    }

    // UnpackValue assumes the label was built using LabelOfValue and returns the value
    // that was passed to that constructor.
    // This method is for implementing new key types, for type safety normal
    // access should be done with the From method of the key.
    gocpp::go_any rec::UnpackValue(golang::label::Label t)
    {
        return t.untyped;
    }

    // Of64 creates a new label from a key and a uint64. This is often
    // used for non uint64 values that can be packed into a uint64.
    // This method is for implementing new key types, label creation should
    // normally be done with the Of method of the key.
    golang::label::Label Of64(golang::label::Key k, uint64_t v)
    {
        return gocpp::Init<golang::label::Label>([=](auto& x) {
            x.key = k;
            x.packed = v;
        });
    }

    // Unpack64 assumes the label was built using LabelOf64 and returns the value that
    // was passed to that constructor.
    // This method is for implementing new key types, for type safety normal
    // access should be done with the From method of the key.
    uint64_t rec::Unpack64(golang::label::Label t)
    {
        return t.packed;
    }

    // OfString creates a new label from a key and a string.
    // This method is for implementing new key types, label creation should
    // normally be done with the Of method of the key.
    golang::label::Label OfString(golang::label::Key k, gocpp::string v)
    {
        auto hdr = (reflect::StringHeader*)(gocpp::unsafe_pointer(& v));
        return gocpp::Init<golang::label::Label>([=](auto& x) {
            x.key = k;
            x.packed = uint64_t(hdr->Len);
            x.untyped = stringptr(hdr->Data);
        });
    }

    // UnpackString assumes the label was built using LabelOfString and returns the
    // value that was passed to that constructor.
    // This method is for implementing new key types, for type safety normal
    // access should be done with the From method of the key.
    gocpp::string rec::UnpackString(golang::label::Label t)
    {
        gocpp::string v = {};
        auto hdr = (reflect::StringHeader*)(gocpp::unsafe_pointer(& v));
        hdr->Data = uintptr_t(gocpp::getValue<stringptr>(t.untyped));
        hdr->Len = int(t.packed);
        return v;
    }

    // Valid returns true if the Label is a valid one (it has a key).
    bool rec::Valid(golang::label::Label t)
    {
        return t.key != nullptr;
    }

    // Key returns the key of this Label.
    golang::label::Key rec::Key(golang::label::Label t)
    {
        return t.key;
    }

    // Format is used for debug printing of labels.
    void rec::Format(golang::label::Label t, fmt::State f, gocpp::rune r)
    {
        if(! rec::Valid(gocpp::recv(t)))
        {
            io::WriteString(f, "nil"_s);
            return;
        }
        io::WriteString(f, rec::Name(gocpp::recv(rec::Key(gocpp::recv(t)))));
        io::WriteString(f, "="_s);
        gocpp::array<unsigned char, 128> buf = {};
        rec::Format(gocpp::recv(rec::Key(gocpp::recv(t))), f, buf.make_slice(0, 0), t);
    }

    bool rec::Valid(list* l, int index)
    {
        return index >= 0 && index < len(l->labels);
    }

    golang::label::Label rec::Label(list* l, int index)
    {
        return l->labels[index];
    }

    bool rec::Valid(filter* f, int index)
    {
        return rec::Valid(gocpp::recv(f->underlying), index);
    }

    golang::label::Label rec::Label(filter* f, int index)
    {
        auto l = rec::Label(gocpp::recv(f->underlying), index);
        for(auto [gocpp_ignored, f] : f->keys)
        {
            if(rec::Key(gocpp::recv(l)) == f)
            {
                return golang::label::Label {};
            }
        }
        return l;
    }

    golang::label::Label rec::Find(listMap lm, golang::label::Key key)
    {
        for(auto [gocpp_ignored, l] : lm.labels)
        {
            if(rec::Key(gocpp::recv(l)) == key)
            {
                return l;
            }
        }
        return golang::label::Label {};
    }

    golang::label::Label rec::Find(mapChain c, golang::label::Key key)
    {
        for(auto [gocpp_ignored, src] : c.maps)
        {
            auto l = rec::Find(gocpp::recv(src), key);
            if(rec::Valid(gocpp::recv(l)))
            {
                return l;
            }
        }
        return golang::label::Label {};
    }

    list* emptyList = new list {};
    List NewList(gocpp::slice<golang::label::Label> labels)
    {
        if(len(labels) == 0)
        {
            return emptyList;
        }
        return gocpp::InitPtr<list>([=](auto& x) {
            x.labels = labels;
        });
    }

    List Filter(List l, gocpp::slice<golang::label::Key> keys)
    {
        if(len(keys) == 0)
        {
            return l;
        }
        return gocpp::InitPtr<filter>([=](auto& x) {
            x.keys = keys;
            x.underlying = l;
        });
    }

    Map NewMap(gocpp::slice<golang::label::Label> labels)
    {
        return gocpp::Init<listMap>([=](auto& x) {
            x.labels = labels;
        });
    }

    Map MergeMaps(gocpp::slice<Map> srcs)
    {
        gocpp::slice<Map> nonNil = {};
        for(auto [gocpp_ignored, src] : srcs)
        {
            if(src != nullptr)
            {
                nonNil = append(nonNil, src);
            }
        }
        if(len(nonNil) == 1)
        {
            return nonNil[0];
        }
        return gocpp::Init<mapChain>([=](auto& x) {
            x.maps = nonNil;
        });
    }

}

