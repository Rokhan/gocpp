// generated by GoCpp from file '$(ImportDir)/golang.org/x/tools/internal/packagesinternal/packages.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "golang/golang.org/x/tools/internal/packagesinternal/packages.h"
#include "gocpp/support.h"

// Package packagesinternal exposes internal-only fields from go/packages.
namespace golang::packagesinternal
{
    namespace rec
    {
    }

    std::function<gocpp::string (gocpp::go_any)> GetForTest = [](gocpp::go_any p) mutable -> gocpp::string
    {
        return ""_s;
    };
    std::function<gocpp::slice<packagesinternal::PackageError*> (gocpp::go_any)> GetDepsErrors = [](gocpp::go_any p) mutable -> gocpp::slice<PackageError*>
    {
        return nullptr;
    };
    
    template<typename T> requires gocpp::GoStruct<T>
    PackageError::operator T()
    {
        T result;
        result.ImportStack = this->ImportStack;
        result.Pos = this->Pos;
        result.Err = this->Err;
        return result;
    }

    template<typename T> requires gocpp::GoStruct<T>
    bool PackageError::operator==(const T& ref) const
    {
        if (ImportStack != ref.ImportStack) return false;
        if (Pos != ref.Pos) return false;
        if (Err != ref.Err) return false;
        return true;
    }

    std::ostream& PackageError::PrintTo(std::ostream& os) const
    {
        os << '{';
        os << "" << ImportStack;
        os << " " << Pos;
        os << " " << Err;
        os << '}';
        return os;
    }

    std::ostream& operator<<(std::ostream& os, const struct PackageError& value)
    {
        return value.PrintTo(os);
    }

    int TypecheckCgo;
    int DepsErrors;
    int ForTest;
    std::function<void (gocpp::go_any, gocpp::string)> SetModFlag = [](gocpp::go_any config, gocpp::string value) mutable -> void
    {
    };
    std::function<void (gocpp::go_any, gocpp::string)> SetModFile = [](gocpp::go_any config, gocpp::string value) mutable -> void
    {
    };
}

