// generated by GoCpp from file '$(ImportDir)/go/types/pointer.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "golang/go/types/pointer.h"
#include "gocpp/support.h"

#include "golang/go/types/type.h"
#include "golang/go/types/typestring.h"

namespace golang::types
{
    namespace rec
    {
    }

    // A Pointer represents a pointer type.
    
    template<typename T> requires gocpp::GoStruct<T>
    Pointer::operator T()
    {
        T result;
        result.base = this->base;
        return result;
    }

    template<typename T> requires gocpp::GoStruct<T>
    bool Pointer::operator==(const T& ref) const
    {
        if (base != ref.base) return false;
        return true;
    }

    std::ostream& Pointer::PrintTo(std::ostream& os) const
    {
        os << '{';
        os << "" << base;
        os << '}';
        return os;
    }

    std::ostream& operator<<(std::ostream& os, const struct Pointer& value)
    {
        return value.PrintTo(os);
    }

    // NewPointer returns a new pointer type for the given element (base) type.
    Pointer* NewPointer(golang::types::Type elem)
    {
        return gocpp::InitPtr<Pointer>([=](auto& x) {
            x.base = elem;
        });
    }

    // Elem returns the element type for the given pointer p.
    golang::types::Type rec::Elem(Pointer* p)
    {
        return p->base;
    }

    golang::types::Type rec::Underlying(Pointer* p)
    {
        return p;
    }

    gocpp::string rec::String(Pointer* p)
    {
        return TypeString(p, nullptr);
    }

}

