// generated by GoCpp from file '$(ImportDir)/golang.org/x/tools/internal/pkgbits/codes.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/golang.org/x/tools/internal/pkgbits/codes.fwd.h"
#include "gocpp/support.h"

#include "golang/golang.org/x/tools/internal/pkgbits/sync.h"

namespace golang::pkgbits
{
    struct Code : virtual gocpp::Interface
    {
        using gocpp::Interface::operator==;
        using gocpp::Interface::operator!=;

        Code(){}
        Code(Code& i) = default;
        Code(const Code& i) = default;
        Code& operator=(Code& i) = default;
        Code& operator=(const Code& i) = default;

        inline Code(nullptr_t) {};
        Code& operator=(nullptr_t) { mValue.reset(); }

        template<typename T>
        Code(T& ref);

        template<typename T>
        Code(const T& ref);

        template<typename T>
        Code(T* ptr);

        using isGoInterface = void;

        std::ostream& PrintTo(std::ostream& os) const;

        struct ICode
        {
            // Marker returns the SyncMarker for the Code's dynamic type.
            virtual SyncMarker vMarker() = 0;
            // Value returns the Code's ordinal value.
            virtual int vValue() = 0;
            virtual void* getPtr() = 0;
        };

        template<typename T, typename TStore, typename TInterface = ICode>
        struct CodeImpl : virtual TInterface
        {
            explicit CodeImpl(T* ptr)
            {
                value.reset(ptr);
            }

            SyncMarker vMarker() override;

            int vValue() override;

            void* getPtr() override
            {
                return value.get();
            }

            TStore value;
        };

        inline ICode* value() const;

        std::shared_ptr<ICode> mValue;
    };

    namespace rec
    {
        SyncMarker Marker(const gocpp::PtrRecv<struct Code, false>& self);
        SyncMarker Marker(const gocpp::ObjRecv<struct Code>& self);

        int Value(const gocpp::PtrRecv<struct Code, false>& self);
        int Value(const gocpp::ObjRecv<struct Code>& self);
    }

    std::ostream& operator<<(std::ostream& os, const struct Code& value);
}

#include "golang/golang.org/x/tools/internal/pkgbits/sync.h"

namespace golang::pkgbits
{

    namespace rec
    {
        SyncMarker Marker(CodeVal c);
        int Value(CodeVal c);
        SyncMarker Marker(CodeType c);
        int Value(CodeType c);
        SyncMarker Marker(CodeObj c);
        int Value(CodeObj c);
    }
}

