// generated by GoCpp from file '$(ImportDir)/math/big/float.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/math/big/float.fwd.h"
#include "gocpp/support.h"


namespace golang::big
{
    struct ErrNaN
    {
        gocpp::string msg{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct ErrNaN& value);
    Accuracy makeAcc(bool above);
    uint32_t umax32(uint32_t x, uint32_t y);
}
#include "golang/math/big/nat.h"

namespace golang::big
{
    struct Float
    {
        uint32_t prec{};
        RoundingMode mode{};
        Accuracy acc{};
        form form{};
        bool neg{};
        nat mant{};
        int32_t exp{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct Float& value);
    int64_t fnorm(nat m);
    uint32_t msb32(nat x);
    uint64_t msb64(nat x);
    Float* NewFloat(double x);
    void validateBinaryOperands(Float* x, Float* y);
}

#include "golang/math/big/int.h"
#include "golang/math/big/rat.h"

namespace golang::big
{

    namespace rec
    {
        gocpp::string Error(ErrNaN err);
        Float* SetPrec(Float* z, unsigned int prec);
        Float* SetMode(Float* z, RoundingMode mode);
        unsigned int Prec(Float* x);
        unsigned int MinPrec(Float* x);
        RoundingMode Mode(Float* x);
        Accuracy Acc(Float* x);
        int Sign(Float* x);
        int MantExp(Float* x, Float* mant);
        void setExpAndRound(Float* z, int64_t exp, unsigned int sbit);
        Float* SetMantExp(Float* z, Float* mant, int exp);
        bool Signbit(Float* x);
        bool IsInf(Float* x);
        bool IsInt(Float* x);
        void validate(Float* x);
        gocpp::string validate0(Float* x);
        void round(Float* z, unsigned int sbit);
        Float* setBits64(Float* z, bool neg, uint64_t x);
        Float* SetUint64(Float* z, uint64_t x);
        Float* SetInt64(Float* z, int64_t x);
        Float* SetFloat64(Float* z, double x);
        Float* SetInt(Float* z, golang::big::Int* x);
        Float* SetRat(Float* z, golang::big::Rat* x);
        Float* SetInf(Float* z, bool signbit);
        Float* Set(Float* z, Float* x);
        Float* Copy(Float* z, Float* x);
        std::tuple<uint64_t, Accuracy> Uint64(Float* x);
        std::tuple<int64_t, Accuracy> Int64(Float* x);
        std::tuple<double, Accuracy> Float32(Float* x);
        std::tuple<double, Accuracy> Float64(Float* x);
        std::tuple<golang::big::Int*, Accuracy> Int(Float* x, golang::big::Int* z);
        std::tuple<golang::big::Rat*, Accuracy> Rat(Float* x, golang::big::Rat* z);
        Float* Abs(Float* z, Float* x);
        Float* Neg(Float* z, Float* x);
        void uadd(Float* z, Float* x, Float* y);
        void usub(Float* z, Float* x, Float* y);
        void umul(Float* z, Float* x, Float* y);
        void uquo(Float* z, Float* x, Float* y);
        int ucmp(Float* x, Float* y);
        Float* Add(Float* z, Float* x, Float* y);
        Float* Sub(Float* z, Float* x, Float* y);
        Float* Mul(Float* z, Float* x, Float* y);
        Float* Quo(Float* z, Float* x, Float* y);
        int Cmp(Float* x, Float* y);
        int ord(Float* x);
    }
}

