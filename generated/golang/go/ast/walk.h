// generated by GoCpp from file '$(ImportDir)/go/ast/walk.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/go/ast/walk.fwd.h"
#include "gocpp/support.h"

#include "golang/go/ast/ast.h"

namespace golang::ast
{
    struct Visitor : virtual gocpp::Interface
    {
        using gocpp::Interface::operator==;
        using gocpp::Interface::operator!=;

        Visitor(){}
        Visitor(Visitor& i) = default;
        Visitor(const Visitor& i) = default;
        Visitor& operator=(Visitor& i) = default;
        Visitor& operator=(const Visitor& i) = default;

        inline Visitor(nullptr_t) {};
        Visitor& operator=(nullptr_t) { mValue.reset(); }

        template<typename T>
        Visitor(T& ref);

        template<typename T>
        Visitor(const T& ref);

        template<typename T>
        Visitor(T* ptr);

        using isGoInterface = void;

        std::ostream& PrintTo(std::ostream& os) const;

        struct IVisitor
        {
            virtual Visitor vVisit(Node node) = 0;
            virtual void* getPtr() = 0;
        };

        template<typename T, typename TStore, typename TInterface = IVisitor>
        struct VisitorImpl : virtual TInterface
        {
            explicit VisitorImpl(T* ptr)
            {
                value.reset(ptr);
            }

            Visitor vVisit(Node node) override;

            void* getPtr() override
            {
                return value.get();
            }

            TStore value;
        };

        inline IVisitor* value() const;

        std::shared_ptr<IVisitor> mValue;
    };

    namespace rec
    {
        Visitor Visit(const gocpp::PtrRecv<struct Visitor, false>& self, Node node);
        Visitor Visit(const gocpp::ObjRecv<struct Visitor>& self, Node node);
    }

    std::ostream& operator<<(std::ostream& os, const struct Visitor& value);
    void Inspect(Node node, std::function<bool (Node _1)> f);
    void walkExprList(Visitor v, gocpp::slice<Expr> list);
    void walkStmtList(Visitor v, gocpp::slice<Stmt> list);
    void walkDeclList(Visitor v, gocpp::slice<Decl> list);
    void Walk(Visitor v, Node node);
    void walkIdentList(Visitor v, gocpp::slice<Ident*> list);
}

#include "golang/go/ast/ast.h"

namespace golang::ast
{

    namespace rec
    {
        Visitor Visit(inspector f, Node node);
    }
}

