// generated by GoCpp from file '$(ImportDir)/encoding/json/scanner.go'
#pragma once

#include "gocpp/support.fwd.h"


namespace golang::json
{
    struct SyntaxError;
    struct scanner;
    // These values are returned by the state transition functions
    // assigned to scanner.state and the method scanner.eof.
    // They give details about the current state of the scan that
    // callers might be interested to know about.
    // It is okay to ignore the return value of any particular
    // call to scanner.state: if one call returns scanError,
    // every subsequent call will return scanError too.
    // Continue.
    const int scanContinue = 0;
    const int scanBeginLiteral = 1;
    const int scanBeginObject = 2;
    const int scanObjectKey = 3;
    const int scanObjectValue = 4;
    const int scanEndObject = 5;
    const int scanBeginArray = 6;
    const int scanArrayValue = 7;
    const int scanEndArray = 8;
    const int scanSkipSpace = 9;
    // Stop.
    const int scanEnd = 10;
    const int scanError = 11;
    // These values are stored in the parseState stack.
    // They give the current state of a composite value
    // being scanned. If the parser is inside a nested value
    // the parseState describes the nested state, outermost at entry 0.
    const int parseObjectKey = 0;
    const int parseObjectValue = 1;
    const int parseArrayValue = 2;
    // This limits the max nesting depth to prevent stack overflow.
    // This is permitted by https://tools.ietf.org/html/rfc7159#section-9
    const long maxNestingDepth = 10000;
}
