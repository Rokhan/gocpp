// generated by GoCpp from file '$(ImportDir)/io/fs/readdir.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/io/fs/readdir.fwd.h"
#include "gocpp/support.h"

#include "golang/io/fs/fs.h"

namespace golang::fs
{
    struct ReadDirFS : virtual gocpp::Interface, FS
    {
        using gocpp::Interface::operator==;
        using gocpp::Interface::operator!=;

        ReadDirFS(){}
        ReadDirFS(ReadDirFS& i) = default;
        ReadDirFS(const ReadDirFS& i) = default;
        ReadDirFS& operator=(ReadDirFS& i) = default;
        ReadDirFS& operator=(const ReadDirFS& i) = default;

        inline ReadDirFS(nullptr_t) {};
        ReadDirFS& operator=(nullptr_t) { mValue.reset(); }

        template<typename T>
        ReadDirFS(T& ref);

        template<typename T>
        ReadDirFS(const T& ref);

        template<typename T>
        ReadDirFS(T* ptr);

        using isGoInterface = void;

        std::ostream& PrintTo(std::ostream& os) const;

        struct IReadDirFS: virtual FS::IFS
        {
            // ReadDir reads the named directory
            // and returns a list of directory entries sorted by filename.
            virtual std::tuple<gocpp::slice<DirEntry>, gocpp::error> vReadDir(gocpp::string name) = 0;
            virtual void* getPtr() = 0;
        };

        template<typename T, typename TStore, typename TInterface = IReadDirFS>
        struct ReadDirFSImpl : virtual TInterface, virtual FS::FSImpl<T, TStore, TInterface>
        {
            explicit ReadDirFSImpl(T* ptr): FS::FSImpl<T, TStore, TInterface>(ptr)
            {
                value.reset(ptr);
            }

            std::tuple<gocpp::slice<DirEntry>, gocpp::error> vReadDir(gocpp::string name) override;

            void* getPtr() override
            {
                return value.get();
            }

            TStore value;
        };

        inline IReadDirFS* value() const;

        std::shared_ptr<IReadDirFS> mValue;
    };

    namespace rec
    {
        std::tuple<gocpp::slice<DirEntry>, gocpp::error> ReadDir(const gocpp::PtrRecv<struct ReadDirFS, false>& self, gocpp::string name);
        std::tuple<gocpp::slice<DirEntry>, gocpp::error> ReadDir(const gocpp::ObjRecv<struct ReadDirFS>& self, gocpp::string name);

        std::tuple<fs::File, gocpp::error> Open(const gocpp::PtrRecv<struct ReadDirFS, false>& self, gocpp::string name);
        std::tuple<fs::File, gocpp::error> Open(const gocpp::ObjRecv<struct ReadDirFS>& self, gocpp::string name);
    }

    std::ostream& operator<<(std::ostream& os, const struct ReadDirFS& value);
    std::tuple<gocpp::slice<DirEntry>, gocpp::error> ReadDir(FS fsys, gocpp::string name);
    struct dirInfo
    {
        FileInfo fileInfo{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct dirInfo& value);
    DirEntry FileInfoToDirEntry(FileInfo info);
}

#include "golang/io/fs/fs.h"

namespace golang::fs
{

    namespace rec
    {
        bool IsDir(dirInfo di);
        FileMode Type(dirInfo di);
        std::tuple<FileInfo, gocpp::error> Info(dirInfo di);
        gocpp::string Name(dirInfo di);
        gocpp::string String(dirInfo di);
    }
}

