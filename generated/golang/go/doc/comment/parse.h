// generated by GoCpp from file '$(ImportDir)/go/doc/comment/parse.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/go/doc/comment/parse.fwd.h"
#include "gocpp/support.h"


namespace golang::comment
{
    struct LinkDef
    {
        gocpp::string Text{}; // the link text
        gocpp::string URL{}; // the link URL
        bool Used{}; // whether the comment uses the definition

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct LinkDef& value);
    struct Block : virtual gocpp::Interface
    {
        using gocpp::Interface::operator==;
        using gocpp::Interface::operator!=;

        Block(){}
        Block(Block& i) = default;
        Block(const Block& i) = default;
        Block& operator=(Block& i) = default;
        Block& operator=(const Block& i) = default;

        inline Block(nullptr_t) {};
        Block& operator=(nullptr_t) { mValue.reset(); }

        template<typename T>
        Block(T& ref);

        template<typename T>
        Block(const T& ref);

        template<typename T>
        Block(T* ptr);

        using isGoInterface = void;

        std::ostream& PrintTo(std::ostream& os) const;

        struct IBlock
        {
            virtual void vblock() = 0;
            virtual void* getPtr() = 0;
        };

        template<typename T, typename TStore, typename TInterface = IBlock>
        struct BlockImpl : virtual TInterface
        {
            explicit BlockImpl(T* ptr)
            {
                value.reset(ptr);
            }

            void vblock() override;

            void* getPtr() override
            {
                return value.get();
            }

            TStore value;
        };

        inline IBlock* value() const;

        std::shared_ptr<IBlock> mValue;
    };

    namespace rec
    {
        void block(const gocpp::PtrRecv<struct Block, false>& self);
        void block(const gocpp::ObjRecv<struct Block>& self);
    }

    std::ostream& operator<<(std::ostream& os, const struct Block& value);
    struct List
    {
        // Items is the list items.
        gocpp::slice<ListItem*> Items{};
        // ForceBlankBefore indicates that the list must be
        // preceded by a blank line when reformatting the comment,
        // overriding the usual conditions. See the BlankBefore method.
        // The comment parser sets ForceBlankBefore for any list
        // that is preceded by a blank line, to make sure
        // the blank line is preserved when printing.
        bool ForceBlankBefore{};
        // ForceBlankBetween indicates that list items must be
        // separated by blank lines when reformatting the comment,
        // overriding the usual conditions. See the BlankBetween method.
        // The comment parser sets ForceBlankBetween for any list
        // that has a blank line between any two of its items, to make sure
        // the blank lines are preserved when printing.
        bool ForceBlankBetween{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct List& value);
    struct Code
    {
        // Text is the preformatted text, ending with a newline character.
        // It may be multiple lines, each of which ends with a newline character.
        // It is never empty, nor does it start or end with a blank line.
        gocpp::string Text{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct Code& value);
    struct Text : virtual gocpp::Interface
    {
        using gocpp::Interface::operator==;
        using gocpp::Interface::operator!=;

        Text(){}
        Text(Text& i) = default;
        Text(const Text& i) = default;
        Text& operator=(Text& i) = default;
        Text& operator=(const Text& i) = default;

        inline Text(nullptr_t) {};
        Text& operator=(nullptr_t) { mValue.reset(); }

        template<typename T>
        Text(T& ref);

        template<typename T>
        Text(const T& ref);

        template<typename T>
        Text(T* ptr);

        using isGoInterface = void;

        std::ostream& PrintTo(std::ostream& os) const;

        struct IText
        {
            virtual void vtext() = 0;
            virtual void* getPtr() = 0;
        };

        template<typename T, typename TStore, typename TInterface = IText>
        struct TextImpl : virtual TInterface
        {
            explicit TextImpl(T* ptr)
            {
                value.reset(ptr);
            }

            void vtext() override;

            void* getPtr() override
            {
                return value.get();
            }

            TStore value;
        };

        inline IText* value() const;

        std::shared_ptr<IText> mValue;
    };

    namespace rec
    {
        void text(const gocpp::PtrRecv<struct Text, false>& self);
        void text(const gocpp::ObjRecv<struct Text>& self);
    }

    std::ostream& operator<<(std::ostream& os, const struct Text& value);
    struct parseDoc
    {
        Parser* Parser{};
        Doc* Doc{};
        gocpp::map<gocpp::string, LinkDef*> links{};
        gocpp::slice<gocpp::string> lines{};
        std::function<bool (gocpp::string recv, gocpp::string name)> lookupSym{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct parseDoc& value);
    bool isStdPkg(gocpp::string path);
    std::tuple<gocpp::string, bool> DefaultLookupPackage(gocpp::string name);
    struct span
    {
        int start{};
        int end{};
        spanKind kind{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct span& value);
    bool indented(gocpp::string line);
    gocpp::slice<gocpp::string> unindent(gocpp::slice<gocpp::string> lines);
    bool isBlank(gocpp::string s);
    gocpp::string commonPrefix(gocpp::string a, gocpp::string b);
    gocpp::string leadingSpace(gocpp::string s);
    bool isOldHeading(gocpp::string line, gocpp::slice<gocpp::string> all, int off);
    bool isHeading(gocpp::string line);
    std::tuple<gocpp::string, gocpp::string, bool> listMarker(gocpp::string line);
    bool isList(gocpp::string line);
    std::tuple<gocpp::string, gocpp::string, bool> splitDocName(gocpp::string text);
    std::tuple<gocpp::string, bool> autoURL(gocpp::string s);
    bool isScheme(gocpp::string s);
    bool isHost(unsigned char c);
    bool isPunct(unsigned char c);
    bool isPath(unsigned char c);
    bool isName(gocpp::string s);
    std::tuple<gocpp::string, bool> ident(gocpp::string s);
    bool isIdentASCII(unsigned char c);
    bool validImportPath(gocpp::string path);
    bool validImportPathElem(gocpp::string elem);
    bool importPathOK(unsigned char c);
    struct Doc
    {
        // Content is the sequence of content blocks in the comment.
        gocpp::slice<Block> Content{};
        // Links is the link definitions in the comment.
        gocpp::slice<LinkDef*> Links{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct Doc& value);
    struct Heading
    {
        gocpp::slice<golang::comment::Text> Text{}; // the heading text

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct Heading& value);
    struct ListItem
    {
        // Number is a decimal string in a numbered list
        // or an empty string in a bullet list.
        gocpp::string Number{}; // "1", "2", ...; "" for bullet list
        // Content is the list content.
        // Currently, restrictions in the parser and printer
        // require every element of Content to be a *Paragraph.
        gocpp::slice<Block> Content{}; // Content of this item.

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct ListItem& value);
    struct Paragraph
    {
        gocpp::slice<golang::comment::Text> Text{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct Paragraph& value);
    struct Link
    {
        bool Auto{}; // is this an automatic (implicit) link of a literal URL?
        gocpp::slice<golang::comment::Text> Text{}; // text of link
        gocpp::string URL{}; // target URL of link

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct Link& value);
    struct DocLink
    {
        gocpp::slice<golang::comment::Text> Text{}; // text of link
        // ImportPath, Recv, and Name identify the Go package or symbol
        // that is the link target. The potential combinations of
        // non-empty fields are:
        // - ImportPath: a link to another package
        // - ImportPath, Name: a link to a const, func, type, or var in another package
        // - ImportPath, Recv, Name: a link to a method in another package
        // - Name: a link to a const, func, type, or var in this package
        // - Recv, Name: a link to a method in this package
        gocpp::string ImportPath{}; // import path
        gocpp::string Recv{}; // receiver type, without any pointer star, for methods
        gocpp::string Name{}; // const, func, type, var, or method name

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct DocLink& value);
    struct Parser
    {
        // Words is a map of Go identifier words that
        // should be italicized and potentially linked.
        // If Words[w] is the empty string, then the word w
        // is only italicized. Otherwise it is linked, using
        // Words[w] as the link target.
        // Words corresponds to the [go/doc.ToHTML] words parameter.
        gocpp::map<gocpp::string, gocpp::string> Words{};
        // LookupPackage resolves a package name to an import path.
        // If LookupPackage(name) returns ok == true, then [name]
        // (or [name.Sym] or [name.Sym.Method])
        // is considered a documentation link to importPath's package docs.
        // It is valid to return "", true, in which case name is considered
        // to refer to the current package.
        // If LookupPackage(name) returns ok == false,
        // then [name] (or [name.Sym] or [name.Sym.Method])
        // will not be considered a documentation link,
        // except in the case where name is the full (but single-element) import path
        // of a package in the standard library, such as in [math] or [io.Reader].
        // LookupPackage is still called for such names,
        // in order to permit references to imports of other packages
        // with the same package names.
        // Setting LookupPackage to nil is equivalent to setting it to
        // a function that always returns "", false.
        std::function<std::tuple<gocpp::string, bool> (gocpp::string name)> LookupPackage{};
        // LookupSym reports whether a symbol name or method name
        // exists in the current package.
        // If LookupSym("", "Name") returns true, then [Name]
        // is considered a documentation link for a const, func, type, or var.
        // Similarly, if LookupSym("Recv", "Name") returns true,
        // then [Recv.Name] is considered a documentation link for
        // type Recv's method Name.
        // Setting LookupSym to nil is equivalent to setting it to a function
        // that always returns false.
        std::function<bool (gocpp::string recv, gocpp::string name)> LookupSym{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct Parser& value);
    gocpp::slice<span> parseSpans(gocpp::slice<gocpp::string> lines);
    std::tuple<LinkDef*, bool> parseLink(gocpp::string line);

    namespace rec
    {
        void block(Heading*);
        void block(List*);
        bool BlankBefore(List* l);
        bool BlankBetween(List* l);
        void block(Paragraph*);
        void block(Code*);
        void text(Plain);
        void text(Italic);
        void text(Link*);
        void text(DocLink*);
        std::tuple<gocpp::string, bool> lookupPkg(parseDoc* d, gocpp::string pkg);
        Doc* Parse(Parser* p, gocpp::string text);
        Block oldHeading(parseDoc* d, gocpp::string line);
        Block heading(parseDoc* d, gocpp::string line);
        Code* code(parseDoc* d, gocpp::slice<gocpp::string> lines);
        Block paragraph(parseDoc* d, gocpp::slice<gocpp::string> lines);
        List* list(parseDoc* d, gocpp::slice<gocpp::string> lines, bool forceBlankBefore);
        gocpp::slice<golang::comment::Text> parseLinkedText(parseDoc* d, gocpp::string text);
        std::tuple<DocLink*, bool> docLink(parseDoc* d, gocpp::string text, gocpp::string before, gocpp::string after);
        gocpp::slice<golang::comment::Text> parseText(parseDoc* d, gocpp::slice<golang::comment::Text> out, gocpp::string s, bool autoLink);
    }
}

