// generated by GoCpp from file '$(ImportDir)/regexp/backtrack.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/regexp/backtrack.fwd.h"
#include "gocpp/support.h"


namespace golang::regexp
{
    struct job
    {
        uint32_t pc{};
        bool arg{};
        int pos{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct job& value);
}
#include "golang/regexp/exec.h"
#include "golang/sync/pool.h"
#include "golang/regexp/syntax/prog.fwd.h"

namespace golang::regexp
{
    struct bitState
    {
        int end{};
        gocpp::slice<int> cap{};
        gocpp::slice<int> matchcap{};
        gocpp::slice<job> jobs{};
        gocpp::slice<uint32_t> visited{};
        inputs inputs{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct bitState& value);
    extern sync::Pool bitStatePool;
    int maxBitStateLen(syntax::Prog* prog);
    bool shouldBacktrack(syntax::Prog* prog);
    bitState* newBitState();
    void freeBitState(bitState* b);
}

#include "golang/regexp/regexp.h"
#include "golang/regexp/syntax/prog.h"

namespace golang::regexp
{

    namespace rec
    {
        void reset(bitState* b, syntax::Prog* prog, int end, int ncap);
        bool shouldVisit(bitState* b, uint32_t pc, int pos);
        void push(bitState* b, Regexp* re, uint32_t pc, int pos, bool arg);
        bool tryBacktrack(Regexp* re, bitState* b, input i, uint32_t pc, int pos);
        gocpp::slice<int> backtrack(Regexp* re, gocpp::slice<unsigned char> ib, gocpp::string is, int pos, int ncap, gocpp::slice<int> dstCap);
    }
}

