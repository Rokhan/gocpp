// generated by GoCpp from file '$(ImportDir)/go/types/labels.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/go/types/labels.fwd.h"
#include "gocpp/support.h"

#include "golang/go/ast/ast.fwd.h"

namespace golang::types
{
    struct block
    {
        block* parent{}; // enclosing block
        ast::LabeledStmt* lstmt{}; // labeled statement to which this block belongs, or nil
        gocpp::map<gocpp::string, ast::LabeledStmt*> labels{}; // allocated lazily

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct block& value);
}

#include "golang/go/ast/ast.h"
#include "golang/go/types/check.h"
#include "golang/go/types/scope.h"

namespace golang::types
{

    namespace rec
    {
        void labels(Checker* check, ast::BlockStmt* body);
        void insert(block* b, ast::LabeledStmt* s);
        ast::LabeledStmt* gotoTarget(block* b, gocpp::string name);
        ast::LabeledStmt* enclosingTarget(block* b, gocpp::string name);
        gocpp::slice<ast::BranchStmt*> blockBranches(Checker* check, golang::types::Scope* all, block* parent, ast::LabeledStmt* lstmt, gocpp::slice<ast::Stmt> list);
    }
}

