// generated by GoCpp from file '$(ImportDir)/go/ast/resolve.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/go/ast/resolve.fwd.h"
#include "gocpp/support.h"

#include "golang/go/scanner/errors.h"
#include "golang/go/ast/ast.fwd.h"
#include "golang/go/ast/scope.fwd.h"
#include "golang/go/token/position.fwd.h"

namespace golang::ast
{
    struct pkgBuilder
    {
        token::FileSet* fset{};
        scanner::ErrorList errors{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct pkgBuilder& value);
    bool resolve(Scope* scope, Ident* ident);
    std::tuple<Package*, gocpp::error> NewPackage(token::FileSet* fset, gocpp::map<gocpp::string, File*> files, Importer importer, Scope* universe);
}

#include "golang/go/ast/scope.h"
#include "golang/go/token/position.h"

namespace golang::ast
{

    namespace rec
    {
        void error(pkgBuilder* p, token::Pos pos, gocpp::string msg);
        void errorf(pkgBuilder* p, token::Pos pos, gocpp::string format, gocpp::slice<go_any> args);
        
        template<typename... Args>
        void errorf(pkgBuilder* p, token::Pos pos, gocpp::string format, Args... args)
        {
            return errorf(p, pos, format, gocpp::ToSlice<go_any>(args...));
        }
        
        template<typename... Args>
        void errorf(pkgBuilder* p, token::Pos pos, gocpp::string format, go_any value, Args... args)
        {
            return errorf(p, pos, format, gocpp::ToSlice<go_any>(value, args...));
        }
        void declare(pkgBuilder* p, Scope* scope, Scope* altScope, Object* obj);
    }
}

