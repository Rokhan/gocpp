// generated by GoCpp from file '$(ImportDir)/context/context.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/context/context.fwd.h"
#include "gocpp/support.h"


namespace golang::context
{
    struct gocpp_id_0
    {

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T()
        {
            T result;
            return result;
        }

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const
        {
            return true;
        }

        std::ostream& PrintTo(std::ostream& os) const
        {
            os << '{';
            os << '}';
            return os;
        }
    };

    std::ostream& operator<<(std::ostream& os, const struct gocpp_id_0& value)
    {
        return value.PrintTo(os);
    }

    struct afterFuncer : virtual gocpp::Interface
    {
        using gocpp::Interface::operator==;
        using gocpp::Interface::operator!=;

        afterFuncer(){}
        afterFuncer(afterFuncer& i) = default;
        afterFuncer(const afterFuncer& i) = default;
        afterFuncer& operator=(afterFuncer& i) = default;
        afterFuncer& operator=(const afterFuncer& i) = default;

        inline afterFuncer(nullptr_t) {};
        afterFuncer& operator=(nullptr_t) { mValue.reset(); }

        template<typename T>
        afterFuncer(T& ref);

        template<typename T>
        afterFuncer(const T& ref);

        template<typename T>
        afterFuncer(T* ptr);

        using isGoInterface = void;

        std::ostream& PrintTo(std::ostream& os) const;

        struct IafterFuncer
        {
            virtual std::function<bool ()> vAfterFunc(std::function<void ()> _1) = 0;
            virtual void* getPtr() = 0;
        };

        template<typename T, typename TStore, typename TInterface = IafterFuncer>
        struct afterFuncerImpl : virtual TInterface
        {
            explicit afterFuncerImpl(T* ptr)
            {
                value.reset(ptr);
            }

            std::function<bool ()> vAfterFunc(std::function<void ()> _1) override;

            void* getPtr() override
            {
                return value.get();
            }

            TStore value;
        };

        inline IafterFuncer* value() const;

        std::shared_ptr<IafterFuncer> mValue;
    };

    namespace rec
    {
        std::function<bool ()> AfterFunc(const gocpp::PtrRecv<struct afterFuncer, false>& self, std::function<void ()> _1);
        std::function<bool ()> AfterFunc(const gocpp::ObjRecv<struct afterFuncer>& self, std::function<void ()> _1);
    }

    std::ostream& operator<<(std::ostream& os, const struct afterFuncer& value);
    extern int cancelCtxKey;
    struct gocpp_id_4
    {

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T()
        {
            T result;
            return result;
        }

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const
        {
            return true;
        }

        std::ostream& PrintTo(std::ostream& os) const
        {
            os << '{';
            os << '}';
            return os;
        }
    };

    std::ostream& operator<<(std::ostream& os, const struct gocpp_id_4& value)
    {
        return value.PrintTo(os);
    }

    struct canceler : virtual gocpp::Interface
    {
        using gocpp::Interface::operator==;
        using gocpp::Interface::operator!=;

        canceler(){}
        canceler(canceler& i) = default;
        canceler(const canceler& i) = default;
        canceler& operator=(canceler& i) = default;
        canceler& operator=(const canceler& i) = default;

        inline canceler(nullptr_t) {};
        canceler& operator=(nullptr_t) { mValue.reset(); }

        template<typename T>
        canceler(T& ref);

        template<typename T>
        canceler(const T& ref);

        template<typename T>
        canceler(T* ptr);

        using isGoInterface = void;

        std::ostream& PrintTo(std::ostream& os) const;

        struct Icanceler
        {
            virtual void vcancel(bool removeFromParent, gocpp::error err, gocpp::error cause) = 0;
            virtual gocpp::channel<gocpp_id_4> vDone() = 0;
            virtual void* getPtr() = 0;
        };

        template<typename T, typename TStore, typename TInterface = Icanceler>
        struct cancelerImpl : virtual TInterface
        {
            explicit cancelerImpl(T* ptr)
            {
                value.reset(ptr);
            }

            void vcancel(bool removeFromParent, gocpp::error err, gocpp::error cause) override;

            gocpp::channel<gocpp_id_4> vDone() override;

            void* getPtr() override
            {
                return value.get();
            }

            TStore value;
        };

        inline Icanceler* value() const;

        std::shared_ptr<Icanceler> mValue;
    };

    namespace rec
    {
        void cancel(const gocpp::PtrRecv<struct canceler, false>& self, bool removeFromParent, gocpp::error err, gocpp::error cause);
        void cancel(const gocpp::ObjRecv<struct canceler>& self, bool removeFromParent, gocpp::error err, gocpp::error cause);

        gocpp::channel<gocpp_id_4> Done(const gocpp::PtrRecv<struct canceler, false>& self);
        gocpp::channel<gocpp_id_4> Done(const gocpp::ObjRecv<struct canceler>& self);
    }

    std::ostream& operator<<(std::ostream& os, const struct canceler& value);
    void init();
    struct gocpp_id_6
    {

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct gocpp_id_6& value);
    struct stringer : virtual gocpp::Interface
    {
        using gocpp::Interface::operator==;
        using gocpp::Interface::operator!=;

        stringer(){}
        stringer(stringer& i) = default;
        stringer(const stringer& i) = default;
        stringer& operator=(stringer& i) = default;
        stringer& operator=(const stringer& i) = default;

        inline stringer(nullptr_t) {};
        stringer& operator=(nullptr_t) { mValue.reset(); }

        template<typename T>
        stringer(T& ref);

        template<typename T>
        stringer(const T& ref);

        template<typename T>
        stringer(T* ptr);

        using isGoInterface = void;

        std::ostream& PrintTo(std::ostream& os) const;

        struct Istringer
        {
            virtual gocpp::string vString() = 0;
            virtual void* getPtr() = 0;
        };

        template<typename T, typename TStore, typename TInterface = Istringer>
        struct stringerImpl : virtual TInterface
        {
            explicit stringerImpl(T* ptr)
            {
                value.reset(ptr);
            }

            gocpp::string vString() override;

            void* getPtr() override
            {
                return value.get();
            }

            TStore value;
        };

        inline Istringer* value() const;

        std::shared_ptr<Istringer> mValue;
    };

    namespace rec
    {
        gocpp::string String(const gocpp::PtrRecv<struct stringer, false>& self);
        gocpp::string String(const gocpp::ObjRecv<struct stringer>& self);
    }

    std::ostream& operator<<(std::ostream& os, const struct stringer& value);
    gocpp::string stringify(go_any v);
    struct deadlineExceededError
    {

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct deadlineExceededError& value);
    struct emptyCtx
    {

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct emptyCtx& value);
    extern gocpp::channel<gocpp_id_5> closedchan;
}
#include "golang/sync/atomic/type.h"
#include "golang/time/time.h"
#include "golang/errors/errors.fwd.h"

namespace golang::context
{
    struct Context : virtual gocpp::Interface
    {
        using gocpp::Interface::operator==;
        using gocpp::Interface::operator!=;

        Context(){}
        Context(Context& i) = default;
        Context(const Context& i) = default;
        Context& operator=(Context& i) = default;
        Context& operator=(const Context& i) = default;

        inline Context(nullptr_t) {};
        Context& operator=(nullptr_t) { mValue.reset(); }

        template<typename T>
        Context(T& ref);

        template<typename T>
        Context(const T& ref);

        template<typename T>
        Context(T* ptr);

        using isGoInterface = void;

        std::ostream& PrintTo(std::ostream& os) const;

        struct IContext
        {
            // Deadline returns the time when work done on behalf of this context
            // should be canceled. Deadline returns ok==false when no deadline is
            // set. Successive calls to Deadline return the same results.
            virtual std::tuple<mocklib::Date, bool> vDeadline() = 0;
            // Done returns a channel that's closed when work done on behalf of this
            // context should be canceled. Done may return nil if this context can
            // never be canceled. Successive calls to Done return the same value.
            // The close of the Done channel may happen asynchronously,
            // after the cancel function returns.
            // WithCancel arranges for Done to be closed when cancel is called;
            // WithDeadline arranges for Done to be closed when the deadline
            // expires; WithTimeout arranges for Done to be closed when the timeout
            // elapses.
            // Done is provided for use in select statements:
            // // Stream generates values with DoSomething and sends them to out
            // // until DoSomething returns an error or ctx.Done is closed.
            // func Stream(ctx context.Context, out chan<- Value) error {
            // for {
            // v, err := DoSomething(ctx)
            // if err != nil {
            // return err
            // }
            // select {
            // case <-ctx.Done():
            // return ctx.Err()
            // case out <- v:
            // }
            // }
            // }
            // See https://blog.golang.org/pipelines for more examples of how to use
            // a Done channel for cancellation.
            virtual gocpp::channel<gocpp_id_0> vDone() = 0;
            // If Done is not yet closed, Err returns nil.
            // If Done is closed, Err returns a non-nil error explaining why:
            // Canceled if the context was canceled
            // or DeadlineExceeded if the context's deadline passed.
            // After Err returns a non-nil error, successive calls to Err return the same error.
            virtual gocpp::error vErr() = 0;
            // Value returns the value associated with this context for key, or nil
            // if no value is associated with key. Successive calls to Value with
            // the same key returns the same result.
            // Use context values only for request-scoped data that transits
            // processes and API boundaries, not for passing optional parameters to
            // functions.
            // A key identifies a specific value in a Context. Functions that wish
            // to store values in Context typically allocate a key in a global
            // variable then use that key as the argument to context.WithValue and
            // Context.Value. A key can be any type that supports equality;
            // packages should define keys as an unexported type to avoid
            // collisions.
            // Packages that define a Context key should provide type-safe accessors
            // for the values stored using that key:
            // // Package user defines a User type that's stored in Contexts.
            // package user
            // import "context"
            // // User is the type of value stored in the Contexts.
            // type User struct {...}
            // // key is an unexported type for keys defined in this package.
            // // This prevents collisions with keys defined in other packages.
            // type key int
            // // userKey is the key for user.User values in Contexts. It is
            // // unexported; clients use user.NewContext and user.FromContext
            // // instead of using this key directly.
            // var userKey key
            // // NewContext returns a new Context that carries value u.
            // func NewContext(ctx context.Context, u *User) context.Context {
            // return context.WithValue(ctx, userKey, u)
            // }
            // // FromContext returns the User value stored in ctx, if any.
            // func FromContext(ctx context.Context) (*User, bool) {
            // u, ok := ctx.Value(userKey).(*User)
            // return u, ok
            // }
            virtual go_any vValue(go_any key) = 0;
            virtual void* getPtr() = 0;
        };

        template<typename T, typename TStore, typename TInterface = IContext>
        struct ContextImpl : virtual TInterface
        {
            explicit ContextImpl(T* ptr)
            {
                value.reset(ptr);
            }

            std::tuple<mocklib::Date, bool> vDeadline() override;

            gocpp::channel<gocpp_id_0> vDone() override;

            gocpp::error vErr() override;

            go_any vValue(go_any key) override;

            void* getPtr() override
            {
                return value.get();
            }

            TStore value;
        };

        inline IContext* value() const;

        std::shared_ptr<IContext> mValue;
    };

    namespace rec
    {
        std::tuple<mocklib::Date, bool> Deadline(const gocpp::PtrRecv<struct Context, false>& self);
        std::tuple<mocklib::Date, bool> Deadline(const gocpp::ObjRecv<struct Context>& self);

        gocpp::channel<gocpp_id_0> Done(const gocpp::PtrRecv<struct Context, false>& self);
        gocpp::channel<gocpp_id_0> Done(const gocpp::ObjRecv<struct Context>& self);

        gocpp::error Err(const gocpp::PtrRecv<struct Context, false>& self);
        gocpp::error Err(const gocpp::ObjRecv<struct Context>& self);

        go_any Value(const gocpp::PtrRecv<struct Context, false>& self, go_any key);
        go_any Value(const gocpp::ObjRecv<struct Context>& self, go_any key);
    }

    std::ostream& operator<<(std::ostream& os, const struct Context& value);
    extern gocpp::error Canceled;
    extern atomic::Int32 goroutines;
    extern gocpp::error DeadlineExceeded;
    struct backgroundCtx
    {
        emptyCtx emptyCtx{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct backgroundCtx& value);
    struct todoCtx
    {
        emptyCtx emptyCtx{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct todoCtx& value);
    Context Background();
    Context TODO();
    std::tuple<Context, CancelFunc> WithCancel(Context parent);
    std::tuple<Context, CancelCauseFunc> WithCancelCause(Context parent);
    gocpp::error Cause(Context c);
    std::function<bool ()> AfterFunc(Context ctx, std::function<void ()> f);
    struct stopCtx
    {
        Context Context{};
        std::function<bool ()> stop{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    namespace rec
    {
        std::tuple<time::Time, bool> Deadline(const gocpp::PtrRecv<struct stopCtx, false>& self);
        std::tuple<time::Time, bool> Deadline(const gocpp::ObjRecv<struct stopCtx>& self);

        gocpp::channel<gocpp_id_0> Done(const gocpp::PtrRecv<struct stopCtx, false>& self);
        gocpp::channel<gocpp_id_0> Done(const gocpp::ObjRecv<struct stopCtx>& self);

        gocpp::error Err(const gocpp::PtrRecv<struct stopCtx, false>& self);
        gocpp::error Err(const gocpp::ObjRecv<struct stopCtx>& self);

        gocpp::go_any Value(const gocpp::PtrRecv<struct stopCtx, false>& self, gocpp::go_any key);
        gocpp::go_any Value(const gocpp::ObjRecv<struct stopCtx>& self, gocpp::go_any key);
    }

    std::ostream& operator<<(std::ostream& os, const struct stopCtx& value);
    void removeChild(Context parent, canceler child);
    gocpp::string contextName(Context c);
    Context WithoutCancel(Context parent);
    struct withoutCancelCtx
    {
        Context c{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct withoutCancelCtx& value);
    std::tuple<Context, CancelFunc> WithDeadline(Context parent, mocklib::Date d);
    std::tuple<Context, CancelFunc> WithDeadlineCause(Context parent, mocklib::Date d, gocpp::error cause);
    std::tuple<Context, CancelFunc> WithTimeout(Context parent, time::Duration timeout);
    std::tuple<Context, CancelFunc> WithTimeoutCause(Context parent, time::Duration timeout, gocpp::error cause);
    Context WithValue(Context parent, go_any key, go_any val);
    struct valueCtx
    {
        Context Context{};
        go_any key{};
        go_any val{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    namespace rec
    {
        std::tuple<time::Time, bool> Deadline(const gocpp::PtrRecv<struct valueCtx, false>& self);
        std::tuple<time::Time, bool> Deadline(const gocpp::ObjRecv<struct valueCtx>& self);

        gocpp::channel<gocpp_id_0> Done(const gocpp::PtrRecv<struct valueCtx, false>& self);
        gocpp::channel<gocpp_id_0> Done(const gocpp::ObjRecv<struct valueCtx>& self);

        gocpp::error Err(const gocpp::PtrRecv<struct valueCtx, false>& self);
        gocpp::error Err(const gocpp::ObjRecv<struct valueCtx>& self);

        gocpp::go_any Value(const gocpp::PtrRecv<struct valueCtx, false>& self, gocpp::go_any key);
        gocpp::go_any Value(const gocpp::ObjRecv<struct valueCtx>& self, gocpp::go_any key);
    }

    std::ostream& operator<<(std::ostream& os, const struct valueCtx& value);
    go_any value(Context c, go_any key);
}
#include "golang/sync/atomic/value.h"
#include "golang/sync/mutex.h"

namespace golang::context
{
    struct cancelCtx
    {
        Context Context{};
        mocklib::Mutex mu{}; // protects following fields
        atomic::Value done{}; // of chan struct{}, created lazily, closed by first cancel call
        gocpp::map<canceler, gocpp_id_6> children{}; // set to nil by the first cancel call
        gocpp::error err{}; // set to non-nil by the first cancel call
        gocpp::error cause{}; // set to non-nil by the first cancel call

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    namespace rec
    {
        std::tuple<time::Time, bool> Deadline(const gocpp::PtrRecv<struct cancelCtx, false>& self);
        std::tuple<time::Time, bool> Deadline(const gocpp::ObjRecv<struct cancelCtx>& self);

        gocpp::channel<gocpp_id_0> Done(const gocpp::PtrRecv<struct cancelCtx, false>& self);
        gocpp::channel<gocpp_id_0> Done(const gocpp::ObjRecv<struct cancelCtx>& self);

        gocpp::error Err(const gocpp::PtrRecv<struct cancelCtx, false>& self);
        gocpp::error Err(const gocpp::ObjRecv<struct cancelCtx>& self);

        gocpp::go_any Value(const gocpp::PtrRecv<struct cancelCtx, false>& self, gocpp::go_any key);
        gocpp::go_any Value(const gocpp::ObjRecv<struct cancelCtx>& self, gocpp::go_any key);
    }

    std::ostream& operator<<(std::ostream& os, const struct cancelCtx& value);
    cancelCtx* withCancel(Context parent);
    std::tuple<cancelCtx*, bool> parentCancelCtx(Context parent);
}
#include "golang/sync/once.h"
#include "golang/time/sleep.fwd.h"

namespace golang::context
{
    struct afterFuncCtx
    {
        cancelCtx cancelCtx{};
        sync::Once once{}; // either starts running f or stops f from running
        std::function<void ()> f{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct afterFuncCtx& value);
    struct timerCtx
    {
        cancelCtx cancelCtx{};
        time::Timer* timer{}; // Under cancelCtx.mu.
        mocklib::Date deadline{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct timerCtx& value);
}

#include "golang/time/time.h"

namespace golang::context
{

    namespace rec
    {
        gocpp::string Error(deadlineExceededError);
        bool Timeout(deadlineExceededError);
        bool Temporary(deadlineExceededError);
        std::tuple<mocklib::Date, bool> Deadline(emptyCtx);
        gocpp::channel<gocpp_id_1> Done(emptyCtx);
        gocpp::error Err(emptyCtx);
        go_any Value(emptyCtx, go_any key);
        gocpp::string String(backgroundCtx);
        gocpp::string String(todoCtx);
        void cancel(afterFuncCtx* a, bool removeFromParent, gocpp::error err, gocpp::error cause);
        go_any Value(cancelCtx* c, go_any key);
        gocpp::channel<gocpp_id_7> Done(cancelCtx* c);
        gocpp::error Err(cancelCtx* c);
        void propagateCancel(cancelCtx* c, Context parent, canceler child);
        gocpp::string String(cancelCtx* c);
        void cancel(cancelCtx* c, bool removeFromParent, gocpp::error err, gocpp::error cause);
        std::tuple<mocklib::Date, bool> Deadline(withoutCancelCtx);
        gocpp::channel<gocpp_id_15> Done(withoutCancelCtx);
        gocpp::error Err(withoutCancelCtx);
        go_any Value(withoutCancelCtx c, go_any key);
        gocpp::string String(withoutCancelCtx c);
        std::tuple<mocklib::Date, bool> Deadline(timerCtx* c);
        gocpp::string String(timerCtx* c);
        void cancel(timerCtx* c, bool removeFromParent, gocpp::error err, gocpp::error cause);
        gocpp::string String(valueCtx* c);
        go_any Value(valueCtx* c, go_any key);
    }
}

