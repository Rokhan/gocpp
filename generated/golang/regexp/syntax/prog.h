// generated by GoCpp from file '$(ImportDir)/regexp/syntax/prog.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/regexp/syntax/prog.fwd.h"
#include "gocpp/support.h"


namespace golang::syntax
{
    extern gocpp::slice<gocpp::string> instOpNames;
    EmptyOp EmptyOpContext(gocpp::rune r1, gocpp::rune r2);
    bool IsWordChar(gocpp::rune r);
    struct Inst
    {
        InstOp Op{};
        uint32_t Out{}; // all but InstMatch, InstFail
        uint32_t Arg{}; // InstAlt, InstAltMatch, InstCapture, InstEmptyWidth
        gocpp::slice<gocpp::rune> Rune{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct Inst& value);
    gocpp::string u32(uint32_t i);
    struct Prog
    {
        gocpp::slice<Inst> Inst{};
        int Start{}; // index of start instruction
        int NumCap{}; // number of InstCapture insts in re

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct Prog& value);
}
#include "golang/strings/builder.fwd.h"

namespace golang::syntax
{
    void bw(strings::Builder* b, gocpp::slice<gocpp::string> args);
    
    template<typename... Args>
    void bw(strings::Builder* b, Args... args)
    {
        return bw(b, gocpp::ToSlice<gocpp::string>(args...));
    }
    
    template<typename... Args>
    void bw(strings::Builder* b, gocpp::string value, Args... args)
    {
        return bw(b, gocpp::ToSlice<gocpp::string>(value, args...));
    }
    void dumpInst(strings::Builder* b, Inst* i);
    void dumpProg(strings::Builder* b, Prog* p);

    namespace rec
    {
        gocpp::string String(InstOp i);
        gocpp::string String(Prog* p);
        Inst* skipNop(Prog* p, uint32_t pc);
        InstOp op(Inst* i);
        std::tuple<gocpp::string, bool> Prefix(Prog* p);
        EmptyOp StartCond(Prog* p);
        bool MatchRune(Inst* i, gocpp::rune r);
        int MatchRunePos(Inst* i, gocpp::rune r);
        bool MatchEmptyWidth(Inst* i, gocpp::rune before, gocpp::rune after);
        gocpp::string String(Inst* i);
    }
}

