// generated by GoCpp from file '$(ImportDir)/os/exec/exec_windows.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "golang/os/exec/exec_windows.h"
#include "gocpp/support.h"

#include "golang/io/fs/fs.h"
#include "golang/syscall/syscall_windows.h"
#include "golang/syscall/types_windows.h"

namespace golang::exec
{
    namespace rec
    {
    }

    // skipStdinCopyError optionally specifies a function which reports
    // whether the provided stdin copy error should be ignored.
    bool skipStdinCopyError(gocpp::error err)
    {
        // Ignore ERROR_BROKEN_PIPE and ERROR_NO_DATA errors copying
        // to stdin if the program completed successfully otherwise.
        // See Issue 20445.
        auto _ERROR_NO_DATA = syscall::Errno(0xe8);
        auto [pe, ok] = gocpp::getValue<fs::PathError*>(err);
        return ok &&
                pe->Op == "write"_s && pe->Path == "|1"_s &&
                (pe->Err == syscall::ERROR_BROKEN_PIPE || pe->Err == _ERROR_NO_DATA);
    }

}

