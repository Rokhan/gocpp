// generated by GoCpp from file '$(ImportDir)/go/doc/doc.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/go/doc/doc.fwd.h"
#include "gocpp/support.h"

#include "golang/go/token/position.h"
#include "golang/go/ast/ast.fwd.h"
#include "golang/go/ast/scope.fwd.h"
#include "golang/go/doc/example.fwd.h"

namespace golang::doc
{
    struct Package
    {
        gocpp::string Doc{};
        gocpp::string Name{};
        gocpp::string ImportPath{};
        gocpp::slice<gocpp::string> Imports{};
        gocpp::slice<gocpp::string> Filenames{};
        gocpp::map<gocpp::string, gocpp::slice<Note*>> Notes{};
        // Deprecated: For backward compatibility Bugs is still populated,
        // but all new code should use Notes instead.
        gocpp::slice<gocpp::string> Bugs{};
        // declarations
        gocpp::slice<Value*> Consts{};
        gocpp::slice<Type*> Types{};
        gocpp::slice<Value*> Vars{};
        gocpp::slice<Func*> Funcs{};
        // Examples is a sorted list of examples associated with
        // the package. Examples are extracted from _test.go files
        // provided to NewFromFiles.
        gocpp::slice<Example*> Examples{};
        gocpp::map<gocpp::string, gocpp::string> importByName{};
        gocpp::map<gocpp::string, bool> syms{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct Package& value);
    struct Value
    {
        gocpp::string Doc{};
        gocpp::slice<gocpp::string> Names{}; // var or const names in declaration order
        ast::GenDecl* Decl{};
        int order{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct Value& value);
    struct Type
    {
        gocpp::string Doc{};
        gocpp::string Name{};
        ast::GenDecl* Decl{};
        // associated declarations
        gocpp::slice<Value*> Consts{}; // sorted list of constants of (mostly) this type
        gocpp::slice<Value*> Vars{}; // sorted list of variables of (mostly) this type
        gocpp::slice<Func*> Funcs{}; // sorted list of functions returning this type
        gocpp::slice<Func*> Methods{}; // sorted list of methods (including embedded ones) of this type
        // Examples is a sorted list of examples associated with
        // this type. Examples are extracted from _test.go files
        // provided to NewFromFiles.
        gocpp::slice<Example*> Examples{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct Type& value);
    struct Func
    {
        gocpp::string Doc{};
        gocpp::string Name{};
        ast::FuncDecl* Decl{};
        // methods
        // (for functions, these fields have the respective zero value)
        gocpp::string Recv{}; // actual   receiver "T" or "*T" possibly followed by type parameters [P1, ..., Pn]
        gocpp::string Orig{}; // original receiver "T" or "*T"
        int Level{}; // embedding level; 0 means not embedded
        // Examples is a sorted list of examples associated with this
        // function or method. Examples are extracted from _test.go files
        // provided to NewFromFiles.
        gocpp::slice<Example*> Examples{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct Func& value);
    struct Note
    {
        token::Pos Pos{}; // position range of the comment containing the marker
        token::Pos End{};
        gocpp::string UID{}; // uid found with the marker
        gocpp::string Body{}; // note body text

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct Note& value);
    std::tuple<ast::Object*, gocpp::error> simpleImporter(gocpp::map<gocpp::string, ast::Object*> imports, gocpp::string path);
    Package* New(ast::Package* pkg, gocpp::string importPath, Mode mode);
    std::tuple<Package*, gocpp::error> NewFromFiles(token::FileSet* fset, gocpp::slice<ast::File*> files, gocpp::string importPath, gocpp::slice<go_any> opts);
    
    template<typename... Args>
    std::tuple<Package*, gocpp::error> NewFromFiles(token::FileSet* fset, gocpp::slice<ast::File*> files, gocpp::string importPath, Args... opts)
    {
        return NewFromFiles(fset, files, importPath, gocpp::ToSlice<go_any>(opts...));
    }
    
    template<typename... Args>
    std::tuple<Package*, gocpp::error> NewFromFiles(token::FileSet* fset, gocpp::slice<ast::File*> files, gocpp::string importPath, go_any value, Args... opts)
    {
        return NewFromFiles(fset, files, importPath, gocpp::ToSlice<go_any>(value, opts...));
    }
}

#include "golang/go/doc/comment/parse.h"
#include "golang/go/doc/comment/print.h"

namespace golang::doc
{

    namespace rec
    {
        void collectValues(Package* p, gocpp::slice<Value*> values);
        void collectTypes(Package* p, gocpp::slice<Type*> types);
        void collectFuncs(Package* p, gocpp::slice<Func*> funcs);
        bool lookupSym(Package* p, gocpp::string recv, gocpp::string name);
        std::tuple<gocpp::string, bool> lookupPackage(Package* p, gocpp::string name);
        comment::Parser* Parser(Package* p);
        comment::Printer* Printer(Package* p);
        gocpp::slice<unsigned char> HTML(Package* p, gocpp::string text);
        gocpp::slice<unsigned char> Markdown(Package* p, gocpp::string text);
        gocpp::slice<unsigned char> Text(Package* p, gocpp::string text);
    }
}

