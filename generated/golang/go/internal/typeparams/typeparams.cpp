// generated by GoCpp from file '$(ImportDir)/go/internal/typeparams/typeparams.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "golang/go/internal/typeparams/typeparams.h"
#include "gocpp/support.h"

#include "golang/go/ast/ast.h"
#include "golang/go/token/position.h"

namespace golang::typeparams
{
    namespace rec
    {
    }

    ast::Expr PackIndexExpr(ast::Expr x, token::Pos lbrack, gocpp::slice<ast::Expr> exprs, token::Pos rbrack)
    {
        //Go switch emulation
        {
            auto condition = len(exprs);
            int conditionId = -1;
            if(condition == 0) { conditionId = 0; }
            else if(condition == 1) { conditionId = 1; }
            switch(conditionId)
            {
                case 0:
                    gocpp::panic("internal error: PackIndexExpr with empty expr slice"_s);
                    break;
                case 1:
                    return gocpp::InitPtr<ast::IndexExpr>([=](auto& y) {
                        y.X = x;
                        y.Lbrack = lbrack;
                        y.Index = exprs[0];
                        y.Rbrack = rbrack;
                    });
                    break;
                default:
                    return gocpp::InitPtr<ast::IndexListExpr>([=](auto& y) {
                        y.X = x;
                        y.Lbrack = lbrack;
                        y.Indices = exprs;
                        y.Rbrack = rbrack;
                    });
                    break;
            }
        }
    }

    // IndexExpr wraps an ast.IndexExpr or ast.IndexListExpr.
    //
    // Orig holds the original ast.Expr from which this IndexExpr was derived.
    
    template<typename T> requires gocpp::GoStruct<T>
    IndexExpr::operator T()
    {
        T result;
        result.Orig = this->Orig;
        result.IndexListExpr = this->IndexListExpr;
        return result;
    }

    template<typename T> requires gocpp::GoStruct<T>
    bool IndexExpr::operator==(const T& ref) const
    {
        if (Orig != ref.Orig) return false;
        if (IndexListExpr != ref.IndexListExpr) return false;
        return true;
    }

    std::ostream& IndexExpr::PrintTo(std::ostream& os) const
    {
        os << '{';
        os << "" << Orig;
        os << " " << IndexListExpr;
        os << '}';
        return os;
    }

    std::ostream& operator<<(std::ostream& os, const struct IndexExpr& value)
    {
        return value.PrintTo(os);
    }

    IndexExpr* UnpackIndexExpr(ast::Node n)
    {
        //Go type switch emulation
        {
            const auto& gocpp_id_0 = gocpp::type_info(n);
            int conditionId = -1;
            if(gocpp_id_0 == typeid(ast::IndexExpr*)) { conditionId = 0; }
            else if(gocpp_id_0 == typeid(ast::IndexListExpr*)) { conditionId = 1; }
            switch(conditionId)
            {
                case 0:
                {
                    ast::IndexExpr* e = gocpp::any_cast<ast::IndexExpr*>(n);
                    return new IndexExpr {e, gocpp::InitPtr<ast::IndexListExpr>([=](auto& x) {
                        x.X = e->X;
                        x.Lbrack = e->Lbrack;
                        x.Indices = gocpp::slice<ast::Expr> {e->Index};
                        x.Rbrack = e->Rbrack;
                    })};
                    break;
                }
                case 1:
                {
                    ast::IndexListExpr* e = gocpp::any_cast<ast::IndexListExpr*>(n);
                    return new IndexExpr {e, e};
                    break;
                }
            }
        }
        return nullptr;
    }

}

