// generated by GoCpp from file '$(ImportDir)/path/path.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/path/path.fwd.h"
#include "gocpp/support.h"


namespace golang::path
{
    struct lazybuf
    {
        gocpp::string s{};
        gocpp::slice<unsigned char> buf{};
        int w{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct lazybuf& value);
    gocpp::string Clean(gocpp::string path);
    std::tuple<gocpp::string, gocpp::string> Split(gocpp::string path);
    gocpp::string Join(gocpp::slice<gocpp::string> elem);
    
    template<typename... Args>
    gocpp::string Join(Args... elem)
    {
        return Join(gocpp::ToSlice<gocpp::string>(elem...));
    }
    
    template<typename... Args>
    gocpp::string Join(gocpp::string value, Args... elem)
    {
        return Join(gocpp::ToSlice<gocpp::string>(value, elem...));
    }
    gocpp::string Ext(gocpp::string path);
    gocpp::string Base(gocpp::string path);
    bool IsAbs(gocpp::string path);
    gocpp::string Dir(gocpp::string path);

    namespace rec
    {
        unsigned char index(lazybuf* b, int i);
        void append(lazybuf* b, unsigned char c);
        gocpp::string string(lazybuf* b);
    }
}

