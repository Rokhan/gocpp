// generated by GoCpp from file '$(ImportDir)/go/ast/print.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/go/ast/print.fwd.h"
#include "gocpp/support.h"


namespace golang::ast
{
    extern gocpp::slice<unsigned char> indent;
    struct localError
    {
        gocpp::error err{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct localError& value);
}
#include "golang/io/io.h"
#include "golang/reflect/value.h"
#include "golang/go/token/position.fwd.h"

namespace golang::ast
{
    bool NotNilFilter(gocpp::string _1, reflect::Value v);
    gocpp::error Fprint(io::Writer w, token::FileSet* fset, go_any x, FieldFilter f);
    gocpp::error fprint(io::Writer w, token::FileSet* fset, go_any x, FieldFilter f);
    gocpp::error Print(token::FileSet* fset, go_any x);
    struct printer
    {
        io::Writer output{};
        token::FileSet* fset{};
        FieldFilter filter{};
        gocpp::map<go_any, int> ptrmap{}; // *T -> line number
        int indent{}; // current indentation level
        unsigned char last{}; // the last byte processed by Write
        int line{}; // current line number

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct printer& value);
}

#include "golang/reflect/value.h"

namespace golang::ast
{

    namespace rec
    {
        std::tuple<int, gocpp::error> Write(printer* p, gocpp::slice<unsigned char> data);
        void printf(printer* p, gocpp::string format, gocpp::slice<go_any> args);
        
        template<typename... Args>
        void printf(printer* p, gocpp::string format, Args... args)
        {
            return printf(p, format, gocpp::ToSlice<go_any>(args...));
        }
        
        template<typename... Args>
        void printf(printer* p, gocpp::string format, go_any value, Args... args)
        {
            return printf(p, format, gocpp::ToSlice<go_any>(value, args...));
        }
        void print(printer* p, reflect::Value x);
    }
}

