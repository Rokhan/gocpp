// generated by GoCpp from file '$(ImportDir)/os/exec_posix.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/os/exec_posix.fwd.h"
#include "gocpp/support.h"

#include "golang/os/exec.h"
#include "golang/syscall/syscall_windows.h"
#include "golang/syscall/types_windows.h"

namespace golang::os
{
    extern golang::os::Signal Interrupt;
    extern golang::os::Signal Kill;
    std::tuple<Process*, gocpp::error> startProcess(gocpp::string name, gocpp::slice<gocpp::string> argv, ProcAttr* attr);
    struct ProcessState
    {
        int pid{}; // The process's id.
        syscall::WaitStatus status{}; // System-dependent status info.
        syscall::Rusage* rusage{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct ProcessState& value);
}

#include "golang/os/exec.h"

namespace golang::os
{

    namespace rec
    {
        gocpp::error kill(Process* p);
        int Pid(ProcessState* p);
        bool exited(ProcessState* p);
        bool success(ProcessState* p);
        go_any sys(ProcessState* p);
        go_any sysUsage(ProcessState* p);
        gocpp::string String(ProcessState* p);
        int ExitCode(ProcessState* p);
    }
}

