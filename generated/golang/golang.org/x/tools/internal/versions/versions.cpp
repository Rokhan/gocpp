// generated by GoCpp from file '$(ImportDir)/golang.org/x/tools/internal/versions/versions.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "golang/golang.org/x/tools/internal/versions/versions.h"
#include "gocpp/support.h"

#include "golang/golang.org/x/tools/internal/versions/gover.h"
#include "golang/strings/strings.h"

namespace golang::versions
{
    namespace rec
    {
    }

    // Lang returns the Go language version for version x.
    // If x is not a valid version, Lang returns the empty string.
    // For example:
    //
    //	Lang("go1.21rc2") = "go1.21"
    //	Lang("go1.21.2") = "go1.21"
    //	Lang("go1.21") = "go1.21"
    //	Lang("go1") = "go1"
    //	Lang("bad") = ""
    //	Lang("1.21") = ""
    gocpp::string Lang(gocpp::string x)
    {
        auto v = lang(stripGo(x));
        if(v == ""_s)
        {
            return ""_s;
        }
        // "go"+v without allocation
        return x.make_slice(0, 2 + len(v));
    }

    // Compare returns -1, 0, or +1 depending on whether
    // x < y, x == y, or x > y, interpreted as Go versions.
    // The versions x and y must begin with a "go" prefix: "go1.21" not "1.21".
    // Invalid versions, including the empty string, compare less than
    // valid versions and equal to each other.
    // The language version "go1.21" compares less than the
    // release candidate and eventual releases "go1.21rc1" and "go1.21.0".
    // Custom toolchain suffixes are ignored during comparison:
    // "go1.21.0" and "go1.21.0-bigcorp" are equal.
    int Compare(gocpp::string x, gocpp::string y)
    {
        return compare(stripGo(x), stripGo(y));
    }

    // IsValid reports whether the version x is valid.
    bool IsValid(gocpp::string x)
    {
        return isValid(stripGo(x));
    }

    // stripGo converts from a "go1.21" version to a "1.21" version.
    // If v does not start with "go", stripGo returns the empty string (a known invalid version).
    gocpp::string stripGo(gocpp::string v)
    {
        // strip -bigcorp suffix.
        std::tie(v, std::ignore, std::ignore) = strings::Cut(v, "-"_s);
        if(len(v) < 2 || v.make_slice(0, 2) != "go"_s)
        {
            return ""_s;
        }
        return v.make_slice(2);
    }

}

