// generated by GoCpp from file '$(ImportDir)/golang.org/x/tools/internal/gocommand/vendor.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/golang.org/x/tools/internal/gocommand/vendor.fwd.h"
#include "gocpp/support.h"

#include "golang/regexp/regexp.fwd.h"
#include "golang/time/time.fwd.h"

namespace golang::gocommand
{
    struct ModuleJSON
    {
        gocpp::string Path{}; // module path
        gocpp::string Version{}; // module version
        gocpp::slice<gocpp::string> Versions{}; // available module versions (with -versions)
        ModuleJSON* Replace{}; // replaced by this module
        mocklib::Date* Time{}; // time version was created
        ModuleJSON* Update{}; // available update, if any (with -u)
        bool Main{}; // is this the main module?
        bool Indirect{}; // is this module only an indirect dependency of main module?
        gocpp::string Dir{}; // directory holding files for this module, if any
        gocpp::string GoMod{}; // path to go.mod file used when loading this module, if any
        gocpp::string GoVersion{}; // go version used in module

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct ModuleJSON& value);
    extern regexp::Regexp* modFlagRegexp;
}
#include "golang/context/context.h"
#include "golang/golang.org/x/tools/internal/gocommand/invoke.h"

namespace golang::gocommand
{
    std::tuple<bool, ModuleJSON*, gocpp::error> VendorEnabled(context::Context ctx, Invocation inv, Runner* r);
    std::tuple<ModuleJSON*, bool, gocpp::error> getMainModuleAnd114(context::Context ctx, Invocation inv, Runner* r);
    std::tuple<bool, gocpp::slice<ModuleJSON*>, gocpp::error> WorkspaceVendorEnabled(context::Context ctx, Invocation inv, Runner* r);
    std::tuple<gocpp::slice<ModuleJSON*>, gocpp::error> getWorkspaceMainModules(context::Context ctx, Invocation inv, Runner* r);

    namespace rec
    {
    }
}

