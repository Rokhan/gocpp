// generated by GoCpp from file '$(ImportDir)/go/types/mono.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/go/types/mono.fwd.h"
#include "gocpp/support.h"

#include "golang/go/token/position.h"
#include "golang/go/types/type.h"
#include "golang/go/types/object.fwd.h"

namespace golang::types
{
    struct monoVertex
    {
        int weight{}; // weight of heaviest known path to this vertex
        int pre{}; // previous edge (if any) in the above path
        int len{}; // length of the above path
        // obj is the defined type or type parameter represented by this
        // vertex.
        TypeName* obj{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct monoVertex& value);
    struct monoEdge
    {
        int dst{};
        int src{};
        int weight{};
        token::Pos pos{};
        golang::types::Type typ{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct monoEdge& value);
}
#include "golang/go/types/typeparam.fwd.h"

namespace golang::types
{
    struct monoGraph
    {
        gocpp::slice<monoVertex> vertices{};
        gocpp::slice<monoEdge> edges{};
        // canon maps method receiver type parameters to their respective
        // receiver type's type parameters.
        gocpp::map<TypeParam*, TypeParam*> canon{};
        // nameIdx maps a defined type or (canonical) type parameter to its
        // vertex index.
        gocpp::map<TypeName*, int> nameIdx{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct monoGraph& value);
}

#include "golang/go/ast/ast.h"
#include "golang/go/token/position.h"
#include "golang/go/types/check.h"
#include "golang/go/types/named.h"
#include "golang/go/types/package.h"
#include "golang/go/types/type.h"
#include "golang/go/types/typeparam.h"

namespace golang::types
{

    namespace rec
    {
        void monomorph(Checker* check);
        void reportInstanceLoop(Checker* check, int v);
        void recordCanon(monoGraph* w, TypeParam* mpar, TypeParam* tpar);
        void recordInstance(monoGraph* w, Package* pkg, token::Pos pos, gocpp::slice<TypeParam*> tparams, gocpp::slice<golang::types::Type> targs, gocpp::slice<ast::Expr> xlist);
        void assign(monoGraph* w, Package* pkg, token::Pos pos, TypeParam* tpar, golang::types::Type targ);
        int localNamedVertex(monoGraph* w, Package* pkg, Named* named);
        int typeParamVertex(monoGraph* w, TypeParam* tpar);
        void addEdge(monoGraph* w, int dst, int src, int weight, token::Pos pos, golang::types::Type typ);
    }
}

