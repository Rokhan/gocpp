// generated by GoCpp from file '$(ImportDir)/regexp/syntax/compile.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/regexp/syntax/compile.fwd.h"
#include "gocpp/support.h"


namespace golang::syntax
{
    struct patchList
    {
        uint32_t head{};
        uint32_t tail{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct patchList& value);
    patchList makePatchList(uint32_t n);
    struct frag
    {
        uint32_t i{}; // index of first instruction
        patchList out{}; // where to record end instruction
        bool nullable{}; // whether fragment can match empty string

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct frag& value);
}
#include "golang/unicode/letter.h"
#include "golang/regexp/syntax/prog.fwd.h"
#include "golang/regexp/syntax/regexp.fwd.h"

namespace golang::syntax
{
    struct compiler
    {
        Prog* p{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct compiler& value);
    std::tuple<Prog*, gocpp::error> Compile(Regexp* re);
    extern gocpp::slice<gocpp::rune> anyRuneNotNL;
    extern gocpp::slice<gocpp::rune> anyRune;
}

#include "golang/regexp/syntax/parse.h"
#include "golang/regexp/syntax/prog.h"
#include "golang/regexp/syntax/regexp.h"

namespace golang::syntax
{

    namespace rec
    {
        void patch(patchList l, Prog* p, uint32_t val);
        patchList append(patchList l1, Prog* p, patchList l2);
        void init(compiler* c);
        frag compile(compiler* c, Regexp* re);
        frag inst(compiler* c, InstOp op);
        frag nop(compiler* c);
        frag fail(compiler* c);
        frag cap(compiler* c, uint32_t arg);
        frag cat(compiler* c, frag f1, frag f2);
        frag alt(compiler* c, frag f1, frag f2);
        frag quest(compiler* c, frag f1, bool nongreedy);
        frag loop(compiler* c, frag f1, bool nongreedy);
        frag star(compiler* c, frag f1, bool nongreedy);
        frag plus(compiler* c, frag f1, bool nongreedy);
        frag empty(compiler* c, EmptyOp op);
        frag rune(compiler* c, gocpp::slice<gocpp::rune> r, Flags flags);
    }
}

