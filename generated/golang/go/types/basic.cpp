// generated by GoCpp from file '$(ImportDir)/go/types/basic.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "golang/go/types/basic.h"
#include "gocpp/support.h"

#include "golang/go/types/type.h"
#include "golang/go/types/typestring.h"

namespace golang::types
{
    namespace rec
    {
    }

    // BasicKind describes the kind of basic type.
    // BasicInfo is a set of flags describing properties of a basic type.
    // A Basic represents a basic type.
    
    template<typename T> requires gocpp::GoStruct<T>
    Basic::operator T()
    {
        T result;
        result.kind = this->kind;
        result.info = this->info;
        result.name = this->name;
        return result;
    }

    template<typename T> requires gocpp::GoStruct<T>
    bool Basic::operator==(const T& ref) const
    {
        if (kind != ref.kind) return false;
        if (info != ref.info) return false;
        if (name != ref.name) return false;
        return true;
    }

    std::ostream& Basic::PrintTo(std::ostream& os) const
    {
        os << '{';
        os << "" << kind;
        os << " " << info;
        os << " " << name;
        os << '}';
        return os;
    }

    std::ostream& operator<<(std::ostream& os, const struct Basic& value)
    {
        return value.PrintTo(os);
    }

    // Kind returns the kind of basic type b.
    BasicKind rec::Kind(Basic* b)
    {
        return b->kind;
    }

    // Info returns information about properties of basic type b.
    BasicInfo rec::Info(Basic* b)
    {
        return b->info;
    }

    // Name returns the name of basic type b.
    gocpp::string rec::Name(Basic* b)
    {
        return b->name;
    }

    golang::types::Type rec::Underlying(Basic* b)
    {
        return b;
    }

    gocpp::string rec::String(Basic* b)
    {
        return TypeString(b, nullptr);
    }

}

