// generated by GoCpp from file '$(ImportDir)/golang.org/x/tools/internal/gcimporter/ureader_yes.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/golang.org/x/tools/internal/gcimporter/ureader_yes.fwd.h"
#include "gocpp/support.h"

#include "golang/go/types/type.h"
#include "golang/golang.org/x/tools/internal/gcimporter/bimport.h"
#include "golang/golang.org/x/tools/internal/pkgbits/decoder.h"
#include "golang/golang.org/x/tools/internal/pkgbits/reloc.h"
#include "golang/go/token/position.fwd.h"
#include "golang/go/types/context.fwd.h"
#include "golang/go/types/interface.fwd.h"
#include "golang/go/types/package.fwd.h"
#include "golang/go/types/scope.fwd.h"

namespace golang::gcimporter
{
    struct pkgReader
    {
        pkgbits::PkgDecoder PkgDecoder{};
        fakeFileSet fake{};
        types::Context* ctxt{};
        gocpp::map<gocpp::string, types::Package*> imports{}; // previously imported packages, indexed by path
        bool aliases{}; // create types.Alias nodes
        // lazily initialized arrays corresponding to the unified IR
        // PosBase, Pkg, and Type sections, respectively.
        gocpp::slice<gocpp::string> posBases{}; // position bases (i.e., file names)
        gocpp::slice<types::Package*> pkgs{};
        gocpp::slice<types::Type> typs{};
        // laterFns holds functions that need to be invoked at the end of
        // import reading.
        gocpp::slice<std::function<void ()>> laterFns{};
        // laterFors is used in case of 'type A B' to ensure that B is processed before A.
        gocpp::map<types::Type, int> laterFors{};
        // ifaces holds a list of constructed Interfaces, which need to have
        // Complete called after importing is done.
        gocpp::slice<types::Interface*> ifaces{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct pkgReader& value);
    struct derivedInfo
    {
        pkgbits::Index idx{};
        bool needed{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct derivedInfo& value);
    struct typeInfo
    {
        pkgbits::Index idx{};
        bool derived{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct typeInfo& value);
    std::tuple<int, types::Package*, gocpp::error> UImportData(token::FileSet* fset, gocpp::map<gocpp::string, types::Package*> imports, gocpp::slice<unsigned char> data, gocpp::string path);
    types::Package* readUnifiedPackage(token::FileSet* fset, types::Context* ctxt, gocpp::map<gocpp::string, types::Package*> imports, pkgbits::PkgDecoder input);
    struct reader
    {
        pkgbits::Decoder Decoder{};
        pkgReader* p{};
        readerDict* dict{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct reader& value);
    types::Scope* pkgScope(types::Package* pkg);
}
#include "golang/go/types/typeparam.fwd.h"

namespace golang::gcimporter
{
    struct readerDict
    {
        // bounds is a slice of typeInfos corresponding to the underlying
        // bounds of the element's type parameters.
        gocpp::slice<typeInfo> bounds{};
        // tparams is a slice of the constructed TypeParams for the element.
        gocpp::slice<types::TypeParam*> tparams{};
        // devived is a slice of types derived from tparams, which may be
        // instantiated while reading the current element.
        gocpp::slice<derivedInfo> derived{};
        gocpp::slice<types::Type> derivedTypes{}; // lazily instantiated from derived

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct readerDict& value);
}

#include "golang/go/token/position.h"
#include "golang/go/types/interface.h"
#include "golang/go/types/object.h"
#include "golang/go/types/package.h"
#include "golang/go/types/signature.h"
#include "golang/go/types/struct.h"
#include "golang/go/types/tuple.h"
#include "golang/go/types/type.h"
#include "golang/go/types/typeparam.h"
#include "golang/go/types/union.h"
#include "golang/golang.org/x/tools/internal/pkgbits/reloc.h"
#include "golang/golang.org/x/tools/internal/pkgbits/sync.h"

namespace golang::gcimporter
{

    namespace rec
    {
        void later(pkgReader* pr, std::function<void ()> fn);
        void laterFor(pkgReader* pr, types::Type t, std::function<void ()> fn);
        reader* newReader(pkgReader* pr, pkgbits::RelocKind k, pkgbits::Index idx, pkgbits::SyncMarker marker);
        reader* tempReader(pkgReader* pr, pkgbits::RelocKind k, pkgbits::Index idx, pkgbits::SyncMarker marker);
        void retireReader(pkgReader* pr, reader* r);
        token::Pos pos(reader* r);
        gocpp::string posBase(reader* r);
        gocpp::string posBaseIdx(pkgReader* pr, pkgbits::Index idx);
        types::Package* pkg(reader* r);
        types::Package* pkgIdx(pkgReader* pr, pkgbits::Index idx);
        types::Package* doPkg(reader* r);
        types::Type typ(reader* r);
        typeInfo typInfo(reader* r);
        types::Type typIdx(pkgReader* pr, typeInfo info, readerDict* dict);
        types::Type doTyp(reader* r);
        types::Struct* structType(reader* r);
        types::Union* unionType(reader* r);
        types::Interface* interfaceType(reader* r);
        types::Signature* signature(reader* r, types::Var* recv, gocpp::slice<types::TypeParam*> rtparams, gocpp::slice<types::TypeParam*> tparams);
        types::Tuple* params(reader* r);
        types::Var* param(reader* r);
        std::tuple<types::Object, gocpp::slice<types::Type>> obj(reader* r);
        std::tuple<types::Package*, gocpp::string> objIdx(pkgReader* pr, pkgbits::Index idx);
        readerDict* objDictIdx(pkgReader* pr, pkgbits::Index idx);
        gocpp::slice<types::TypeParam*> typeParamNames(reader* r);
        types::Func* method(reader* r);
        std::tuple<types::Package*, gocpp::string> qualifiedIdent(reader* r);
        std::tuple<types::Package*, gocpp::string> localIdent(reader* r);
        std::tuple<types::Package*, gocpp::string> selector(reader* r);
        std::tuple<types::Package*, gocpp::string> ident(reader* r, pkgbits::SyncMarker marker);
    }
}

