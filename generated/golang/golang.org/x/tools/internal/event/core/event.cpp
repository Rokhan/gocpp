// generated by GoCpp from file '$(ImportDir)/golang.org/x/tools/internal/event/core/event.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "golang/golang.org/x/tools/internal/event/core/event.h"
#include "gocpp/support.h"

#include "golang/fmt/print.h"
#include "golang/golang.org/x/tools/internal/event/label/label.h"
#include "golang/io/io.h"
#include "golang/time/format.h"
#include "golang/time/time.h"

// Package core provides support for event based telemetry.
namespace golang::core
{
    namespace rec
    {
        using fmt::rec::Write;
        using label::rec::Key;
        using label::rec::Valid;
        using time::rec::Format;
        using time::rec::IsZero;
    }

    // Event holds the information about an event of note that occurred.
    
    template<typename T> requires gocpp::GoStruct<T>
    Event::operator T()
    {
        T result;
        result.at = this->at;
        result.go_static = this->go_static;
        result.dynamic = this->dynamic;
        return result;
    }

    template<typename T> requires gocpp::GoStruct<T>
    bool Event::operator==(const T& ref) const
    {
        if (at != ref.at) return false;
        if (go_static != ref.go_static) return false;
        if (dynamic != ref.dynamic) return false;
        return true;
    }

    std::ostream& Event::PrintTo(std::ostream& os) const
    {
        os << '{';
        os << "" << at;
        os << " " << go_static;
        os << " " << dynamic;
        os << '}';
        return os;
    }

    std::ostream& operator<<(std::ostream& os, const struct Event& value)
    {
        return value.PrintTo(os);
    }

    // eventLabelMap implements label.Map for a the labels of an Event.
    
    template<typename T> requires gocpp::GoStruct<T>
    eventLabelMap::operator T()
    {
        T result;
        result.event = this->event;
        return result;
    }

    template<typename T> requires gocpp::GoStruct<T>
    bool eventLabelMap::operator==(const T& ref) const
    {
        if (event != ref.event) return false;
        return true;
    }

    std::ostream& eventLabelMap::PrintTo(std::ostream& os) const
    {
        os << '{';
        os << "" << event;
        os << '}';
        return os;
    }

    std::ostream& operator<<(std::ostream& os, const struct eventLabelMap& value)
    {
        return value.PrintTo(os);
    }

    mocklib::Date rec::At(Event ev)
    {
        return ev.at;
    }

    void rec::Format(Event ev, fmt::State f, gocpp::rune r)
    {
        if(! rec::IsZero(gocpp::recv(ev.at)))
        {
            fmt::Fprint(f, rec::Format(gocpp::recv(ev.at), "2006/01/02 15:04:05 "_s));
        }
        for(auto index = 0; rec::Valid(gocpp::recv(ev), index); index++)
        {
            if(auto l = rec::Label(gocpp::recv(ev), index); rec::Valid(gocpp::recv(l)))
            {
                fmt::Fprintf(f, "\n\t%v"_s, l);
            }
        }
    }

    bool rec::Valid(Event ev, int index)
    {
        return index >= 0 && index < len(ev.go_static) + len(ev.dynamic);
    }

    label::Label rec::Label(Event ev, int index)
    {
        if(index < len(ev.go_static))
        {
            return ev.go_static[index];
        }
        return ev.dynamic[index - len(ev.go_static)];
    }

    label::Label rec::Find(Event ev, label::Key key)
    {
        for(auto [gocpp_ignored, l] : ev.go_static)
        {
            if(rec::Key(gocpp::recv(l)) == key)
            {
                return l;
            }
        }
        for(auto [gocpp_ignored, l] : ev.dynamic)
        {
            if(rec::Key(gocpp::recv(l)) == key)
            {
                return l;
            }
        }
        return label::Label {};
    }

    Event MakeEvent(gocpp::array<label::Label, 3> go_static, gocpp::slice<label::Label> labels)
    {
        return gocpp::Init<Event>([=](auto& x) {
            x.go_static = go_static;
            x.dynamic = labels;
        });
    }

    // CloneEvent event returns a copy of the event with the time adjusted to at.
    Event CloneEvent(Event ev, mocklib::Date at)
    {
        ev.at = at;
        return ev;
    }

}

