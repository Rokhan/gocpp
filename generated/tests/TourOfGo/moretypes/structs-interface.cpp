// generated by GoCpp from file 'tests/TourOfGo/moretypes/structs-interface.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "tests/TourOfGo/moretypes/structs-interface.h"
#include "gocpp/support.h"

#include "golang/fmt/print.h"

namespace golang::main
{
    namespace rec
    {
    }

    // Small example extracted from io/io.go
    
    template<typename T>
    Reader::Reader(T& ref)
    {
        value.reset(new ReaderImpl<T, std::unique_ptr<T>>(new T(ref)));
    }

    template<typename T>
    Reader::Reader(const T& ref)
    {
        value.reset(new ReaderImpl<T, std::unique_ptr<T>>(new T(ref)));
    }

    template<typename T>
    Reader::Reader(T* ptr)
    {
        value.reset(new ReaderImpl<T, gocpp::ptr<T>>(ptr));
    }

    std::ostream& Reader::PrintTo(std::ostream& os) const
    {
        return os;
    }

    template<typename T, typename TStore, typename TInterface>
    int Reader::ReaderImpl<T, TStore, TInterface>::vRead()
    {
        return rec::Read(gocpp::PtrRecv<T, false>(value.get()));
    }

    namespace rec
    {
        int Read(const gocpp::PtrRecv<struct Reader, false>& self)
        {
            return self.ptr->value->vRead();
        }

        int Read(const gocpp::ObjRecv<struct Reader>& self)
        {
            return self.obj.value->vRead();
        }
    }

    std::ostream& operator<<(std::ostream& os, const struct Reader& value)
    {
        return value.PrintTo(os);
    }

    
    template<typename T>
    WriterTo::WriterTo(T& ref)
    {
        value.reset(new WriterToImpl<T, std::unique_ptr<T>>(new T(ref)));
    }

    template<typename T>
    WriterTo::WriterTo(const T& ref)
    {
        value.reset(new WriterToImpl<T, std::unique_ptr<T>>(new T(ref)));
    }

    template<typename T>
    WriterTo::WriterTo(T* ptr)
    {
        value.reset(new WriterToImpl<T, gocpp::ptr<T>>(ptr));
    }

    std::ostream& WriterTo::PrintTo(std::ostream& os) const
    {
        return os;
    }

    template<typename T, typename TStore, typename TInterface>
    int WriterTo::WriterToImpl<T, TStore, TInterface>::vWriteTo()
    {
        return rec::WriteTo(gocpp::PtrRecv<T, false>(value.get()));
    }

    namespace rec
    {
        int WriteTo(const gocpp::PtrRecv<struct WriterTo, false>& self)
        {
            return self.ptr->value->vWriteTo();
        }

        int WriteTo(const gocpp::ObjRecv<struct WriterTo>& self)
        {
            return self.obj.value->vWriteTo();
        }
    }

    std::ostream& operator<<(std::ostream& os, const struct WriterTo& value)
    {
        return value.PrintTo(os);
    }

    
    template<typename T>
    Closer::Closer(T& ref)
    {
        value.reset(new CloserImpl<T, std::unique_ptr<T>>(new T(ref)));
    }

    template<typename T>
    Closer::Closer(const T& ref)
    {
        value.reset(new CloserImpl<T, std::unique_ptr<T>>(new T(ref)));
    }

    template<typename T>
    Closer::Closer(T* ptr)
    {
        value.reset(new CloserImpl<T, gocpp::ptr<T>>(ptr));
    }

    std::ostream& Closer::PrintTo(std::ostream& os) const
    {
        return os;
    }

    template<typename T, typename TStore, typename TInterface>
    gocpp::string Closer::CloserImpl<T, TStore, TInterface>::vClose()
    {
        return rec::Close(gocpp::PtrRecv<T, false>(value.get()));
    }

    namespace rec
    {
        gocpp::string Close(const gocpp::PtrRecv<struct Closer, false>& self)
        {
            return self.ptr->value->vClose();
        }

        gocpp::string Close(const gocpp::ObjRecv<struct Closer>& self)
        {
            return self.obj.value->vClose();
        }
    }

    std::ostream& operator<<(std::ostream& os, const struct Closer& value)
    {
        return value.PrintTo(os);
    }

    
    template<typename T>
    ReadCloser::ReadCloser(T& ref)
    {
        value.reset(new ReadCloserImpl<T, std::unique_ptr<T>>(new T(ref)));
    }

    template<typename T>
    ReadCloser::ReadCloser(const T& ref)
    {
        value.reset(new ReadCloserImpl<T, std::unique_ptr<T>>(new T(ref)));
    }

    template<typename T>
    ReadCloser::ReadCloser(T* ptr)
    {
        value.reset(new ReadCloserImpl<T, gocpp::ptr<T>>(ptr));
    }

    std::ostream& ReadCloser::PrintTo(std::ostream& os) const
    {
        return os;
    }


    namespace rec
    {
        int Read(const gocpp::PtrRecv<struct ReadCloser, false>& self)
        {
            return self.ptr->value->vRead();
        }

        int Read(const gocpp::ObjRecv<struct ReadCloser>& self)
        {
            return self.obj.value->vRead();
        }

        gocpp::string Close(const gocpp::PtrRecv<struct ReadCloser, false>& self)
        {
            return self.ptr->value->vClose();
        }

        gocpp::string Close(const gocpp::ObjRecv<struct ReadCloser>& self)
        {
            return self.obj.value->vClose();
        }
    }

    std::ostream& operator<<(std::ostream& os, const struct ReadCloser& value)
    {
        return value.PrintTo(os);
    }

    // NopCloser returns a [ReadCloser] with a no-op Close method wrapping
    // the provided [Reader] r.
    // If r implements [WriterTo], the returned [ReadCloser] will implement [WriterTo]
    // by forwarding calls to r.
    struct ReadCloser NopCloser(struct Reader r)
    {
        if(auto [gocpp_id_0, ok] = gocpp::getValue<WriterTo>(r); ok)
        {
            return new golang::main::nopCloserWriterTo {r};
        }
        return new golang::main::nopCloser {r};
    }

    
    template<typename T> requires gocpp::GoStruct<T>
    nopCloser::operator T()
    {
        T result;
        result.Reader = this->Reader;
        return result;
    }

    template<typename T> requires gocpp::GoStruct<T>
    bool nopCloser::operator==(const T& ref) const
    {
        if (Reader != ref.Reader) return false;
        return true;
    }

    std::ostream& nopCloser::PrintTo(std::ostream& os) const
    {
        os << '{';
        os << "" << Reader;
        os << '}';
        return os;
    }

    namespace rec
    {
        int Read(const gocpp::PtrRecv<struct nopCloser, false>& self)
        {
            return rec::Read(self.ptr->Reader);
        }

        int Read(const gocpp::ObjRecv<struct nopCloser>& self)
        {
            return rec::Read(self.obj.Reader);
        }
    }

    std::ostream& operator<<(std::ostream& os, const struct nopCloser& value)
    {
        return value.PrintTo(os);
    }

    gocpp::string rec::Close(nopCloser)
    {
        return "nopCloser.Close"_s;
    }

    
    template<typename T> requires gocpp::GoStruct<T>
    nopCloserWriterTo::operator T()
    {
        T result;
        result.Reader = this->Reader;
        return result;
    }

    template<typename T> requires gocpp::GoStruct<T>
    bool nopCloserWriterTo::operator==(const T& ref) const
    {
        if (Reader != ref.Reader) return false;
        return true;
    }

    std::ostream& nopCloserWriterTo::PrintTo(std::ostream& os) const
    {
        os << '{';
        os << "" << Reader;
        os << '}';
        return os;
    }

    namespace rec
    {
        int Read(const gocpp::PtrRecv<struct nopCloserWriterTo, false>& self)
        {
            return rec::Read(self.ptr->Reader);
        }

        int Read(const gocpp::ObjRecv<struct nopCloserWriterTo>& self)
        {
            return rec::Read(self.obj.Reader);
        }
    }

    std::ostream& operator<<(std::ostream& os, const struct nopCloserWriterTo& value)
    {
        return value.PrintTo(os);
    }

    gocpp::string rec::Close(nopCloserWriterTo)
    {
        return "nopCloserWriterTo.Close"_s;
    }

    int rec::WriteTo(nopCloserWriterTo c)
    {
        return rec::WriteTo(gocpp::recv(gocpp::getValue<WriterTo>(c.Reader)));
    }

    // myReader implements only Reader.
    
    template<typename T> requires gocpp::GoStruct<T>
    myReader::operator T()
    {
        T result;
        result.val = this->val;
        return result;
    }

    template<typename T> requires gocpp::GoStruct<T>
    bool myReader::operator==(const T& ref) const
    {
        if (val != ref.val) return false;
        return true;
    }

    std::ostream& myReader::PrintTo(std::ostream& os) const
    {
        os << '{';
        os << "" << val;
        os << '}';
        return os;
    }

    std::ostream& operator<<(std::ostream& os, const struct myReader& value)
    {
        return value.PrintTo(os);
    }

    int rec::Read(myReader r)
    {
        return r.val;
    }

    // myReaderWriterTo implements both Reader and WriterTo.
    
    template<typename T> requires gocpp::GoStruct<T>
    myReaderWriterTo::operator T()
    {
        T result;
        result.val = this->val;
        return result;
    }

    template<typename T> requires gocpp::GoStruct<T>
    bool myReaderWriterTo::operator==(const T& ref) const
    {
        if (val != ref.val) return false;
        return true;
    }

    std::ostream& myReaderWriterTo::PrintTo(std::ostream& os) const
    {
        os << '{';
        os << "" << val;
        os << '}';
        return os;
    }

    std::ostream& operator<<(std::ostream& os, const struct myReaderWriterTo& value)
    {
        return value.PrintTo(os);
    }

    int rec::Read(myReaderWriterTo r)
    {
        return r.val;
    }

    int rec::WriteTo(myReaderWriterTo r)
    {
        return r.val * 2;
    }

    void testCloser()
    {
        // Case 1: r implements only Reader -> should return nopCloser{r}
        auto r1 = gocpp::Init<golang::main::myReader>([=](auto& x) {
            x.val = 1;
        });
        auto c1 = NopCloser(r1);
        auto [gocpp_id_1, isWriterTo1] = gocpp::getValue<WriterTo>(c1);
        mocklib::Println("case1 implements WriterTo:"_s, isWriterTo1);
        mocklib::Println("case1 Read():"_s, rec::Read(gocpp::recv(c1)));
        mocklib::Println("case1 Close():"_s, rec::Close(gocpp::recv(c1)));

        // Case 2: r implements Reader and WriterTo -> should return nopCloserWriterTo{r}
        auto r2 = gocpp::Init<golang::main::myReaderWriterTo>([=](auto& x) {
            x.val = 5;
        });
        auto c2 = NopCloser(r2);
        auto [wt2, isWriterTo2] = gocpp::getValue<WriterTo>(c2);
        mocklib::Println("case2 implements WriterTo:"_s, isWriterTo2);
        mocklib::Println("case2 Read():"_s, rec::Read(gocpp::recv(c2)));
        mocklib::Println("case2 Close():"_s, rec::Close(gocpp::recv(c2)));
        if(isWriterTo2)
        {
            mocklib::Println("case2 WriteTo():"_s, rec::WriteTo(gocpp::recv(wt2)));
        }

        // Case 3: r passed as a static Reader interface (only Reader, no WriterTo)
        Reader r3 = gocpp::Init<golang::main::myReader>([=](auto& x) {
            x.val = 0;
        });
        auto c3 = NopCloser(r3);
        auto [gocpp_id_2, isWriterTo3] = gocpp::getValue<WriterTo>(c3);
        mocklib::Println("case3 implements WriterTo:"_s, isWriterTo3);
        mocklib::Println("case3 Read():"_s, rec::Read(gocpp::recv(c3)));

        // Case 4: double-wrapping NopCloser(NopCloser(r)) where inner has WriterTo.
        // The outer nopCloser wraps a ReadCloser; since the inner concrete type
        // (nopCloserWriterTo) implements WriterTo, the outer wrapper should
        // also be detected as implementing WriterTo via forwarding.
        auto inner = NopCloser(gocpp::Init<golang::main::myReaderWriterTo>([=](auto& x) {
            x.val = 7;
        }));
        auto outer = NopCloser(inner);
        auto [wt4, isWriterTo4] = gocpp::getValue<WriterTo>(outer);
        mocklib::Println("case4 implements WriterTo:"_s, isWriterTo4);
        if(isWriterTo4)
        {
            mocklib::Println("case4 WriteTo():"_s, rec::WriteTo(gocpp::recv(wt4)));
        }
        mocklib::Println("case4 Read():"_s, rec::Read(gocpp::recv(outer)));
        mocklib::Println("case4 Close():"_s, rec::Close(gocpp::recv(outer)));

        // Case 5: double-wrapping where inner does NOT implement WriterTo
        auto inner2 = NopCloser(gocpp::Init<golang::main::myReader>([=](auto& x) {
            x.val = 9;
        }));
        auto outer2 = NopCloser(inner2);
        auto [gocpp_id_3, isWriterTo5] = gocpp::getValue<WriterTo>(outer2);
        mocklib::Println("case5 implements WriterTo:"_s, isWriterTo5);
        mocklib::Println("case5 Read():"_s, rec::Read(gocpp::recv(outer2)));
        mocklib::Println("case5 Close():"_s, rec::Close(gocpp::recv(outer2)));
    }

    void main()
    {
        testCloser();
    }

}

int main()
{
    try
    {
        std::cout << std::boolalpha << std::setprecision(5) << std::fixed;
        golang::main::main();
        return 0;
    }
    catch(const gocpp::GoPanic& ex)
    {
        std::cout << "Panic: " << ex.what() << std::endl;
        return -1;
    }
}
