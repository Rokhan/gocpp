// generated by GoCpp from file '$(ImportDir)/go/types/tuple.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "golang/go/types/tuple.h"
#include "gocpp/support.h"

#include "golang/go/types/object.h"
#include "golang/go/types/type.h"
#include "golang/go/types/typestring.h"

namespace golang::types
{
    namespace rec
    {
    }

    // A Tuple represents an ordered list of variables; a nil *Tuple is a valid (empty) tuple.
    // Tuples are used as components of signatures and to represent the type of multiple
    // assignments; they are not first class types of Go.
    
    template<typename T> requires gocpp::GoStruct<T>
    Tuple::operator T()
    {
        T result;
        result.vars = this->vars;
        return result;
    }

    template<typename T> requires gocpp::GoStruct<T>
    bool Tuple::operator==(const T& ref) const
    {
        if (vars != ref.vars) return false;
        return true;
    }

    std::ostream& Tuple::PrintTo(std::ostream& os) const
    {
        os << '{';
        os << "" << vars;
        os << '}';
        return os;
    }

    std::ostream& operator<<(std::ostream& os, const struct Tuple& value)
    {
        return value.PrintTo(os);
    }

    // NewTuple returns a new tuple for the given variables.
    Tuple* NewTuple(gocpp::slice<Var*> x)
    {
        if(len(x) > 0)
        {
            return gocpp::InitPtr<Tuple>([=](auto& y) {
                y.vars = x;
            });
        }
        return nullptr;
    }

    // Len returns the number variables of tuple t.
    int rec::Len(Tuple* t)
    {
        if(t != nullptr)
        {
            return len(t->vars);
        }
        return 0;
    }

    // At returns the i'th variable of tuple t.
    Var* rec::At(Tuple* t, int i)
    {
        return t->vars[i];
    }

    golang::types::Type rec::Underlying(Tuple* t)
    {
        return t;
    }

    gocpp::string rec::String(Tuple* t)
    {
        return TypeString(t, nullptr);
    }

}

