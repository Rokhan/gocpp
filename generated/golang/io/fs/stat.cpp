// generated by GoCpp from file '$(ImportDir)/io/fs/stat.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "golang/io/fs/stat.h"
#include "gocpp/support.h"

#include "golang/io/fs/fs.h"

namespace golang::fs
{
    namespace rec
    {
    }

    // A StatFS is a file system with a Stat method.
    
    template<typename T>
    StatFS::StatFS(T& ref)
    {
        mValue.reset(new StatFSImpl<T, std::unique_ptr<T>>(new T(ref)));
    }

    template<typename T>
    StatFS::StatFS(const T& ref)
    {
        mValue.reset(new StatFSImpl<T, std::unique_ptr<T>>(new T(ref)));
    }

    template<typename T>
    StatFS::StatFS(T* ptr)
    {
        mValue.reset(new StatFSImpl<T, gocpp::ptr<T>>(ptr));
    }

    std::ostream& StatFS::PrintTo(std::ostream& os) const
    {
        return os;
    }

    template<typename T, typename TStore, typename TInterface>
    std::tuple<FileInfo, gocpp::error> StatFS::StatFSImpl<T, TStore, TInterface>::vStat(gocpp::string name)
    {
        return rec::Stat(gocpp::PtrRecv<T, false>(value.get()), name);
    }

    inline StatFS::IStatFS* StatFS::value() const
    {
        if(auto res = mValue.get()) { return res; }
        throw gocpp::GoPanic("using nil value for interface 'StatFS'");
    }

    namespace rec
    {
        std::tuple<FileInfo, gocpp::error> Stat(const gocpp::PtrRecv<struct StatFS, false>& self, gocpp::string name)
        {
            return self.ptr->value()->vStat(name);
        }

        std::tuple<FileInfo, gocpp::error> Stat(const gocpp::ObjRecv<struct StatFS>& self, gocpp::string name)
        {
            return self.obj.value()->vStat(name);
        }

        std::tuple<fs::File, gocpp::error> Open(const gocpp::PtrRecv<struct StatFS, false>& self, gocpp::string name)
        {
            return self.ptr->value()->vOpen(name);
        }

        std::tuple<fs::File, gocpp::error> Open(const gocpp::ObjRecv<struct StatFS>& self, gocpp::string name)
        {
            return self.obj.value()->vOpen(name);
        }
    }

    std::ostream& operator<<(std::ostream& os, const struct StatFS& value)
    {
        return value.PrintTo(os);
    }

    // Stat returns a [FileInfo] describing the named file from the file system.
    //
    // If fs implements [StatFS], Stat calls fs.Stat.
    // Otherwise, Stat opens the [File] to stat it.
    std::tuple<FileInfo, gocpp::error> Stat(FS fsys, gocpp::string name)
    {
        gocpp::Defer defer;
        try
        {
            {
                auto [fsys_tmp, ok] = gocpp::getValue<StatFS>(fsys);
                if(auto& fsys = fsys_tmp; ok)
                {
                    return rec::Stat(gocpp::recv(fsys), name);
                }
            }

            auto [file, err] = rec::Open(gocpp::recv(fsys), name);
            if(err != nullptr)
            {
                return {nullptr, err};
            }
            defer.push_back([=]{ rec::Close(gocpp::recv(file)); });
            return rec::Stat(gocpp::recv(file));
        }
        catch(gocpp::GoPanic& gp)
        {
            defer.handlePanic(gp);
        }
    }

}

