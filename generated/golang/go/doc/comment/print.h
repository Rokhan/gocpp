// generated by GoCpp from file '$(ImportDir)/go/doc/comment/print.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/go/doc/comment/print.fwd.h"
#include "gocpp/support.h"


namespace golang::comment
{
    struct commentPrinter
    {
        Printer* Printer{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct commentPrinter& value);
}
#include "golang/go/doc/comment/parse.h"

namespace golang::comment
{
    struct Printer
    {
        // HeadingLevel is the nesting level used for
        // HTML and Markdown headings.
        // If HeadingLevel is zero, it defaults to level 3,
        // meaning to use <h3> and ###.
        int HeadingLevel{};
        // HeadingID is a function that computes the heading ID
        // (anchor tag) to use for the heading h when generating
        // HTML and Markdown. If HeadingID returns an empty string,
        // then the heading ID is omitted.
        // If HeadingID is nil, h.DefaultID is used.
        std::function<gocpp::string (Heading* h)> HeadingID{};
        // DocLinkURL is a function that computes the URL for the given DocLink.
        // If DocLinkURL is nil, then link.DefaultURL(p.DocLinkBaseURL) is used.
        std::function<gocpp::string (DocLink* link)> DocLinkURL{};
        // DocLinkBaseURL is used when DocLinkURL is nil,
        // passed to [DocLink.DefaultURL] to construct a DocLink's URL.
        // See that method's documentation for details.
        gocpp::string DocLinkBaseURL{};
        // TextPrefix is a prefix to print at the start of every line
        // when generating text output using the Text method.
        gocpp::string TextPrefix{};
        // TextCodePrefix is the prefix to print at the start of each
        // preformatted (code block) line when generating text output,
        // instead of (not in addition to) TextPrefix.
        // If TextCodePrefix is the empty string, it defaults to TextPrefix+"\t".
        gocpp::string TextCodePrefix{};
        // TextWidth is the maximum width text line to generate,
        // measured in Unicode code points,
        // excluding TextPrefix and the newline character.
        // If TextWidth is zero, it defaults to 80 minus the number of code points in TextPrefix.
        // If TextWidth is negative, there is no limit.
        int TextWidth{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct Printer& value);
    bool blankBefore(Block x);
}

#include "golang/bytes/buffer.h"
#include "golang/go/doc/comment/parse.h"

namespace golang::comment
{

    namespace rec
    {
        int headingLevel(Printer* p);
        gocpp::string headingID(Printer* p, Heading* h);
        gocpp::string docLinkURL(Printer* p, DocLink* link);
        gocpp::string DefaultURL(DocLink* l, gocpp::string baseURL);
        gocpp::string DefaultID(Heading* h);
        gocpp::slice<unsigned char> Comment(Printer* p, Doc* d);
        void block(commentPrinter* p, bytes::Buffer* out, Block x);
        void text(commentPrinter* p, bytes::Buffer* out, gocpp::string indent, gocpp::slice<golang::comment::Text> x);
        void indent(commentPrinter* p, bytes::Buffer* out, gocpp::string indent, gocpp::string s);
    }
}

