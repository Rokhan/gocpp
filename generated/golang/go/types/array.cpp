// generated by GoCpp from file '$(ImportDir)/go/types/array.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "golang/go/types/array.h"
#include "gocpp/support.h"

#include "golang/go/types/type.h"
#include "golang/go/types/typestring.h"

namespace golang::types
{
    namespace rec
    {
    }

    // An Array represents an array type.
    
    template<typename T> requires gocpp::GoStruct<T>
    Array::operator T()
    {
        T result;
        result.len = this->len;
        result.elem = this->elem;
        return result;
    }

    template<typename T> requires gocpp::GoStruct<T>
    bool Array::operator==(const T& ref) const
    {
        if (len != ref.len) return false;
        if (elem != ref.elem) return false;
        return true;
    }

    std::ostream& Array::PrintTo(std::ostream& os) const
    {
        os << '{';
        os << "" << len;
        os << " " << elem;
        os << '}';
        return os;
    }

    std::ostream& operator<<(std::ostream& os, const struct Array& value)
    {
        return value.PrintTo(os);
    }

    // NewArray returns a new array type for the given element type and length.
    // A negative length indicates an unknown length.
    Array* NewArray(golang::types::Type elem, int64_t len)
    {
        return gocpp::InitPtr<Array>([=](auto& x) {
            x.len = len;
            x.elem = elem;
        });
    }

    // Len returns the length of array a.
    // A negative result indicates an unknown length.
    int64_t rec::Len(Array* a)
    {
        return a->len;
    }

    // Elem returns element type of array a.
    golang::types::Type rec::Elem(Array* a)
    {
        return a->elem;
    }

    golang::types::Type rec::Underlying(Array* a)
    {
        return a;
    }

    gocpp::string rec::String(Array* a)
    {
        return TypeString(a, nullptr);
    }

}

