// generated by GoCpp from file '$(ImportDir)/go/scanner/errors.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/go/scanner/errors.fwd.h"
#include "gocpp/support.h"


namespace golang::scanner
{
    struct GoTag_ErrorList { };
    using ErrorList = gocpp::defined<gocpp::slice<golang::scanner::Error*>, GoTag_ErrorList>;
}
#include "golang/go/token/position.h"
#include "golang/io/io.h"

namespace golang::scanner
{
    struct Error
    {
        token::Position Pos{};
        gocpp::string Msg{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct Error& value);
    void PrintError(io::Writer w, gocpp::error err);
}

#include "golang/go/token/position.h"

namespace golang::scanner
{

    namespace rec
    {
        gocpp::string Error(golang::scanner::Error e);
        void Add(ErrorList* p, token::Position pos, gocpp::string msg);
        void Reset(ErrorList* p);
        int Len(ErrorList p);
        void Swap(ErrorList p, int i, int j);
        bool Less(ErrorList p, int i, int j);
        void Sort(ErrorList p);
        void RemoveMultiples(ErrorList* p);
        gocpp::string Error(ErrorList p);
        gocpp::error Err(ErrorList p);
    }
}

