// generated by GoCpp from file '$(ImportDir)/go/types/objset.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "golang/go/types/objset.h"
#include "gocpp/support.h"

#include "golang/go/types/object.h"

namespace golang::types
{
    namespace rec
    {
    }

    // An objset is a set of objects identified by their unique id.
    // The zero value for objset is a ready-to-use empty objset.
    // // initialized lazily
    // insert attempts to insert an object obj into objset s.
    // If s already contains an alternative object alt with
    // the same name, insert leaves s unchanged and returns alt.
    // Otherwise it inserts obj and returns nil.
    Object rec::insert(objset* s, Object obj)
    {
        auto id = rec::Id(gocpp::recv(obj));
        if(auto alt = (*s)[id]; alt != nullptr)
        {
            return alt;
        }
        if(*s == nullptr)
        {
            *s = gocpp::make(gocpp::Tag<gocpp::map<gocpp::string, Object>>());
        }
        (*s)[id] = obj;
        return nullptr;
    }

}

