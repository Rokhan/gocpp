// generated by GoCpp from file '$(ImportDir)/golang.org/x/tools/internal/gcimporter/iexport.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/golang.org/x/tools/internal/gcimporter/iexport.fwd.h"
#include "gocpp/support.h"


namespace golang::gcimporter
{
    gocpp::string tparamName(gocpp::string exportName);
    void assert(bool cond);
    gocpp::error internalErrorf(gocpp::string format, gocpp::slice<gocpp::go_any> args);
    
    template<typename... Args>
    gocpp::error internalErrorf(gocpp::string format, Args... args)
    {
        return internalErrorf(format, gocpp::ToSlice<gocpp::go_any>(args...));
    }
    
    template<typename... Args>
    gocpp::error internalErrorf(gocpp::string format, gocpp::go_any value, Args... args)
    {
        return internalErrorf(format, gocpp::ToSlice<gocpp::go_any>(value, args...));
    }
}
#include "golang/bytes/buffer.h"
#include "golang/go/constant/value.h"
#include "golang/go/types/object.h"
#include "golang/golang.org/x/tools/internal/gcimporter/iimport.h"
#include "golang/io/io.h"
#include "golang/go/token/position.fwd.h"
#include "golang/go/types/package.fwd.h"
#include "golang/go/types/typeparam.fwd.h"
#include "golang/math/big/float.fwd.h"
#include "golang/math/big/rat.fwd.h"

namespace golang::gcimporter
{
    std::tuple<gocpp::slice<unsigned char>, gocpp::error> IExportShallow(token::FileSet* fset, types::Package* pkg, ReportFunc reportf);
    std::tuple<types::Package*, gocpp::error> IImportShallow(token::FileSet* fset, GetPackagesFunc getPackages, gocpp::slice<unsigned char> data, gocpp::string path, ReportFunc reportf);
    gocpp::error IExportData(io::Writer out, token::FileSet* fset, types::Package* pkg);
    gocpp::error IExportBundle(io::Writer out, token::FileSet* fset, gocpp::slice<types::Package*> pkgs);
    gocpp::error iexportCommon(io::Writer out, token::FileSet* fset, bool bundle, bool shallow, int version, gocpp::slice<types::Package*> pkgs);
    struct filePositions
    {
        token::File* file{};
        gocpp::slice<uint64_t> needed{}; // unordered list of needed file offsets

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct filePositions& value);
    gocpp::string tparamExportName(gocpp::string prefix, types::TypeParam* tparam);
    big::Float* constantToFloat(constant::Value x);
    big::Rat* valueToRat(constant::Value x);
    struct intWriter
    {
        bytes::Buffer Buffer{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct intWriter& value);
    struct objQueue
    {
        gocpp::slice<types::Object> ring{};
        int head{};
        int tail{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct objQueue& value);
    struct exportWriter
    {
        iexporter* p{};
        intWriter data{};
        gocpp::string prevFile{};
        int64_t prevLine{};
        int64_t prevColumn{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct exportWriter& value);
}
#include "golang/go/types/type.h"
#include "golang/golang.org/x/tools/go/types/objectpath/objectpath.fwd.h"

namespace golang::gcimporter
{
    struct iexporter
    {
        token::FileSet* fset{};
        bytes::Buffer* out{};
        int version{};
        bool shallow{}; // don't put types from other packages in the index
        objectpath::Encoder* objEncoder{}; // encodes objects from other packages in shallow mode; lazily allocated
        types::Package* localpkg{}; // (nil in bundle mode)
        // allPkgs tracks all packages that have been referenced by
        // the export data, so we can ensure to include them in the
        // main index.
        gocpp::map<types::Package*, bool> allPkgs{};
        objQueue declTodo{};
        intWriter strings{};
        gocpp::map<gocpp::string, uint64_t> stringIndex{};
        // In shallow mode, object positions are encoded as (file, offset).
        // Each file is recorded as a line-number table.
        // Only the lines of needed positions are saved faithfully.
        gocpp::map<token::File*, uint64_t> fileInfo{}; // value is index in fileInfos
        gocpp::slice<filePositions*> fileInfos{};
        intWriter data0{};
        gocpp::map<types::Object, uint64_t> declIndex{};
        gocpp::map<types::Object, gocpp::string> tparamNames{}; // typeparam->exported name
        gocpp::map<types::Type, uint64_t> typIndex{};
        int indent{}; // for tracing support

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct iexporter& value);
}

#include "golang/go/constant/value.h"
#include "golang/go/token/position.h"
#include "golang/go/types/object.h"
#include "golang/go/types/package.h"
#include "golang/go/types/signature.h"
#include "golang/go/types/tuple.h"
#include "golang/go/types/type.h"
#include "golang/go/types/typelists.h"
#include "golang/golang.org/x/tools/go/types/objectpath/objectpath.h"
#include "golang/golang.org/x/tools/internal/gcimporter/iimport.h"
#include "golang/math/big/float.h"
#include "golang/math/big/int.h"

namespace golang::gcimporter
{

    namespace rec
    {
        void encodeFile(iexporter* p, intWriter* w, token::File* file, gocpp::slice<uint64_t> needed);
        void writeIndex(exportWriter* w, gocpp::map<types::Object, uint64_t> index);
        gocpp::string exportName(iexporter* p, types::Object obj);
        void trace(iexporter* p, gocpp::string format, gocpp::slice<gocpp::go_any> args);
        
        template<typename... Args>
        void trace(iexporter* p, gocpp::string format, Args... args)
        {
            return trace(p, format, gocpp::ToSlice<gocpp::go_any>(args...));
        }
        
        template<typename... Args>
        void trace(iexporter* p, gocpp::string format, gocpp::go_any value, Args... args)
        {
            return trace(p, format, gocpp::ToSlice<gocpp::go_any>(value, args...));
        }
        objectpath::Encoder* objectpathEncoder(iexporter* p);
        uint64_t stringOff(iexporter* p, gocpp::string s);
        std::tuple<uint64_t, uint64_t> fileIndexAndOffset(iexporter* p, token::File* file, token::Pos pos);
        void pushDecl(iexporter* p, types::Object obj);
        gocpp::string exportPath(exportWriter* w, types::Package* pkg);
        void doDecl(iexporter* p, types::Object obj);
        void tag(exportWriter* w, unsigned char tag);
        void pos(exportWriter* w, token::Pos pos);
        void posV2(exportWriter* w, token::Pos pos);
        void posV1(exportWriter* w, token::Pos pos);
        void posV0(exportWriter* w, token::Pos pos);
        void pkg(exportWriter* w, types::Package* pkg);
        void qualifiedType(exportWriter* w, types::TypeName* obj);
        void typ(exportWriter* w, types::Type t, types::Package* pkg);
        exportWriter* newWriter(iexporter* p);
        uint64_t flush(exportWriter* w);
        uint64_t typOff(iexporter* p, types::Type t, types::Package* pkg);
        void startType(exportWriter* w, itag k);
        void doTyp(exportWriter* w, types::Type t, types::Package* pkg);
        void objectPath(exportWriter* w, types::Object obj);
        void signature(exportWriter* w, types::Signature* sig);
        void typeList(exportWriter* w, types::TypeList* ts, types::Package* pkg);
        void tparamList(exportWriter* w, gocpp::string prefix, types::TypeParamList* list, types::Package* pkg);
        void paramList(exportWriter* w, types::Tuple* tup);
        void param(exportWriter* w, types::Object obj);
        void value(exportWriter* w, types::Type typ, constant::Value v);
        void mpint(exportWriter* w, big::Int* x, types::Type typ);
        void mpfloat(exportWriter* w, big::Float* f, types::Type typ);
        bool go_bool(exportWriter* w, bool b);
        void int64(exportWriter* w, int64_t x);
        void uint64(exportWriter* w, uint64_t x);
        void string(exportWriter* w, gocpp::string s);
        void localIdent(exportWriter* w, types::Object obj);
        void int64(intWriter* w, int64_t x);
        void uint64(intWriter* w, uint64_t x);
        bool empty(objQueue* q);
        void pushTail(objQueue* q, types::Object obj);
        types::Object popHead(objQueue* q);
        gocpp::string Error(internalError e);
    }
}

