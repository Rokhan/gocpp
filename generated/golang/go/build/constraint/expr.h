// generated by GoCpp from file '$(ImportDir)/go/build/constraint/expr.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/go/build/constraint/expr.fwd.h"
#include "gocpp/support.h"


namespace golang::constraint
{
    struct Expr : virtual gocpp::Interface
    {
        using gocpp::Interface::operator==;
        using gocpp::Interface::operator!=;

        Expr(){}
        Expr(Expr& i) = default;
        Expr(const Expr& i) = default;
        Expr& operator=(Expr& i) = default;
        Expr& operator=(const Expr& i) = default;

        inline Expr(nullptr_t) {};
        Expr& operator=(nullptr_t) { mValue.reset(); }

        template<typename T>
        Expr(T& ref);

        template<typename T>
        Expr(const T& ref);

        template<typename T>
        Expr(T* ptr);

        using isGoInterface = void;

        std::ostream& PrintTo(std::ostream& os) const;

        struct IExpr
        {
            // String returns the string form of the expression,
            // using the boolean syntax used in //go:build lines.
            virtual gocpp::string vString() = 0;
            // Eval reports whether the expression evaluates to true.
            // It calls ok(tag) as needed to find out whether a given build tag
            // is satisfied by the current build configuration.
            virtual bool vEval(std::function<bool (gocpp::string tag)> ok) = 0;
            // The presence of an isExpr method explicitly marks the type as an Expr.
            // Only implementations in this package should be used as Exprs.
            virtual void visExpr() = 0;
            virtual void* getPtr() = 0;
        };

        template<typename T, typename TStore, typename TInterface = IExpr>
        struct ExprImpl : virtual TInterface
        {
            explicit ExprImpl(T* ptr)
            {
                value.reset(ptr);
            }

            gocpp::string vString() override;

            bool vEval(std::function<bool (gocpp::string tag)> ok) override;

            void visExpr() override;

            void* getPtr() override
            {
                return value.get();
            }

            TStore value;
        };

        inline IExpr* value() const;

        std::shared_ptr<IExpr> mValue;
    };

    namespace rec
    {
        gocpp::string String(const gocpp::PtrRecv<struct Expr, false>& self);
        gocpp::string String(const gocpp::ObjRecv<struct Expr>& self);

        bool Eval(const gocpp::PtrRecv<struct Expr, false>& self, std::function<bool (gocpp::string tag)> ok);
        bool Eval(const gocpp::ObjRecv<struct Expr>& self, std::function<bool (gocpp::string tag)> ok);

        void isExpr(const gocpp::PtrRecv<struct Expr, false>& self);
        void isExpr(const gocpp::ObjRecv<struct Expr>& self);
    }

    std::ostream& operator<<(std::ostream& os, const struct Expr& value);
    struct TagExpr
    {
        gocpp::string Tag{}; // for example, “linux” or “cgo”

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct TagExpr& value);
    struct SyntaxError
    {
        int Offset{}; // byte offset in input where error was detected
        gocpp::string Err{}; // description of error

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct SyntaxError& value);
    bool IsGoBuild(gocpp::string line);
    std::tuple<gocpp::string, bool> splitGoBuild(gocpp::string line);
    struct exprParser
    {
        gocpp::string s{}; // input string
        int i{}; // next read location in s
        gocpp::string tok{}; // last token read
        bool isTag{};
        int pos{}; // position (start) of last token

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct exprParser& value);
    bool IsPlusBuild(gocpp::string line);
    std::tuple<gocpp::string, bool> splitPlusBuild(gocpp::string line);
    bool isValidTag(gocpp::string word);
    Expr tag(gocpp::string tag);
    struct NotExpr
    {
        Expr X{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct NotExpr& value);
    Expr not(Expr x);
    struct AndExpr
    {
        Expr X{};
        Expr Y{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct AndExpr& value);
    gocpp::string andArg(Expr x);
    Expr and(Expr x, Expr y);
    struct OrExpr
    {
        Expr X{};
        Expr Y{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct OrExpr& value);
    gocpp::string orArg(Expr x);
    Expr or(Expr x, Expr y);
    std::tuple<Expr, gocpp::error> Parse(gocpp::string line);
    std::tuple<Expr, gocpp::error> parseExpr(gocpp::string text);
    Expr parsePlusBuildExpr(gocpp::string text);
    std::tuple<gocpp::slice<gocpp::string>, gocpp::error> PlusBuildLines(Expr x);
    Expr pushNot(Expr x, bool not);
    gocpp::slice<Expr> appendSplitAnd(gocpp::slice<Expr> list, Expr x);
    gocpp::slice<Expr> appendSplitOr(gocpp::slice<Expr> list, Expr x);
}
#include "golang/errors/errors.fwd.h"

namespace golang::constraint
{
    extern gocpp::error errNotConstraint;
    extern gocpp::error errComplex;

    namespace rec
    {
        void isExpr(TagExpr* x);
        bool Eval(TagExpr* x, std::function<bool (gocpp::string tag)> ok);
        gocpp::string String(TagExpr* x);
        void isExpr(NotExpr* x);
        bool Eval(NotExpr* x, std::function<bool (gocpp::string tag)> ok);
        gocpp::string String(NotExpr* x);
        void isExpr(AndExpr* x);
        bool Eval(AndExpr* x, std::function<bool (gocpp::string tag)> ok);
        gocpp::string String(AndExpr* x);
        void isExpr(OrExpr* x);
        bool Eval(OrExpr* x, std::function<bool (gocpp::string tag)> ok);
        gocpp::string String(OrExpr* x);
        gocpp::string Error(SyntaxError* e);
        Expr or(exprParser* p);
        Expr and(exprParser* p);
        Expr not(exprParser* p);
        Expr atom(exprParser* p);
        void lex(exprParser* p);
    }
}

