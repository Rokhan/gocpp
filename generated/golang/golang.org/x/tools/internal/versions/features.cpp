// generated by GoCpp from file '$(ImportDir)/golang.org/x/tools/internal/versions/features.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "golang/golang.org/x/tools/internal/versions/features.h"
#include "gocpp/support.h"

#include "golang/golang.org/x/tools/internal/versions/versions.h"

namespace golang::versions
{
    namespace rec
    {
    }

    // AtLeast reports whether the file version v comes after a Go release.
    //
    // Use this predicate to enable a behavior once a certain Go release
    // has happened (and stays enabled in the future).
    bool AtLeast(gocpp::string v, gocpp::string release)
    {
        if(v == Future)
        {
            // an unknown future version is always after y.
            return true;
        }
        return Compare(Lang(v), Lang(release)) >= 0;
    }

    // Before reports whether the file version v is strictly before a Go release.
    //
    // Use this predicate to disable a behavior once a certain Go release
    // has happened (and stays enabled in the future).
    bool Before(gocpp::string v, gocpp::string release)
    {
        if(v == Future)
        {
            // an unknown future version happens after y.
            return false;
        }
        return Compare(Lang(v), Lang(release)) < 0;
    }

}

