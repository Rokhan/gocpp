// generated by GoCpp from file '$(ImportDir)/go/types/tuple.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/go/types/tuple.fwd.h"
#include "gocpp/support.h"

#include "golang/go/types/object.fwd.h"

namespace golang::types
{
    struct Tuple
    {
        gocpp::slice<Var*> vars{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct Tuple& value);
    Tuple* NewTuple(gocpp::slice<Var*> x);
    
    template<typename... Args>
    Tuple* NewTuple(Args... x)
    {
        return NewTuple(gocpp::ToSlice<Var*>(x...));
    }
    
    template<typename... Args>
    Tuple* NewTuple(Var* value, Args... x)
    {
        return NewTuple(gocpp::ToSlice<Var*>(value, x...));
    }
}

#include "golang/go/types/object.h"
#include "golang/go/types/type.h"

namespace golang::types
{

    namespace rec
    {
        int Len(Tuple* t);
        Var* At(Tuple* t, int i);
        golang::types::Type Underlying(Tuple* t);
        gocpp::string String(Tuple* t);
    }
}

