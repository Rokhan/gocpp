// generated by GoCpp from file '$(ImportDir)/go/ast/scope.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/go/ast/scope.fwd.h"
#include "gocpp/support.h"


namespace golang::ast
{
    struct Scope
    {
        Scope* Outer{};
        gocpp::map<gocpp::string, Object*> Objects{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct Scope& value);
    struct Object
    {
        ObjKind Kind{};
        gocpp::string Name{}; // declared name
        go_any Decl{}; // corresponding Field, XxxSpec, FuncDecl, LabeledStmt, AssignStmt, Scope; or nil
        go_any Data{}; // object-specific data; or nil
        go_any Type{}; // placeholder for type information; may be nil

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct Object& value);
    extern gocpp::array<gocpp::string, 7> objKindStrings;
    Scope* NewScope(Scope* outer);
    Object* NewObj(ObjKind kind, gocpp::string name);
}

#include "golang/go/token/position.h"

namespace golang::ast
{

    namespace rec
    {
        Object* Lookup(Scope* s, gocpp::string name);
        Object* Insert(Scope* s, Object* obj);
        gocpp::string String(Scope* s);
        token::Pos Pos(Object* obj);
        gocpp::string String(ObjKind kind);
    }
}

