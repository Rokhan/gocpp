// generated by GoCpp from file '$(ImportDir)/io/fs/stat.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/io/fs/stat.fwd.h"
#include "gocpp/support.h"

#include "golang/io/fs/fs.h"

namespace golang::fs
{
    struct StatFS : virtual gocpp::Interface, FS
    {
        using gocpp::Interface::operator==;
        using gocpp::Interface::operator!=;

        StatFS(){}
        StatFS(StatFS& i) = default;
        StatFS(const StatFS& i) = default;
        StatFS& operator=(StatFS& i) = default;
        StatFS& operator=(const StatFS& i) = default;

        inline StatFS(nullptr_t) {};
        StatFS& operator=(nullptr_t) { mValue.reset(); }

        template<typename T>
        StatFS(T& ref);

        template<typename T>
        StatFS(const T& ref);

        template<typename T>
        StatFS(T* ptr);

        using isGoInterface = void;

        std::ostream& PrintTo(std::ostream& os) const;

        struct IStatFS: virtual FS::IFS
        {
            // Stat returns a FileInfo describing the file.
            // If there is an error, it should be of type *PathError.
            virtual std::tuple<FileInfo, gocpp::error> vStat(gocpp::string name) = 0;
            virtual void* getPtr() = 0;
        };

        template<typename T, typename TStore, typename TInterface = IStatFS>
        struct StatFSImpl : virtual TInterface, virtual FS::FSImpl<T, TStore, TInterface>
        {
            explicit StatFSImpl(T* ptr): FS::FSImpl<T, TStore, TInterface>(ptr)
            {
                value.reset(ptr);
            }

            std::tuple<FileInfo, gocpp::error> vStat(gocpp::string name) override;

            void* getPtr() override
            {
                return value.get();
            }

            TStore value;
        };

        inline IStatFS* value() const;

        std::shared_ptr<IStatFS> mValue;
    };

    namespace rec
    {
        std::tuple<FileInfo, gocpp::error> Stat(const gocpp::PtrRecv<struct StatFS, false>& self, gocpp::string name);
        std::tuple<FileInfo, gocpp::error> Stat(const gocpp::ObjRecv<struct StatFS>& self, gocpp::string name);

        std::tuple<fs::File, gocpp::error> Open(const gocpp::PtrRecv<struct StatFS, false>& self, gocpp::string name);
        std::tuple<fs::File, gocpp::error> Open(const gocpp::ObjRecv<struct StatFS>& self, gocpp::string name);
    }

    std::ostream& operator<<(std::ostream& os, const struct StatFS& value);
    std::tuple<FileInfo, gocpp::error> Stat(FS fsys, gocpp::string name);

    namespace rec
    {
    }
}

