// generated by GoCpp from file '$(ImportDir)/golang.org/x/tools/internal/pkgbits/frames_go17.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "golang/golang.org/x/tools/internal/pkgbits/frames_go17.h"
#include "gocpp/support.h"

#include "golang/golang.org/x/tools/internal/pkgbits/sync.h"
#include "golang/runtime/symtab.h"

namespace golang::pkgbits
{
    namespace rec
    {
        using runtime::rec::Next;
    }

    // walkFrames calls visit for each call frame represented by pcs.
    //
    // pcs should be a slice of PCs, as returned by runtime.Callers.
    void walkFrames(gocpp::slice<uintptr_t> pcs, frameVisitor visit)
    {
        if(len(pcs) == 0)
        {
            return;
        }

        auto frames = runtime::CallersFrames(pcs);
        for(; ; )
        {
            auto [frame, more] = rec::Next(gocpp::recv(frames));
            visit(frame.File, frame.Line, frame.Function, frame.PC - frame.Entry);
            if(! more)
            {
                return;
            }
        }
    }

}

