// generated by GoCpp from file '$(ImportDir)/golang.org/x/tools/internal/pkgbits/sync.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "golang/golang.org/x/tools/internal/pkgbits/sync.h"
#include "gocpp/support.h"

#include "golang/fmt/print.h"
#include "golang/golang.org/x/tools/internal/pkgbits/frames_go17.h"
#include "golang/strings/strings.h"

namespace golang::pkgbits
{
    namespace rec
    {
    }

    // fmtFrames formats a backtrace for reporting reader/writer desyncs.
    gocpp::slice<gocpp::string> fmtFrames(gocpp::slice<uintptr_t> pcs)
    {
        auto res = gocpp::make(gocpp::Tag<gocpp::slice<gocpp::string>>(), 0, len(pcs));
        walkFrames(pcs, [=](gocpp::string file, int line, gocpp::string name, uintptr_t offset) mutable -> void
        {
            // Trim package from function name. It's just redundant noise.
            name = strings::TrimPrefix(name, "cmd/compile/internal/noder."_s);

            res = append(res, mocklib::Sprintf("%s:%v: %s +0x%v"_s, file, line, name, offset));
        });
        return res;
    }

    // SyncMarker is an enum type that represents markers that may be
    // written to export data to ensure the reader and writer stay
    // synchronized.
}

