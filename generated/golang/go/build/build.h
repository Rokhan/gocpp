// generated by GoCpp from file '$(ImportDir)/go/build/build.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/go/build/build.fwd.h"
#include "gocpp/support.h"


namespace golang::build
{
    std::tuple<gocpp::string, bool> hasSubdir(gocpp::string root, gocpp::string dir);
    gocpp::string defaultGOPATH();
    extern gocpp::slice<gocpp::string> defaultToolTags;
    extern gocpp::slice<gocpp::string> defaultReleaseTags;
    gocpp::string envOr(gocpp::string name, gocpp::string def);
    struct NoGoError
    {
        gocpp::string Dir{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct NoGoError& value);
    struct MultiplePackageError
    {
        gocpp::string Dir{}; // directory containing files
        gocpp::slice<gocpp::string> Packages{}; // package names found
        gocpp::slice<gocpp::string> Files{}; // corresponding files: Files[i] declares package Packages[i]

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct MultiplePackageError& value);
    gocpp::string nameExt(gocpp::string name);
    gocpp::slice<gocpp::string> uniq(gocpp::slice<gocpp::string> list);
    bool equal(gocpp::slice<gocpp::string> x, gocpp::slice<gocpp::string> y);
    std::tuple<gocpp::string, int> findImportComment(gocpp::slice<unsigned char> data);
    extern gocpp::slice<unsigned char> slashSlash;
    extern gocpp::slice<unsigned char> slashStar;
    extern gocpp::slice<unsigned char> starSlash;
    extern gocpp::slice<unsigned char> newline;
    gocpp::slice<unsigned char> skipSpaceOrComment(gocpp::slice<unsigned char> data);
    std::tuple<gocpp::slice<unsigned char>, gocpp::slice<unsigned char>> parseWord(gocpp::slice<unsigned char> data);
    extern gocpp::slice<unsigned char> plusBuild;
    extern gocpp::slice<unsigned char> goBuildComment;
    bool isGoBuildComment(gocpp::slice<unsigned char> line);
    extern gocpp::slice<unsigned char> binaryOnlyComment;
    std::tuple<gocpp::slice<unsigned char>, gocpp::slice<unsigned char>, bool, gocpp::error> parseFileHeader(gocpp::slice<unsigned char> content);
    std::tuple<gocpp::string, bool> expandSrcDir(gocpp::string str, gocpp::string srcdir);
    bool safeCgoName(gocpp::string s);
    std::tuple<gocpp::slice<gocpp::string>, gocpp::error> splitQuoted(gocpp::string s);
    bool IsLocalImport(gocpp::string path);
    std::tuple<gocpp::string, gocpp::error> ArchChar(gocpp::string goarch);
}
#include "golang/go/token/position.h"
#include "golang/io/fs/fs.h"
#include "golang/io/io.h"
#include "golang/errors/errors.fwd.h"
#include "golang/go/ast/ast.fwd.h"
#include "golang/go/build/gc.fwd.h"
#include "golang/internal/godebug/godebug.fwd.h"

namespace golang::build
{
    struct Context
    {
        gocpp::string GOARCH{}; // target architecture
        gocpp::string GOOS{}; // target operating system
        gocpp::string GOROOT{}; // Go root
        gocpp::string GOPATH{}; // Go paths
        // Dir is the caller's working directory, or the empty string to use
        // the current directory of the running process. In module mode, this is used
        // to locate the main module.
        // If Dir is non-empty, directories passed to Import and ImportDir must
        // be absolute.
        gocpp::string Dir{};
        bool CgoEnabled{}; // whether cgo files are included
        bool UseAllFiles{}; // use files regardless of go:build lines, file names
        gocpp::string Compiler{}; // compiler to assume when computing target paths
        // The build, tool, and release tags specify build constraints
        // that should be considered satisfied when processing go:build lines.
        // Clients creating a new context may customize BuildTags, which
        // defaults to empty, but it is usually an error to customize ToolTags or ReleaseTags.
        // ToolTags defaults to build tags appropriate to the current Go toolchain configuration.
        // ReleaseTags defaults to the list of Go releases the current release is compatible with.
        // BuildTags is not set for the Default build Context.
        // In addition to the BuildTags, ToolTags, and ReleaseTags, build constraints
        // consider the values of GOARCH and GOOS as satisfied tags.
        // The last element in ReleaseTags is assumed to be the current release.
        gocpp::slice<gocpp::string> BuildTags{};
        gocpp::slice<gocpp::string> ToolTags{};
        gocpp::slice<gocpp::string> ReleaseTags{};
        // The install suffix specifies a suffix to use in the name of the installation
        // directory. By default it is empty, but custom builds that need to keep
        // their outputs separate can set InstallSuffix to do so. For example, when
        // using the race detector, the go command uses InstallSuffix = "race", so
        // that on a Linux/386 system, packages are written to a directory named
        // "linux_386_race" instead of the usual "linux_386".
        gocpp::string InstallSuffix{};
        // JoinPath joins the sequence of path fragments into a single path.
        // If JoinPath is nil, Import uses filepath.Join.
        std::function<gocpp::string (gocpp::slice<gocpp::string> elem)> JoinPath{};
        // SplitPathList splits the path list into a slice of individual paths.
        // If SplitPathList is nil, Import uses filepath.SplitList.
        std::function<gocpp::slice<gocpp::string> (gocpp::string list)> SplitPathList{};
        // IsAbsPath reports whether path is an absolute path.
        // If IsAbsPath is nil, Import uses filepath.IsAbs.
        std::function<bool (gocpp::string path)> IsAbsPath{};
        // IsDir reports whether the path names a directory.
        // If IsDir is nil, Import calls os.Stat and uses the result's IsDir method.
        std::function<bool (gocpp::string path)> IsDir{};
        // HasSubdir reports whether dir is lexically a subdirectory of
        // root, perhaps multiple levels below. It does not try to check
        // whether dir exists.
        // If so, HasSubdir sets rel to a slash-separated path that
        // can be joined to root to produce a path equivalent to dir.
        // If HasSubdir is nil, Import uses an implementation built on
        // filepath.EvalSymlinks.
        std::function<std::tuple<gocpp::string, bool> (gocpp::string root, gocpp::string dir)> HasSubdir{};
        // ReadDir returns a slice of fs.FileInfo, sorted by Name,
        // describing the content of the named directory.
        // If ReadDir is nil, Import uses os.ReadDir.
        std::function<std::tuple<gocpp::slice<fs::FileInfo>, gocpp::error> (gocpp::string dir)> ReadDir{};
        // OpenFile opens a file (not a directory) for reading.
        // If OpenFile is nil, Import uses os.Open.
        std::function<std::tuple<io::ReadCloser, gocpp::error> (gocpp::string path)> OpenFile{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct Context& value);
    struct Directive
    {
        gocpp::string Text{}; // full line comment including leading slashes
        token::Position Pos{}; // position of comment

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct Directive& value);
    extern godebug::Setting* installgoroot;
    extern gocpp::error errNoModules;
    struct fileImport
    {
        gocpp::string path{};
        token::Pos pos{};
        ast::CommentGroup* doc{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct fileImport& value);
    struct fileEmbed
    {
        gocpp::string pattern{};
        token::Position pos{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct fileEmbed& value);
    std::tuple<gocpp::slice<gocpp::string>, gocpp::map<gocpp::string, gocpp::slice<token::Position>>> cleanDecls(gocpp::map<gocpp::string, gocpp::slice<token::Position>> m);
    extern gocpp::error errMultipleGoBuild;
    extern gocpp::string ToolDir;
    Context defaultContext();
    struct Package
    {
        gocpp::string Dir{}; // directory containing package sources
        gocpp::string Name{}; // package name
        gocpp::string ImportComment{}; // path in import comment on package statement
        gocpp::string Doc{}; // documentation synopsis
        gocpp::string ImportPath{}; // import path of package ("" if unknown)
        gocpp::string Root{}; // root of Go tree where this package lives
        gocpp::string SrcRoot{}; // package source root directory ("" if unknown)
        gocpp::string PkgRoot{}; // package install root directory ("" if unknown)
        gocpp::string PkgTargetRoot{}; // architecture dependent install root directory ("" if unknown)
        gocpp::string BinDir{}; // command install directory ("" if unknown)
        bool Goroot{}; // package found in Go root
        gocpp::string PkgObj{}; // installed .a file
        gocpp::slice<gocpp::string> AllTags{}; // tags that can influence file selection in this directory
        gocpp::string ConflictDir{}; // this directory shadows Dir in $GOPATH
        bool BinaryOnly{}; // cannot be rebuilt from source (has //go:binary-only-package comment)
        // Source files
        gocpp::slice<gocpp::string> GoFiles{}; // .go source files (excluding CgoFiles, TestGoFiles, XTestGoFiles)
        gocpp::slice<gocpp::string> CgoFiles{}; // .go source files that import "C"
        gocpp::slice<gocpp::string> IgnoredGoFiles{}; // .go source files ignored for this build (including ignored _test.go files)
        gocpp::slice<gocpp::string> InvalidGoFiles{}; // .go source files with detected problems (parse error, wrong package name, and so on)
        gocpp::slice<gocpp::string> IgnoredOtherFiles{}; // non-.go source files ignored for this build
        gocpp::slice<gocpp::string> CFiles{}; // .c source files
        gocpp::slice<gocpp::string> CXXFiles{}; // .cc, .cpp and .cxx source files
        gocpp::slice<gocpp::string> MFiles{}; // .m (Objective-C) source files
        gocpp::slice<gocpp::string> HFiles{}; // .h, .hh, .hpp and .hxx source files
        gocpp::slice<gocpp::string> FFiles{}; // .f, .F, .for and .f90 Fortran source files
        gocpp::slice<gocpp::string> SFiles{}; // .s source files
        gocpp::slice<gocpp::string> SwigFiles{}; // .swig files
        gocpp::slice<gocpp::string> SwigCXXFiles{}; // .swigcxx files
        gocpp::slice<gocpp::string> SysoFiles{}; // .syso system object files to add to archive
        // Cgo directives
        gocpp::slice<gocpp::string> CgoCFLAGS{}; // Cgo CFLAGS directives
        gocpp::slice<gocpp::string> CgoCPPFLAGS{}; // Cgo CPPFLAGS directives
        gocpp::slice<gocpp::string> CgoCXXFLAGS{}; // Cgo CXXFLAGS directives
        gocpp::slice<gocpp::string> CgoFFLAGS{}; // Cgo FFLAGS directives
        gocpp::slice<gocpp::string> CgoLDFLAGS{}; // Cgo LDFLAGS directives
        gocpp::slice<gocpp::string> CgoPkgConfig{}; // Cgo pkg-config directives
        // Test information
        gocpp::slice<gocpp::string> TestGoFiles{}; // _test.go files in package
        gocpp::slice<gocpp::string> XTestGoFiles{}; // _test.go files outside package
        // Go directive comments (//go:zzz...) found in source files.
        gocpp::slice<Directive> Directives{};
        gocpp::slice<Directive> TestDirectives{};
        gocpp::slice<Directive> XTestDirectives{};
        // Dependency information
        gocpp::slice<gocpp::string> Imports{}; // import paths from GoFiles, CgoFiles
        gocpp::map<gocpp::string, gocpp::slice<token::Position>> ImportPos{}; // line information for Imports
        gocpp::slice<gocpp::string> TestImports{}; // import paths from TestGoFiles
        gocpp::map<gocpp::string, gocpp::slice<token::Position>> TestImportPos{}; // line information for TestImports
        gocpp::slice<gocpp::string> XTestImports{}; // import paths from XTestGoFiles
        gocpp::map<gocpp::string, gocpp::slice<token::Position>> XTestImportPos{}; // line information for XTestImports
        // //go:embed patterns found in Go source files
        // For example, if a source file says
        // //go:embed a* b.c
        // then the list will contain those two strings as separate entries.
        // (See package embed for more details about //go:embed.)
        gocpp::slice<gocpp::string> EmbedPatterns{}; // patterns from GoFiles, CgoFiles
        gocpp::map<gocpp::string, gocpp::slice<token::Position>> EmbedPatternPos{}; // line information for EmbedPatterns
        gocpp::slice<gocpp::string> TestEmbedPatterns{}; // patterns from TestGoFiles
        gocpp::map<gocpp::string, gocpp::slice<token::Position>> TestEmbedPatternPos{}; // line information for TestEmbedPatterns
        gocpp::slice<gocpp::string> XTestEmbedPatterns{}; // patterns from XTestGoFiles
        gocpp::map<gocpp::string, gocpp::slice<token::Position>> XTestEmbedPatternPos{}; // line information for XTestEmbedPatternPos

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct Package& value);
    bool hasGoFiles(Context* ctxt, gocpp::string dir);
    struct fileInfo
    {
        gocpp::string name{}; // full name including dir
        gocpp::slice<unsigned char> header{};
        token::FileSet* fset{};
        ast::File* parsed{};
        gocpp::error parseErr{};
        gocpp::slice<fileImport> imports{};
        gocpp::slice<fileEmbed> embeds{};
        gocpp::slice<Directive> directives{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct fileInfo& value);
    extern Context Default;
    gocpp::slice<gocpp::string>* fileListForExt(Package* p, gocpp::string ext);
    extern Package dummyPkg;
    std::tuple<Package*, gocpp::error> Import(gocpp::string path, gocpp::string srcDir, ImportMode mode);
    std::tuple<Package*, gocpp::error> ImportDir(gocpp::string dir, ImportMode mode);
}

#include "golang/go/ast/ast.h"
#include "golang/go/build/constraint/expr.h"
#include "golang/go/token/position.h"
#include "golang/io/fs/fs.h"
#include "golang/io/io.h"

namespace golang::build
{

    namespace rec
    {
        gocpp::string joinPath(Context* ctxt, gocpp::slice<gocpp::string> elem);
        
        template<typename... Args>
        gocpp::string joinPath(Context* ctxt, Args... elem)
        {
            return joinPath(ctxt, gocpp::ToSlice<gocpp::string>(elem...));
        }
        
        template<typename... Args>
        gocpp::string joinPath(Context* ctxt, gocpp::string value, Args... elem)
        {
            return joinPath(ctxt, gocpp::ToSlice<gocpp::string>(value, elem...));
        }
        gocpp::slice<gocpp::string> splitPathList(Context* ctxt, gocpp::string s);
        bool isAbsPath(Context* ctxt, gocpp::string path);
        bool isDir(Context* ctxt, gocpp::string path);
        std::tuple<gocpp::string, bool> hasSubdir(Context* ctxt, gocpp::string root, gocpp::string dir);
        std::tuple<gocpp::slice<fs::DirEntry>, gocpp::error> readDir(Context* ctxt, gocpp::string path);
        std::tuple<io::ReadCloser, gocpp::error> openFile(Context* ctxt, gocpp::string path);
        bool isFile(Context* ctxt, gocpp::string path);
        gocpp::slice<gocpp::string> gopath(Context* ctxt);
        gocpp::slice<gocpp::string> SrcDirs(Context* ctxt);
        bool IsCommand(Package* p);
        std::tuple<Package*, gocpp::error> ImportDir(Context* ctxt, gocpp::string dir, ImportMode mode);
        gocpp::string Error(NoGoError* e);
        gocpp::string Error(MultiplePackageError* e);
        std::tuple<Package*, gocpp::error> Import(Context* ctxt, gocpp::string path, gocpp::string srcDir, ImportMode mode);
        gocpp::error importGo(Context* ctxt, Package* p, gocpp::string path, gocpp::string srcDir, ImportMode mode);
        std::tuple<bool, gocpp::error> MatchFile(Context* ctxt, gocpp::string dir, gocpp::string name);
        std::tuple<fileInfo*, gocpp::error> matchFile(Context* ctxt, gocpp::string dir, gocpp::string name, gocpp::map<gocpp::string, bool> allTags, bool* binaryOnly, token::FileSet* fset);
        std::tuple<bool, bool, gocpp::error> shouldBuild(Context* ctxt, gocpp::slice<unsigned char> content, gocpp::map<gocpp::string, bool> allTags);
        gocpp::error saveCgo(Context* ctxt, gocpp::string filename, Package* di, ast::CommentGroup* cg);
        void makePathsAbsolute(Context* ctxt, gocpp::slice<gocpp::string> args, gocpp::string srcDir);
        bool matchAuto(Context* ctxt, gocpp::string text, gocpp::map<gocpp::string, bool> allTags);
        bool eval(Context* ctxt, constraint::Expr x, gocpp::map<gocpp::string, bool> allTags);
        bool matchTag(Context* ctxt, gocpp::string name, gocpp::map<gocpp::string, bool> allTags);
        bool goodOSArchFile(Context* ctxt, gocpp::string name, gocpp::map<gocpp::string, bool> allTags);
    }
}

