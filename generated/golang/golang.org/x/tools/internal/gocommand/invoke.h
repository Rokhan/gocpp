// generated by GoCpp from file '$(ImportDir)/golang.org/x/tools/internal/gocommand/invoke.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/golang.org/x/tools/internal/gocommand/invoke.fwd.h"
#include "gocpp/support.h"


namespace golang::gocommand
{
    struct gocpp_id_0
    {

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct gocpp_id_0& value);
    struct gocpp_id_1
    {

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct gocpp_id_1& value);
    struct Invocation
    {
        gocpp::string Verb{};
        gocpp::slice<gocpp::string> Args{};
        gocpp::slice<gocpp::string> BuildFlags{};
        // If ModFlag is set, the go command is invoked with -mod=ModFlag.
        // TODO(rfindley): remove, in favor of Args.
        gocpp::string ModFlag{};
        // If ModFile is set, the go command is invoked with -modfile=ModFile.
        // TODO(rfindley): remove, in favor of Args.
        gocpp::string ModFile{};
        // Overlay is the name of the JSON overlay file that describes
        // unsaved editor buffers; see [WriteOverlays].
        // If set, the go command is invoked with -overlay=Overlay.
        // TODO(rfindley): remove, in favor of Args.
        gocpp::string Overlay{};
        // If CleanEnv is set, the invocation will run only with the environment
        // in Env, not starting with os.Environ.
        bool CleanEnv{};
        gocpp::slice<gocpp::string> Env{};
        gocpp::string WorkingDir{};
        std::function<void (gocpp::string format, gocpp::slice<gocpp::go_any> args)> Logf{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct Invocation& value);
    extern bool DebugHangingGoCommands;
    std::tuple<gocpp::string, std::function<void ()>, gocpp::error> WriteOverlays(gocpp::map<gocpp::string, gocpp::slice<unsigned char>> overlay);
}
#include "golang/context/context.h"
#include "golang/golang.org/x/tools/internal/event/label/label.h"
#include "golang/sync/once.h"
#include "golang/time/time.h"
#include "golang/golang.org/x/tools/internal/event/keys/keys.fwd.h"
#include "golang/os/exec/exec.fwd.h"
#include "golang/regexp/regexp.fwd.h"

namespace golang::gocommand
{
    struct Runner
    {
        // once guards the runner initialization.
        sync::Once once{};
        // inFlight tracks available workers.
        gocpp::channel<gocpp_id_0> inFlight{};
        // serialized guards the ability to run a go command serially,
        // to avoid deadlocks when claiming workers.
        gocpp::channel<gocpp_id_1> serialized{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct Runner& value);
    extern regexp::Regexp* modConcurrencyError;
    extern keys::String* verb;
    extern keys::String* directory;
    gocpp::slice<label::Label> invLabels(Invocation inv);
    gocpp::error runCmdContext(context::Context ctx, exec::Cmd* cmd);
    void HandleHangingGoCommand(mocklib::Date start, exec::Cmd* cmd);
    gocpp::string cmdDebugStr(exec::Cmd* cmd);
}

#include "golang/bytes/buffer.h"
#include "golang/context/context.h"
#include "golang/io/io.h"

namespace golang::gocommand
{

    namespace rec
    {
        void initialize(Runner* runner);
        std::tuple<bytes::Buffer*, gocpp::error> Run(Runner* runner, context::Context ctx, Invocation inv);
        gocpp::error RunPiped(Runner* runner, context::Context ctx, Invocation inv, io::Writer go_stdout, io::Writer go_stderr);
        std::tuple<bytes::Buffer*, bytes::Buffer*, gocpp::error, gocpp::error> RunRaw(Runner* runner, context::Context ctx, Invocation inv);
        std::tuple<bytes::Buffer*, bytes::Buffer*, gocpp::error, gocpp::error> runConcurrent(Runner* runner, context::Context ctx, Invocation inv);
        std::tuple<gocpp::error, gocpp::error> runPiped(Runner* runner, context::Context ctx, Invocation inv, io::Writer go_stdout, io::Writer go_stderr);
        std::tuple<gocpp::error, gocpp::error> runWithFriendlyError(Invocation* i, context::Context ctx, io::Writer go_stdout, io::Writer go_stderr);
        void logf(Invocation* i, gocpp::string format, gocpp::slice<go_any> args);
        
        template<typename... Args>
        void logf(Invocation* i, gocpp::string format, Args... args)
        {
            return logf(i, format, gocpp::ToSlice<go_any>(args...));
        }
        
        template<typename... Args>
        void logf(Invocation* i, gocpp::string format, go_any value, Args... args)
        {
            return logf(i, format, gocpp::ToSlice<go_any>(value, args...));
        }
        gocpp::error run(Invocation* i, context::Context ctx, io::Writer go_stdout, io::Writer go_stderr);
    }
}

