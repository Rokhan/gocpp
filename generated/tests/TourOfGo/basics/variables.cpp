// generated by GoCpp from file 'tests/TourOfGo/basics/variables.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "tests/TourOfGo/basics/variables.h"
#include "gocpp/support.h"

#include "golang/fmt/errors.h"
#include "golang/fmt/print.h"

namespace golang::main
{
    namespace rec
    {
    }

    bool c;
    bool python;
    bool java;
    int ii = 3;
    int jj = 4;
    int kk = 5;
    bool debug = gocpp::init_multi(ParseBool("True"_s), _);
    gocpp::string v2;
    int v3;
    double v4;
    bool v1 = gocpp::init_multi(FourValues(), v2, v3, v4);
    gocpp::string u2;
    double u3;
    bool u1 = gocpp::init_multi(FourValues(), u2, _, u3);
    
    template<typename T> requires gocpp::GoStruct<T>
    pos::operator T()
    {
        T result;
        result.x = this->x;
        result.y = this->y;
        return result;
    }

    template<typename T> requires gocpp::GoStruct<T>
    bool pos::operator==(const T& ref) const
    {
        if (x != ref.x) return false;
        if (y != ref.y) return false;
        return true;
    }

    std::ostream& pos::PrintTo(std::ostream& os) const
    {
        os << '{';
        os << "" << x;
        os << " " << y;
        os << '}';
        return os;
    }

    std::ostream& operator<<(std::ostream& os, const struct pos& value)
    {
        return value.PrintTo(os);
    }

    std::function<void (void)> funcVar1 = []() mutable -> void
    {
        mocklib::Println("funcVar"_s);
    };
    std::function<int (int)> funcVar2 = [](int i) mutable -> int
    {
        mocklib::Println("funcVar,"_s, i);
        return 3;
    };
    std::function<std::tuple<int, gocpp::string> (int)> funcVar3 = [](int i) mutable -> std::tuple<int, gocpp::string>
    {
        mocklib::Println("funcVar,"_s, i);
        return {3, "zz"_s};
    };
    void main()
    {
        int i = {};
        mocklib::Println(i, c, python, java);
        mocklib::Println(ii, jj, kk);

        std::tie(ii, jj, kk) = std::tuple{6, 7, 8};
        mocklib::Println(ii, jj, kk);

        auto [ii, zz, kk] = std::tuple{9, 10, 11};
        mocklib::Println(ii, zz, kk);

        {
            auto [ii, zz, ll] = std::tuple{12, 13, 14};
            mocklib::Println(ii, zz, ll);
        }
        mocklib::Println(ii, zz, kk);

        if(true)
        {
            auto [ii, zz, ll] = std::tuple{15, 16, 17};
            mocklib::Println(ii, zz, ll);
        }
        mocklib::Println(ii, zz, kk);

        auto [go_do, go_while, go_template, go_class] = std::tuple{"do"_s, "while"_s, "template"_s, "class"_s};
        mocklib::Println(go_do, go_while, go_template, go_class);

        dummy_vars();
        funcVar1();
        ii = funcVar2(3);

        gocpp::string s = {};
        std::tie(ii, s) = funcVar3(3);
        mocklib::Println(ii, s);
        withNamedResults();
        inlineAssign(99);

        mocklib::Println(debug);
        mocklib::Println(v1, v2, v3, v4);
        mocklib::Println(u1, u2, u3);
    }

    std::tuple<int, gocpp::string> withNamedResults()
    {
        int i;
        gocpp::string s;
        i = 3;
        s = "hello"_s;
        auto p = gocpp::Init<pos>([=](auto& x) {
            x.x = 1;
            x.y = 2;
        });
        {
            auto j = i;
            auto i_tmp = 10 + i;
            auto& i = i_tmp;
            auto x = p.x;
            auto p_tmp = p.y;
            auto& p = p_tmp;
            auto k = i;
            auto s = "world"_s;
            mocklib::Println("withNamedResults:"_s, i, s, j, k, x, p);
        }
        mocklib::Println("withNamedResults:"_s, i, s);
        return {i, s};
    }

    void inlineAssign(int i)
    {
        if(auto [i, ok1] = std::tuple{1, true}; ok1)
        {
            mocklib::Println("inlineAssign:"_s, i);
        }
        mocklib::Println("inlineAssign:"_s, i);

        if(auto [i, ok2] = std::tuple{2, true}; ok2)
        {
            mocklib::Println("inlineAssign:"_s, i);
        }
        mocklib::Println("inlineAssign:"_s, i);

        if(auto [i, ok2] = std::tuple{22, true}; ok2)
        {
            mocklib::Println("inlineAssign:"_s, i);
        }

        {
            if(auto [i, ok1] = std::tuple{3, true}; ok1)
            {
                mocklib::Println("inlineAssign:"_s, i);
            }
            mocklib::Println("inlineAssign:"_s, i);
        }
        mocklib::Println("inlineAssign:"_s, i);

        {
            auto i = 66;
            if(auto [i, ok1] = std::tuple{4, true}; ok1)
            {
                mocklib::Println("inlineAssign:"_s, i);
            }
            mocklib::Println("inlineAssign:"_s, i);
        }
        mocklib::Println("inlineAssign:"_s, i);
    }

    void dummy_vars()
    {
        gocpp::string go_do = "do"_s;
        gocpp::string go_while = "while"_s;
        gocpp::string go_template = "template"_s;
        gocpp::string go_class = "class"_s;
        mocklib::Println(go_do, go_while, go_template, go_class);
    }

    void dummy_funcs()
    {
        go_do();
        go_while();
        go_template();
        go_class();
    }

    void go_do()
    {
    }

    void go_while()
    {
    }

    void go_template()
    {
    }

    void go_class()
    {
    }

    std::tuple<bool, gocpp::error> ParseBool(gocpp::string str)
    {
        //Go switch emulation
        {
            auto condition = str;
            int conditionId = -1;
            if(condition == "1"_s) { conditionId = 0; }
            else if(condition == "t"_s) { conditionId = 1; }
            else if(condition == "T"_s) { conditionId = 2; }
            else if(condition == "true"_s) { conditionId = 3; }
            else if(condition == "TRUE"_s) { conditionId = 4; }
            else if(condition == "True"_s) { conditionId = 5; }
            else if(condition == "0"_s) { conditionId = 6; }
            else if(condition == "f"_s) { conditionId = 7; }
            else if(condition == "F"_s) { conditionId = 8; }
            else if(condition == "false"_s) { conditionId = 9; }
            else if(condition == "FALSE"_s) { conditionId = 10; }
            else if(condition == "False"_s) { conditionId = 11; }
            switch(conditionId)
            {
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                    return {true, nullptr};
                    break;
                case 6:
                case 7:
                case 8:
                case 9:
                case 10:
                case 11:
                    return {false, nullptr};
                    break;
            }
        }
        return {false, mocklib::Errorf("ParseBool: %s"_s, str)};
    }

    std::tuple<bool, gocpp::string, int, double> FourValues()
    {
        return {true, "aaa"_s, 123, 1.23};
    }

}

int main()
{
    try
    {
        std::cout << std::boolalpha << std::setprecision(5) << std::fixed;
        golang::main::main();
        return 0;
    }
    catch(const gocpp::GoPanic& ex)
    {
        std::cout << "Panic: " << ex.what() << std::endl;
        return -1;
    }
}
