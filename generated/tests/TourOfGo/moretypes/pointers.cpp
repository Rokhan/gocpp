// generated by GoCpp from file 'tests/TourOfGo/moretypes/pointers.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "tests/TourOfGo/moretypes/pointers.h"
#include "gocpp/support.h"

#include "golang/fmt/print.h"

namespace golang::main
{
    namespace rec
    {
        using namespace mocklib::rec;
    }

    void main()
    {
        auto [i, j] = std::tuple{42, 2701};

        // point to i
        auto p = & i;
        // read i through the pointer
        mocklib::Println(*p);
        // set i through the pointer
        *p = 21;
        // see the new value of i
        mocklib::Println(i);

        // point to j
        p = & j;
        // divide j through the pointer
        *p = *p / 37;
        // see the new value of j
        mocklib::Println(j);
    }

}

int main()
{
    try
    {
        std::cout << std::boolalpha << std::setprecision(5) << std::fixed;
        golang::main::main();
        return 0;
    }
    catch(const gocpp::GoPanic& ex)
    {
        std::cout << "Panic: " << ex.what() << std::endl;
        return -1;
    }
}
