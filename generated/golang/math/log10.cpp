// generated by GoCpp from file '$(ImportDir)/math/log10.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "golang/math/log10.h"
#include "gocpp/support.h"

#include "golang/math/const.h"
#include "golang/math/frexp.h"
#include "golang/math/log.h"
#include "golang/math/stubs.h"

namespace golang::math
{
    namespace rec
    {
    }

    // Log10 returns the decimal logarithm of x.
    // The special cases are the same as for [Log].
    double Log10(double x)
    {
        if(haveArchLog10)
        {
            return archLog10(x);
        }
        return log10(x);
    }

    double log10(double x)
    {
        return Log(x) * (1 / Ln10);
    }

    // Log2 returns the binary logarithm of x.
    // The special cases are the same as for [Log].
    double Log2(double x)
    {
        if(haveArchLog2)
        {
            return archLog2(x);
        }
        return log2(x);
    }

    double log2(double x)
    {
        auto [frac, exp] = Frexp(x);
        // Make sure exact powers of two give an exact answer.
        // Don't depend on Log(0.5)*(1/Ln2)+exp being exactly exp-1.
        if(frac == 0.5)
        {
            return double(exp - 1);
        }
        return Log(frac) * (1 / Ln2) + double(exp);
    }

}

