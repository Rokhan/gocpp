// generated by GoCpp from file 'tests/TourOfGo/concurrency/range-and-close.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "tests/TourOfGo/concurrency/range-and-close.h"
#include "gocpp/support.h"

#include "golang/fmt/print.h"

namespace golang::main
{
    namespace rec
    {
        using namespace mocklib::rec;
        using namespace fmt::rec;
    }

    void fibonacci(int n, gocpp::channel<int> c)
    {
        auto [x, y] = std::tuple{0, 1};
        for(auto i = 0; i < n; i++)
        {
            c.send(x);
            std::tie(x, y) = std::tuple{y, x + y};
        }
        close(c);
    }

    void main()
    {
        auto c = gocpp::make(gocpp::Tag<gocpp::channel<int>>(), 10);
        gocpp::go([&]{ fibonacci(cap(c), c); });
        for(auto [i, gocpp_ignored] : c)
        {
            mocklib::Println(i);
        }
    }

}

int main()
{
    try
    {
        std::cout << std::boolalpha << std::fixed << std::setprecision(5);
        golang::main::main();
        return 0;
    }
    catch(const gocpp::GoPanic& ex)
    {
        std::cout << "Panic: " << ex.what() << std::endl;
        return -1;
    }
}
