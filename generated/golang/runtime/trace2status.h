// generated by GoCpp from file '$(ImportDir)/runtime/trace2status.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/runtime/trace2status.fwd.h"
#include "gocpp/support.h"

#include "golang/runtime/internal/atomic/types.h"
#include "golang/runtime/runtime2.h"

namespace golang::runtime
{
    traceGoStatus goStatusToTraceGoStatus(uint32_t status, waitReason wr);
    struct traceSchedResourceState
    {
        // statusTraced indicates whether a status event was traced for this resource
        // a particular generation.
        // There are 3 of these because when transitioning across generations, traceAdvance
        // needs to be able to reliably observe whether a status was traced for the previous
        // generation, while we need to clear the value for the next generation.
        gocpp::array<atomic::Uint32, 3> statusTraced{};
        // seq is the sequence counter for this scheduling resource's events.
        // The purpose of the sequence counter is to establish a partial order between
        // events that don't obviously happen serially (same M) in the stream ofevents.
        // There are two of these so that we can reset the counter on each generation.
        // This saves space in the resulting trace by keeping the counter small and allows
        // GoStatus and GoCreate events to omit a sequence number (implicitly 0).
        gocpp::array<uint64_t, 2> seq{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct traceSchedResourceState& value);
}

#include "golang/runtime/runtime2.h"
#include "golang/runtime/trace2buf.h"
#include "golang/runtime/trace2event.h"

namespace golang::runtime
{

    namespace rec
    {
        traceWriter writeGoStatus(traceWriter w, uint64_t goid, int64_t mid, traceGoStatus status, bool markAssist);
        traceWriter writeProcStatusForP(traceWriter w, golang::runtime::p* pp, bool inSTW);
        traceWriter writeProcStatus(traceWriter w, uint64_t pid, traceProcStatus status, bool inSweep);
        bool acquireStatus(traceSchedResourceState* r, uintptr_t gen);
        void readyNextGen(traceSchedResourceState* r, uintptr_t gen);
        bool statusWasTraced(traceSchedResourceState* r, uintptr_t gen);
        void setStatusTraced(traceSchedResourceState* r, uintptr_t gen);
        traceArg nextSeq(traceSchedResourceState* r, uintptr_t gen);
    }
}

