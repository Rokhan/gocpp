// generated by GoCpp from file '$(ImportDir)/go/types/typelists.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "golang/go/types/typelists.h"
#include "gocpp/support.h"

#include "golang/go/types/type.h"
#include "golang/go/types/typeparam.h"

namespace golang::types
{
    namespace rec
    {
    }

    // TypeParamList holds a list of type parameters.
    
    template<typename T> requires gocpp::GoStruct<T>
    TypeParamList::operator T()
    {
        T result;
        result.tparams = this->tparams;
        return result;
    }

    template<typename T> requires gocpp::GoStruct<T>
    bool TypeParamList::operator==(const T& ref) const
    {
        if (tparams != ref.tparams) return false;
        return true;
    }

    std::ostream& TypeParamList::PrintTo(std::ostream& os) const
    {
        os << '{';
        os << "" << tparams;
        os << '}';
        return os;
    }

    std::ostream& operator<<(std::ostream& os, const struct TypeParamList& value)
    {
        return value.PrintTo(os);
    }

    // Len returns the number of type parameters in the list.
    // It is safe to call on a nil receiver.
    int rec::Len(TypeParamList* l)
    {
        return len(rec::list(gocpp::recv(l)));
    }

    // At returns the i'th type parameter in the list.
    TypeParam* rec::At(TypeParamList* l, int i)
    {
        return l->tparams[i];
    }

    // list is for internal use where we expect a []*TypeParam.
    // TODO(rfindley): list should probably be eliminated: we can pass around a
    // TypeParamList instead.
    gocpp::slice<TypeParam*> rec::list(TypeParamList* l)
    {
        if(l == nullptr)
        {
            return nullptr;
        }
        return l->tparams;
    }

    // TypeList holds a list of types.
    
    template<typename T> requires gocpp::GoStruct<T>
    TypeList::operator T()
    {
        T result;
        result.types = this->types;
        return result;
    }

    template<typename T> requires gocpp::GoStruct<T>
    bool TypeList::operator==(const T& ref) const
    {
        if (types != ref.types) return false;
        return true;
    }

    std::ostream& TypeList::PrintTo(std::ostream& os) const
    {
        os << '{';
        os << "" << types;
        os << '}';
        return os;
    }

    std::ostream& operator<<(std::ostream& os, const struct TypeList& value)
    {
        return value.PrintTo(os);
    }

    // newTypeList returns a new TypeList with the types in list.
    TypeList* newTypeList(gocpp::slice<golang::types::Type> list)
    {
        if(len(list) == 0)
        {
            return nullptr;
        }
        return new TypeList {list};
    }

    // Len returns the number of types in the list.
    // It is safe to call on a nil receiver.
    int rec::Len(TypeList* l)
    {
        return len(rec::list(gocpp::recv(l)));
    }

    // At returns the i'th type in the list.
    golang::types::Type rec::At(TypeList* l, int i)
    {
        return l->types[i];
    }

    // list is for internal use where we expect a []Type.
    // TODO(rfindley): list should probably be eliminated: we can pass around a
    // TypeList instead.
    gocpp::slice<golang::types::Type> rec::list(TypeList* l)
    {
        if(l == nullptr)
        {
            return nullptr;
        }
        return l->types;
    }

    TypeParamList* bindTParams(gocpp::slice<TypeParam*> list)
    {
        if(len(list) == 0)
        {
            return nullptr;
        }
        for(auto [i, typ] : list)
        {
            if(typ->index >= 0)
            {
                gocpp::panic("type parameter bound more than once"_s);
            }
            typ->index = i;
        }
        return gocpp::InitPtr<TypeParamList>([=](auto& x) {
            x.tparams = list;
        });
    }

}

