// generated by GoCpp from file '$(ImportDir)/golang.org/x/tools/internal/gcimporter/support_go118.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "golang/golang.org/x/tools/internal/gcimporter/support_go118.h"
#include "gocpp/support.h"

#include "golang/go/types/object.h"
#include "golang/go/types/scope.h"
#include "golang/go/types/type.h"
#include "golang/go/types/universe.h"
#include "golang/golang.org/x/tools/internal/gcimporter/iimport.h"

namespace golang::gcimporter
{
    namespace rec
    {
        using types::rec::Lookup;
        using types::rec::Type;
    }

    // additionalPredeclared returns additional predeclared types in go.1.18.
    gocpp::slice<types::Type> additionalPredeclared()
    {
        return gocpp::slice<types::Type> {
            // comparable
            rec::Type(gocpp::recv(rec::Lookup(gocpp::recv(types::Universe), "comparable"_s))),

            // any
            rec::Type(gocpp::recv(rec::Lookup(gocpp::recv(types::Universe), "any"_s)))
        };
    }

    // See cmd/compile/internal/types.SplitVargenSuffix.
    std::tuple<gocpp::string, gocpp::string> splitVargenSuffix(gocpp::string name)
    {
        gocpp::string base;
        gocpp::string suffix;
        auto i = len(name);
        for(; i > 0 && name[i - 1] >= '0' && name[i - 1] <= '9'; )
        {
            i--;
        }
        auto dot = "·"_s;
        if(i >= len(dot) && name.make_slice(i - len(dot), i) == dot)
        {
            i -= len(dot);
            return {name.make_slice(0, i), name.make_slice(i)};
        }
        return {name, ""_s};
    }

}

