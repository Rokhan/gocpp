// generated by GoCpp from file '$(ImportDir)/time/tick.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "golang/time/tick.h"
#include "gocpp/support.h"

#include "golang/time/sleep.h"
#include "golang/time/time.h"

namespace golang::time
{
    
    std::ostream& Ticker::PrintTo(std::ostream& os) const
    {
        os << '{';
        os << "" << C;
        os << " " << r;
        os << '}';
        return os;
    }

    std::ostream& operator<<(std::ostream& os, const struct Ticker& value)
    {
        return value.PrintTo(os);
    }

    Ticker* NewTicker(Duration d)
    {
        if(d <= 0)
        {
            gocpp::panic("non-positive interval for NewTicker");
        }
        auto c = gocpp::make(gocpp::Tag<gocpp::channel<Time>>(), 1);
        auto t = gocpp::InitPtr<Ticker>([](Ticker& x) { x.C = c; x.r = gocpp::Init<runtimeTimer>([](runtimeTimer& x) { x.when = when(d); x.period = int64(d); x.f = sendTime; x.arg = c; }); });
        startTimer(& t->r);
        return t;
    }

    void Stop(struct Ticker* t)
    {
        stopTimer(& t->r);
    }

    void Reset(struct Ticker* t, Duration d)
    {
        if(d <= 0)
        {
            gocpp::panic("non-positive interval for Ticker.Reset");
        }
        if(t->r.f == nullptr)
        {
            gocpp::panic("time: Reset called on uninitialized Ticker");
        }
        modTimer(& t->r, when(d), int64(d), t->r.f, t->r.arg, t->r.seq);
    }

    gocpp::channel<Time> Tick(Duration d)
    {
        if(d <= 0)
        {
            return nullptr;
        }
        return NewTicker(d)->C;
    }

}

