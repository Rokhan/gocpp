// generated by GoCpp from file '$(ImportDir)/regexp/exec.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/regexp/exec.fwd.h"
#include "gocpp/support.h"


namespace golang::regexp
{
    struct entry
    {
        uint32_t pc{};
        thread* t{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct entry& value);
    lazyFlag newLazyFlag(gocpp::rune r1, gocpp::rune r2);
    extern gocpp::array<int, 0> arrayNoInts;
    struct queue
    {
        gocpp::slice<uint32_t> sparse{};
        gocpp::slice<entry> dense{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct queue& value);
}
#include "golang/regexp/regexp.h"
#include "golang/sync/pool.h"
#include "golang/regexp/syntax/prog.fwd.h"

namespace golang::regexp
{
    struct thread
    {
        syntax::Inst* inst{};
        gocpp::slice<int> cap{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct thread& value);
    struct inputs
    {
        // cached inputs, to avoid allocation
        inputBytes bytes{};
        inputString string{};
        inputReader reader{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct inputs& value);
    extern sync::Pool onePassPool;
    struct onePassMachine
    {
        inputs inputs{};
        gocpp::slice<int> matchcap{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct onePassMachine& value);
    struct machine
    {
        Regexp* re{}; // corresponding Regexp
        syntax::Prog* p{}; // compiled program
        queue q0{}; // two queues for runq, nextq
        queue q1{};
        gocpp::slice<thread*> pool{}; // pool of available threads
        bool matched{}; // whether a match was found
        gocpp::slice<int> matchcap{}; // capture information for the match
        inputs inputs{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct machine& value);
    onePassMachine* newOnePassMachine();
    void freeOnePassMachine(onePassMachine* m);
}

#include "golang/io/io.h"
#include "golang/regexp/regexp.h"
#include "golang/regexp/syntax/prog.h"

namespace golang::regexp
{

    namespace rec
    {
        input newBytes(inputs* i, gocpp::slice<unsigned char> b);
        input newString(inputs* i, gocpp::string s);
        input newReader(inputs* i, io::RuneReader r);
        void clear(inputs* i);
        std::tuple<input, int> init(inputs* i, io::RuneReader r, gocpp::slice<unsigned char> b, gocpp::string s);
        void init(machine* m, int ncap);
        thread* alloc(machine* m, syntax::Inst* i);
        bool match(lazyFlag f, syntax::EmptyOp op);
        bool match(machine* m, input i, int pos);
        void clear(machine* m, queue* q);
        void step(machine* m, queue* runq, queue* nextq, int pos, int nextPos, gocpp::rune c, lazyFlag* nextCond);
        thread* add(machine* m, queue* q, uint32_t pc, int pos, gocpp::slice<int> cap, lazyFlag* cond, thread* t);
        gocpp::slice<int> doOnePass(Regexp* re, io::RuneReader ir, gocpp::slice<unsigned char> ib, gocpp::string is, int pos, int ncap, gocpp::slice<int> dstCap);
        bool doMatch(Regexp* re, io::RuneReader r, gocpp::slice<unsigned char> b, gocpp::string s);
        gocpp::slice<int> doExecute(Regexp* re, io::RuneReader r, gocpp::slice<unsigned char> b, gocpp::string s, int pos, int ncap, gocpp::slice<int> dstCap);
    }
}

