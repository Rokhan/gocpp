// generated by GoCpp from file 'tests/TourOfGo/concurrency/buffered-channels.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "tests/TourOfGo/concurrency/buffered-channels.h"
#include "gocpp/support.h"

// #include "golang/fmt/print.h"  [Ignored, known errors]

namespace golang::main
{
    void main()
    {
        auto ch = gocpp::make(gocpp::Tag<gocpp::channel<int>>(), 2);
        ch.send(1);
        ch.send(2);
        mocklib::Println(ch.recv());
        mocklib::Println(ch.recv());
    }

}

int main()
{
    try
    {
        std::cout << std::boolalpha << std::fixed << std::setprecision(5);
        golang::main::main();
        return 0;
    }
    catch(const gocpp::GoPanic& ex)
    {
        std::cout << "Panic: " << ex.what() << std::endl;
        return -1;
    }
}
