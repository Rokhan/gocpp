// generated by GoCpp from file '$(ImportDir)/golang.org/x/tools/internal/pkgbits/decoder.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/golang.org/x/tools/internal/pkgbits/decoder.fwd.h"
#include "gocpp/support.h"

#include "golang/golang.org/x/tools/internal/pkgbits/reloc.h"
#include "golang/strings/reader.h"
#include "golang/errors/errors.fwd.h"

namespace golang::pkgbits
{
    struct PkgDecoder
    {
        // version is the file format version.
        uint32_t version{};
        // aliases determines whether types.Aliases should be created
        bool aliases{};
        // sync indicates whether the file uses sync markers.
        bool sync{};
        // pkgPath is the package path for the package to be decoded.
        // TODO(mdempsky): Remove; unneeded since CL 391014.
        gocpp::string pkgPath{};
        // elemData is the full data payload of the encoded package.
        // Elements are densely and contiguously packed together.
        // The last 8 bytes of elemData are the package fingerprint.
        gocpp::string elemData{};
        // elemEnds stores the byte-offset end positions of element
        // bitstreams within elemData.
        // For example, element I's bitstream data starts at elemEnds[I-1]
        // (or 0, if I==0) and ends at elemEnds[I].
        // Note: elemEnds is indexed by absolute indices, not
        // section-relative indices.
        gocpp::slice<uint32_t> elemEnds{};
        // elemEndsEnds stores the index-offset end positions of relocation
        // sections within elemEnds.
        // For example, section K's end positions start at elemEndsEnds[K-1]
        // (or 0, if K==0) and end at elemEndsEnds[K].
        gocpp::array<uint32_t, numRelocs> elemEndsEnds{};
        gocpp::slice<RelocEnt> scratchRelocEnt{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct PkgDecoder& value);
    struct Decoder
    {
        PkgDecoder* common{};
        gocpp::slice<RelocEnt> Relocs{};
        strings::Reader Data{};
        RelocKind k{};
        Index Idx{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct Decoder& value);
    std::tuple<uint64_t, gocpp::error> readUvarint(strings::Reader* r);
    extern gocpp::error overflow;
    PkgDecoder NewPkgDecoder(gocpp::string pkgPath, gocpp::string input);
}

#include "golang/go/constant/value.h"
#include "golang/golang.org/x/tools/internal/pkgbits/codes.h"
#include "golang/golang.org/x/tools/internal/pkgbits/reloc.h"
#include "golang/golang.org/x/tools/internal/pkgbits/sync.h"
#include "golang/math/big/float.h"
#include "golang/math/big/int.h"

namespace golang::pkgbits
{

    namespace rec
    {
        gocpp::string PkgPath(PkgDecoder* pr);
        bool SyncMarkers(PkgDecoder* pr);
        int NumElems(PkgDecoder* pr, RelocKind k);
        int TotalElems(PkgDecoder* pr);
        gocpp::array<unsigned char, 8> Fingerprint(PkgDecoder* pr);
        int AbsIdx(PkgDecoder* pr, RelocKind k, Index idx);
        gocpp::string DataIdx(PkgDecoder* pr, RelocKind k, Index idx);
        gocpp::string StringIdx(PkgDecoder* pr, Index idx);
        Decoder NewDecoder(PkgDecoder* pr, RelocKind k, Index idx, SyncMarker marker);
        Decoder TempDecoder(PkgDecoder* pr, RelocKind k, Index idx, SyncMarker marker);
        void RetireDecoder(PkgDecoder* pr, Decoder* d);
        Decoder NewDecoderRaw(PkgDecoder* pr, RelocKind k, Index idx);
        Decoder TempDecoderRaw(PkgDecoder* pr, RelocKind k, Index idx);
        void checkErr(Decoder* r, gocpp::error err);
        uint64_t rawUvarint(Decoder* r);
        int64_t rawVarint(Decoder* r);
        Index rawReloc(Decoder* r, RelocKind k, int idx);
        void Sync(Decoder* r, SyncMarker mWant);
        bool Bool(Decoder* r);
        int64_t Int64(Decoder* r);
        uint64_t Uint64(Decoder* r);
        int Len(Decoder* r);
        int Int(Decoder* r);
        unsigned int Uint(Decoder* r);
        int Code(Decoder* r, SyncMarker mark);
        Index Reloc(Decoder* r, RelocKind k);
        gocpp::string String(Decoder* r);
        gocpp::slice<gocpp::string> Strings(Decoder* r);
        constant::Value Value(Decoder* r);
        constant::Value scalar(Decoder* r);
        big::Int* bigInt(Decoder* r);
        big::Float* bigFloat(Decoder* r);
        gocpp::string PeekPkgPath(PkgDecoder* pr, Index idx);
        std::tuple<gocpp::string, gocpp::string, CodeObj> PeekObj(PkgDecoder* pr, Index idx);
    }
}

