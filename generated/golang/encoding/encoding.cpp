// generated by GoCpp from file '$(ImportDir)/encoding/encoding.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "golang/encoding/encoding.h"
#include "gocpp/support.h"

// Package encoding defines interfaces shared by other packages that
// convert data to and from byte-level and textual representations.
// Packages that check for these interfaces include encoding/gob,
// encoding/json, and encoding/xml. As a result, implementing an
// interface once can make a type useful in multiple encodings.
// Standard types that implement these interfaces include time.Time and net.IP.
// The interfaces come in pairs that produce and consume encoded data.
//
// Adding encoding/decoding methods to existing types may constitute a breaking change,
// as they can be used for serialization in communicating with programs
// written with different library versions.
// The policy for packages maintained by the Go project is to only allow
// the addition of marshaling functions if no existing, reasonable marshaling exists.
namespace golang::encoding
{
    namespace rec
    {
    }

    // BinaryMarshaler is the interface implemented by an object that can
    // marshal itself into a binary form.
    //
    // MarshalBinary encodes the receiver into a binary form and returns the result.
    
    template<typename T>
    BinaryMarshaler::BinaryMarshaler(T& ref)
    {
        mValue.reset(new BinaryMarshalerImpl<T, std::unique_ptr<T>>(new T(ref)));
    }

    template<typename T>
    BinaryMarshaler::BinaryMarshaler(const T& ref)
    {
        mValue.reset(new BinaryMarshalerImpl<T, std::unique_ptr<T>>(new T(ref)));
    }

    template<typename T>
    BinaryMarshaler::BinaryMarshaler(T* ptr)
    {
        mValue.reset(new BinaryMarshalerImpl<T, gocpp::ptr<T>>(ptr));
    }

    std::ostream& BinaryMarshaler::PrintTo(std::ostream& os) const
    {
        return os;
    }

    template<typename T, typename TStore, typename TInterface>
    std::tuple<gocpp::slice<unsigned char>, gocpp::error> BinaryMarshaler::BinaryMarshalerImpl<T, TStore, TInterface>::vMarshalBinary()
    {
        return rec::MarshalBinary(gocpp::PtrRecv<T, false>(value.get()));
    }

    inline BinaryMarshaler::IBinaryMarshaler* BinaryMarshaler::value() const
    {
        if(auto res = mValue.get()) { return res; }
        throw gocpp::GoPanic("using nil value for interface 'BinaryMarshaler'");
    }

    namespace rec
    {
        std::tuple<gocpp::slice<unsigned char>, gocpp::error> MarshalBinary(const gocpp::PtrRecv<struct BinaryMarshaler, false>& self)
        {
            return self.ptr->value()->vMarshalBinary();
        }

        std::tuple<gocpp::slice<unsigned char>, gocpp::error> MarshalBinary(const gocpp::ObjRecv<struct BinaryMarshaler>& self)
        {
            return self.obj.value()->vMarshalBinary();
        }
    }

    std::ostream& operator<<(std::ostream& os, const struct BinaryMarshaler& value)
    {
        return value.PrintTo(os);
    }

    // BinaryUnmarshaler is the interface implemented by an object that can
    // unmarshal a binary representation of itself.
    //
    // UnmarshalBinary must be able to decode the form generated by MarshalBinary.
    // UnmarshalBinary must copy the data if it wishes to retain the data
    // after returning.
    
    template<typename T>
    BinaryUnmarshaler::BinaryUnmarshaler(T& ref)
    {
        mValue.reset(new BinaryUnmarshalerImpl<T, std::unique_ptr<T>>(new T(ref)));
    }

    template<typename T>
    BinaryUnmarshaler::BinaryUnmarshaler(const T& ref)
    {
        mValue.reset(new BinaryUnmarshalerImpl<T, std::unique_ptr<T>>(new T(ref)));
    }

    template<typename T>
    BinaryUnmarshaler::BinaryUnmarshaler(T* ptr)
    {
        mValue.reset(new BinaryUnmarshalerImpl<T, gocpp::ptr<T>>(ptr));
    }

    std::ostream& BinaryUnmarshaler::PrintTo(std::ostream& os) const
    {
        return os;
    }

    template<typename T, typename TStore, typename TInterface>
    gocpp::error BinaryUnmarshaler::BinaryUnmarshalerImpl<T, TStore, TInterface>::vUnmarshalBinary(gocpp::slice<unsigned char> data)
    {
        return rec::UnmarshalBinary(gocpp::PtrRecv<T, false>(value.get()), data);
    }

    inline BinaryUnmarshaler::IBinaryUnmarshaler* BinaryUnmarshaler::value() const
    {
        if(auto res = mValue.get()) { return res; }
        throw gocpp::GoPanic("using nil value for interface 'BinaryUnmarshaler'");
    }

    namespace rec
    {
        gocpp::error UnmarshalBinary(const gocpp::PtrRecv<struct BinaryUnmarshaler, false>& self, gocpp::slice<unsigned char> data)
        {
            return self.ptr->value()->vUnmarshalBinary(data);
        }

        gocpp::error UnmarshalBinary(const gocpp::ObjRecv<struct BinaryUnmarshaler>& self, gocpp::slice<unsigned char> data)
        {
            return self.obj.value()->vUnmarshalBinary(data);
        }
    }

    std::ostream& operator<<(std::ostream& os, const struct BinaryUnmarshaler& value)
    {
        return value.PrintTo(os);
    }

    // TextMarshaler is the interface implemented by an object that can
    // marshal itself into a textual form.
    //
    // MarshalText encodes the receiver into UTF-8-encoded text and returns the result.
    
    template<typename T>
    TextMarshaler::TextMarshaler(T& ref)
    {
        mValue.reset(new TextMarshalerImpl<T, std::unique_ptr<T>>(new T(ref)));
    }

    template<typename T>
    TextMarshaler::TextMarshaler(const T& ref)
    {
        mValue.reset(new TextMarshalerImpl<T, std::unique_ptr<T>>(new T(ref)));
    }

    template<typename T>
    TextMarshaler::TextMarshaler(T* ptr)
    {
        mValue.reset(new TextMarshalerImpl<T, gocpp::ptr<T>>(ptr));
    }

    std::ostream& TextMarshaler::PrintTo(std::ostream& os) const
    {
        return os;
    }

    template<typename T, typename TStore, typename TInterface>
    std::tuple<gocpp::slice<unsigned char>, gocpp::error> TextMarshaler::TextMarshalerImpl<T, TStore, TInterface>::vMarshalText()
    {
        return rec::MarshalText(gocpp::PtrRecv<T, false>(value.get()));
    }

    inline TextMarshaler::ITextMarshaler* TextMarshaler::value() const
    {
        if(auto res = mValue.get()) { return res; }
        throw gocpp::GoPanic("using nil value for interface 'TextMarshaler'");
    }

    namespace rec
    {
        std::tuple<gocpp::slice<unsigned char>, gocpp::error> MarshalText(const gocpp::PtrRecv<struct TextMarshaler, false>& self)
        {
            return self.ptr->value()->vMarshalText();
        }

        std::tuple<gocpp::slice<unsigned char>, gocpp::error> MarshalText(const gocpp::ObjRecv<struct TextMarshaler>& self)
        {
            return self.obj.value()->vMarshalText();
        }
    }

    std::ostream& operator<<(std::ostream& os, const struct TextMarshaler& value)
    {
        return value.PrintTo(os);
    }

    // TextUnmarshaler is the interface implemented by an object that can
    // unmarshal a textual representation of itself.
    //
    // UnmarshalText must be able to decode the form generated by MarshalText.
    // UnmarshalText must copy the text if it wishes to retain the text
    // after returning.
    
    template<typename T>
    TextUnmarshaler::TextUnmarshaler(T& ref)
    {
        mValue.reset(new TextUnmarshalerImpl<T, std::unique_ptr<T>>(new T(ref)));
    }

    template<typename T>
    TextUnmarshaler::TextUnmarshaler(const T& ref)
    {
        mValue.reset(new TextUnmarshalerImpl<T, std::unique_ptr<T>>(new T(ref)));
    }

    template<typename T>
    TextUnmarshaler::TextUnmarshaler(T* ptr)
    {
        mValue.reset(new TextUnmarshalerImpl<T, gocpp::ptr<T>>(ptr));
    }

    std::ostream& TextUnmarshaler::PrintTo(std::ostream& os) const
    {
        return os;
    }

    template<typename T, typename TStore, typename TInterface>
    gocpp::error TextUnmarshaler::TextUnmarshalerImpl<T, TStore, TInterface>::vUnmarshalText(gocpp::slice<unsigned char> text)
    {
        return rec::UnmarshalText(gocpp::PtrRecv<T, false>(value.get()), text);
    }

    inline TextUnmarshaler::ITextUnmarshaler* TextUnmarshaler::value() const
    {
        if(auto res = mValue.get()) { return res; }
        throw gocpp::GoPanic("using nil value for interface 'TextUnmarshaler'");
    }

    namespace rec
    {
        gocpp::error UnmarshalText(const gocpp::PtrRecv<struct TextUnmarshaler, false>& self, gocpp::slice<unsigned char> text)
        {
            return self.ptr->value()->vUnmarshalText(text);
        }

        gocpp::error UnmarshalText(const gocpp::ObjRecv<struct TextUnmarshaler>& self, gocpp::slice<unsigned char> text)
        {
            return self.obj.value()->vUnmarshalText(text);
        }
    }

    std::ostream& operator<<(std::ostream& os, const struct TextUnmarshaler& value)
    {
        return value.PrintTo(os);
    }

}

