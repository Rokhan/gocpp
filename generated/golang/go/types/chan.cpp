// generated by GoCpp from file '$(ImportDir)/go/types/chan.go'
#include <complex>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <string>
#include <tuple>
#include <vector>

#include "golang/go/types/chan.h"
#include "gocpp/support.h"

#include "golang/go/types/type.h"
#include "golang/go/types/typestring.h"

namespace golang::types
{
    namespace rec
    {
    }

    // A Chan represents a channel type.
    
    template<typename T> requires gocpp::GoStruct<T>
    Chan::operator T()
    {
        T result;
        result.dir = this->dir;
        result.elem = this->elem;
        return result;
    }

    template<typename T> requires gocpp::GoStruct<T>
    bool Chan::operator==(const T& ref) const
    {
        if (dir != ref.dir) return false;
        if (elem != ref.elem) return false;
        return true;
    }

    std::ostream& Chan::PrintTo(std::ostream& os) const
    {
        os << '{';
        os << "" << dir;
        os << " " << elem;
        os << '}';
        return os;
    }

    std::ostream& operator<<(std::ostream& os, const struct Chan& value)
    {
        return value.PrintTo(os);
    }

    // A ChanDir value indicates a channel direction.
    // NewChan returns a new channel type for the given direction and element type.
    Chan* NewChan(ChanDir dir, golang::types::Type elem)
    {
        return gocpp::InitPtr<Chan>([=](auto& x) {
            x.dir = dir;
            x.elem = elem;
        });
    }

    // Dir returns the direction of channel c.
    ChanDir rec::Dir(Chan* c)
    {
        return c->dir;
    }

    // Elem returns the element type of channel c.
    golang::types::Type rec::Elem(Chan* c)
    {
        return c->elem;
    }

    golang::types::Type rec::Underlying(Chan* c)
    {
        return c;
    }

    gocpp::string rec::String(Chan* c)
    {
        return TypeString(c, nullptr);
    }

}

