// generated by GoCpp from file '$(ImportDir)/go/types/package.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/go/types/package.fwd.h"
#include "gocpp/support.h"

#include "golang/go/types/scope.fwd.h"

namespace golang::types
{
    struct Package
    {
        gocpp::string path{};
        gocpp::string name{};
        golang::types::Scope* scope{};
        gocpp::slice<Package*> imports{};
        bool complete{};
        bool fake{}; // scope lookup errors are silently dropped if package is fake (internal use only)
        bool cgo{}; // uses of this package will be rewritten into uses of declarations from _cgo_gotypes.go
        gocpp::string goVersion{}; // minimum Go version required for package (by Config.GoVersion, typically from go.mod)

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct Package& value);
    Package* NewPackage(gocpp::string path, gocpp::string name);
}

#include "golang/go/types/scope.h"

namespace golang::types
{

    namespace rec
    {
        gocpp::string Path(Package* pkg);
        gocpp::string Name(Package* pkg);
        void SetName(Package* pkg, gocpp::string name);
        gocpp::string GoVersion(Package* pkg);
        golang::types::Scope* Scope(Package* pkg);
        bool Complete(Package* pkg);
        void MarkComplete(Package* pkg);
        gocpp::slice<Package*> Imports(Package* pkg);
        void SetImports(Package* pkg, gocpp::slice<Package*> list);
        gocpp::string String(Package* pkg);
    }
}

