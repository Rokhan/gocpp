// generated by GoCpp from file '$(ImportDir)/os/exec.go'
#pragma once

#include <functional>
#include <string>
#include <tuple>
#include <vector>

#include "golang/os/exec.fwd.h"
#include "gocpp/support.h"


namespace golang::os
{
    struct Signal : virtual gocpp::Interface
    {
        using gocpp::Interface::operator==;
        using gocpp::Interface::operator!=;

        Signal(){}
        Signal(Signal& i) = default;
        Signal(const Signal& i) = default;
        Signal& operator=(Signal& i) = default;
        Signal& operator=(const Signal& i) = default;

        inline Signal(nullptr_t) {};
        Signal& operator=(nullptr_t) { mValue.reset(); }

        template<typename T>
        Signal(T& ref);

        template<typename T>
        Signal(const T& ref);

        template<typename T>
        Signal(T* ptr);

        using isGoInterface = void;

        std::ostream& PrintTo(std::ostream& os) const;

        struct ISignal
        {
            virtual gocpp::string vString() = 0;
            virtual void vSignal() = 0; // to distinguish from other Stringers
            virtual void* getPtr() = 0;
        };

        template<typename T, typename TStore, typename TInterface = ISignal>
        struct SignalImpl : virtual TInterface
        {
            explicit SignalImpl(T* ptr)
            {
                value.reset(ptr);
            }

            gocpp::string vString() override;

            void vSignal() override;

            void* getPtr() override
            {
                return value.get();
            }

            TStore value;
        };

        inline ISignal* value() const;

        std::shared_ptr<ISignal> mValue;
    };

    namespace rec
    {
        gocpp::string String(const gocpp::PtrRecv<struct Signal, false>& self);
        gocpp::string String(const gocpp::ObjRecv<struct Signal>& self);

        void Signal(const gocpp::PtrRecv<struct Signal, false>& self);
        void Signal(const gocpp::ObjRecv<struct Signal>& self);
    }

    std::ostream& operator<<(std::ostream& os, const struct Signal& value);
    int Getpid();
    int Getppid();
}
#include "golang/sync/atomic/type.h"
#include "golang/sync/rwmutex.h"
#include "golang/errors/errors.fwd.h"
#include "golang/os/types.fwd.h"
#include "golang/syscall/exec_windows.fwd.h"

namespace golang::os
{
    extern gocpp::error ErrProcessDone;
    struct Process
    {
        int Pid{};
        uintptr_t handle{}; // handle is accessed atomically on Windows
        atomic::Bool isdone{}; // process has been successfully waited on
        sync::RWMutex sigMu{}; // avoid race between wait and signal

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct Process& value);
    struct ProcAttr
    {
        // If Dir is non-empty, the child changes into the directory before
        // creating the process.
        gocpp::string Dir{};
        // If Env is non-nil, it gives the environment variables for the
        // new process in the form returned by Environ.
        // If it is nil, the result of Environ will be used.
        gocpp::slice<gocpp::string> Env{};
        // Files specifies the open files inherited by the new process. The
        // first three entries correspond to standard input, standard output, and
        // standard error. An implementation may support additional entries,
        // depending on the underlying operating system. A nil entry corresponds
        // to that file being closed when the process starts.
        // On Unix systems, StartProcess will change these File values
        // to blocking mode, which means that SetDeadline will stop working
        // and calling Close will not interrupt a Read or Write.
        gocpp::slice<File*> Files{};
        // Operating system-specific process creation attributes.
        // Note that setting this field means that your program
        // may not execute properly or even compile on some
        // operating systems.
        syscall::SysProcAttr* Sys{};

        using isGoStruct = void;

        template<typename T> requires gocpp::GoStruct<T>
        operator T();

        template<typename T> requires gocpp::GoStruct<T>
        bool operator==(const T& ref) const;

        std::ostream& PrintTo(std::ostream& os) const;
    };

    std::ostream& operator<<(std::ostream& os, const struct ProcAttr& value);
    Process* newProcess(int pid, uintptr_t handle);
    std::tuple<Process*, gocpp::error> FindProcess(int pid);
    std::tuple<Process*, gocpp::error> StartProcess(gocpp::string name, gocpp::slice<gocpp::string> argv, ProcAttr* attr);
}

#include "golang/os/exec_posix.h"
#include "golang/time/time.h"

namespace golang::os
{

    namespace rec
    {
        void setDone(Process* p);
        bool done(Process* p);
        gocpp::error Release(Process* p);
        gocpp::error Kill(Process* p);
        std::tuple<ProcessState*, gocpp::error> Wait(Process* p);
        gocpp::error Signal(Process* p, golang::os::Signal sig);
        time::Duration UserTime(ProcessState* p);
        time::Duration SystemTime(ProcessState* p);
        bool Exited(ProcessState* p);
        bool Success(ProcessState* p);
        go_any Sys(ProcessState* p);
        go_any SysUsage(ProcessState* p);
    }
}

